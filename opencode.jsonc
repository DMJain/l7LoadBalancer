{
  "$schema": "https://opencode.ai/config.json",
  // LSP is off by default in OpenCode. Turning on all built-in servers gives
  // gopls (resolved via the `go` toolchain this repo already requires) to
  // agents working through OpenCode. See docs/agents/lsp.md.
  "lsp": true
}
