# Benchmarks

Populated in Sprint 5. See `MILESTONES.md`.

Planned structure:
- `bench/nginx.conf` — Nginx comparison config.
- `bench/docker-compose.yml` — LB + Nginx + backends + load generator.
- `bench/wrk/` — wrk scripts.
- `bench/vegeta/` — vegeta target files and run scripts.
- `bench/results/` — raw and summarized results (committed).
- `bench/run.sh` — one-command reproducer.
