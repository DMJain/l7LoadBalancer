# Completed Task Archive — Sprints 1–3

Archived from PROGRESS.md on 2026-09-26. These entries are the historical record of completed work.
They are not read by agents during normal task execution. For the current task tracker, see PROGRESS.md.

---

## Sprint 1 — Foundation

- [DONE] S1.T0 — Repository scaffold (bootstrap agent, 2026-08-31T00:00:00Z, commit: 25c1952)

- [DONE] S1.T0.5 — Interface & Schema Freeze (Phase A) (claude, started 2026-09-01T16:04:36Z, completed 2026-09-01T16:35:00Z)
  - Goal: freeze all cross-package contracts as compilable Go stubs + a design doc before any Sprint 1 implementation begins, so T1–T10 (potentially executed by different agents/sessions) cannot silently diverge.
  - Files: `internal/config/config.go`; `internal/backend/backend.go`, `internal/backend/registry.go`; `internal/balancer/selector.go`, `roundrobin.go`, `leastconn.go`, `consistent_hash.go`, `p2c_ewma.go`; `internal/proxy/proxy.go`; `internal/logger/logger.go` (+doc.go); `internal/metrics/metrics.go` (+doc.go); `internal/health/health.go`; `internal/circuit/circuit.go`; `docs/design/sprint-1-contracts.md`; `docs/adr/0002-interface-placement-and-schema-freeze.md`
  - Depends on: S1.T0
  - Acceptance: `go build ./...` and `go vet ./...` pass with every stubbed body either `panic("not implemented: <task-id>")` or trivial (`logger.New`); compile-time `var _ Selector = (*X)(nil)` assertions in every selector file; design doc covers dependency graph, interface placement, YAML schema, algorithm identifier table, error convention, log field vocabulary, metric reservations, concurrency ownership table; ADR-0002 written; no changes to `main.go` behavior (still returns 501).
  - Test approach: none — Phase A is analysis/contract-definition only, no logic, no test files.

- [DONE] S1.T1 — Add Go dependencies (opencode, started 2026-09-17T21:57:24Z, completed 2026-09-17T22:01:39Z)
  - Goal: pull in the third-party deps Sprint 1 needs (YAML parsing, test assertions) so later tasks aren't blocked on dependency wrangling.
  - Files: `go.mod`, `go.sum` (both tool-managed, not hand-edited)
  - Depends on: S1.T0
  - Acceptance:
    - `go.mod` requires `gopkg.in/yaml.v3` and `github.com/stretchr/testify`.
    - `go.sum` generated and committed.
    - `go mod tidy` produces no further diff after this task.
    - `go build ./...` and `go vet ./...` still pass.
  - Test approach: none (dependency-only); gated by existing build/vet checks.

- [DONE] S1.T2 — Implement `internal/config` (opencode, started 2026-09-17T22:12:47Z, completed 2026-09-17T22:14:08Z)
  - Goal: load and strictly validate the YAML config (listen address + backend list + algorithm choice) into a typed struct, as the canonical config source for the registry and proxy.
  - Files: `internal/config/config.go`, `internal/config/config_load_test.go`, `internal/config/config_validate_test.go`, `configs/example.yaml` (updated to a realistic 3-backend example)
  - Depends on: S1.T1
  - Acceptance:
    - `Config` struct: `Listen string`; `Backends []BackendConfig` (`Name`, `URL` string fields at minimum); `Algorithm string`, defaulting to `"round_robin"` when empty.
    - `Load(path string) (*Config, error)` reads + unmarshals YAML.
    - Unknown YAML fields cause `Load` to return an error. Use `yaml.NewDecoder(f).KnownFields(true)`; do not use `yaml.Unmarshal` directly.
    - `Validate() error` rejects: empty `Listen`, zero backends, a backend whose `URL` fails `url.Parse` or has no host, duplicate backend names, unrecognized `Algorithm` value.
    - Errors wrapped `fmt.Errorf("config: ...: %w", err)` per AGENTS.md.
    - `configs/example.yaml` updated to a valid 3-backend config that passes `Validate()`.
  - Test approach: table-driven tests covering valid config, missing listen, empty backends, malformed URL, duplicate names, unknown algorithm, unknown field. testify/require for setup, testify/assert for values.

- [DONE] S1.T2-fix — Address S1.T2 code-review audit findings (opencode, started 2026-09-17T22:29:55Z, completed 2026-09-17T22:31:12Z)
  - Goal: close out the two-axis audit of S1.T2 — reject uppercase URL schemes (real bug), add an `example.yaml` round-trip test, record ADR-0004 for the frozen-comment/implemented-set deviation, align tracking files, and apply small cleanups.
  - Files: `internal/config/config.go`, `internal/config/config_validate_test.go`, `internal/config/config_load_test.go`, `docs/adr/0004-reject-unimplemented-algorithms-in-validate.md`, `AGENTS.md`, `PROGRESS.md`, `.scratch/s1-t1-t2-deps-and-config/issues/02-implement-config-package.md`, `docs/sessions/2026-09-18-opencode.md`
  - Depends on: S1.T2
  - Acceptance: uppercase `HTTP://`/`HTTPS://` backend URLs rejected by `Validate` with a scheme error; `configs/example.yaml` round-trips through `Load`+`Validate` in a test; ADR-0004 written and indexed in AGENTS.md; S1.T2 Files bullet corrected; issue 02 acceptance boxes ticked; session log commit list completed and audit summary appended; `b` loop var renamed; no `.scratch/`-pointing comments in `config.go`; `make test`, `make test-race`, `go vet`, `make fmt`, `go mod tidy` clean.
  - Test approach: two new rejection cases in the `TestValidate` table; one new `TestExampleConfig` integration test.

- [DONE] S1.T2.6 — ADR-0005: Scope of "production-grade" (claude, started 2026-09-18T07:50:00Z, completed 2026-09-18T07:55:00Z)
  - Goal: write down, in one canonical place, what this project's "production-grade" framing (AGENTS.md) does and does not claim, before Sprint 1 implementation and the eventual README/Sprint 5 design-decisions doc build further on an undefined term.
  - Files: `docs/adr/0005-scope-of-production-grade.md`
  - Depends on: none (docs-only, no code dependency)
  - Acceptance: ADR states what "production-grade" means (demonstrates production L7 LB patterns, every non-trivial decision defensible, honest reproducible Nginx benchmarking) and what it explicitly excludes (adversarial-traffic hardening/WAF, TLS cert rotation, kernel/OS tuning, SLO instrumentation/alerting, formal security review, multi-tenancy, secrets management beyond env-var interpolation, disaster recovery, capacity planning/SLA, multi-region validation, and a settled deployment target — deployment target explicitly deferred to Sprint 4/5). Decided and dated by the project owner; no invented prior provenance.
  - Test approach: none — docs-only (AGENTS.md TDD exception).

- [DONE] S1.T3 — Implement `internal/backend` (opencode, started 2026-09-18T09:29:18Z, completed 2026-09-18T09:30:24Z)
  - Goal: define `Backend` and a concurrency-safe `Registry` tracking identity, health, and active-connection count, since balancer and proxy both read/mutate this under concurrent requests.
  - Files: `internal/backend/backend.go`, `internal/backend/registry.go`, `internal/backend/backend_test.go` (replaces the placeholder `TestScaffold`), `internal/backend/registry_test.go`
  - Depends on: S1.T2
  - Acceptance:
    - `Backend`: exported `Name string` and `URL *url.URL`; unexported `healthy` (`atomic.Bool`) and `active` (`atomic.Int64`) fields, reachable only via methods `IsHealthy()`, `SetHealthy(bool)`, `IncActive()`, `DecActive()`, `ActiveConns() int64`. `SetHealthy` is added per ADR-0006 (amends ADR-0002 decision 5) — needed now so S1.T8 can drive health transitions before Sprint 3's health checker exists; Sprint 3 reuses it unchanged.
    - `Backend` exposes an `IsHealthy() bool` method; downstream code (balancer, proxy) accesses health only via this method, never via the `healthy` field directly. This isolates the field type from callers so Sprint 3 can replace `atomic.Bool` with a state enum without touching balancer or proxy code.
    - `NewRegistry(cfgs []config.BackendConfig) (*Registry, error)` builds backends from validated config; every backend starts `healthy = true` (commented as such — there is no health checker yet to set it any other way, and Sprint 1's exit criteria requires all 3 backends reachable from the start).
    - `Registry` holds an ordered slice only (no name-indexed map — nothing through Sprint 3 needs O(1) lookup by name; add it in Sprint 4 when reload-diffing needs it). `Registry.All()` and `Registry.Healthy()` each return a fresh slice per call, preserving that order (required for `LeastConnections`' deterministic tie-break) — safe to iterate concurrently with registry mutation, no lock held by caller.
    - Concurrent `ActiveConns` increment/decrement and health toggling from multiple goroutines is race-free under `go test -race`.
    - Placeholder `TestScaffold` removed, replaced with real tests.
  - Test approach: table-driven tests for construction/filtering; a concurrent test with N goroutines mutating `ActiveConns`, asserting the final count under `-race`.

- [DONE] S1.T4 — Implement RoundRobin selector (opencode, started 2026-09-18T09:38:54Z, completed 2026-09-18T09:40:23Z)
  - Goal: implement round-robin backend selection behind a Selector interface.
  - Files: `internal/balancer/selector.go` (Selector interface + `ErrNoHealthyBackends`), `internal/balancer/roundrobin.go`, `internal/balancer/roundrobin_test.go`
  - Depends on: S1.T3
  - Acceptance:
    - `Selector` interface: `Select(ctx, r *http.Request) (*backend.Backend, error)`.
    - `RoundRobin` selects only from `Registry.Healthy()`, uses an atomic counter (no mutex) for rotation, safe under concurrent access.
    - Returns `ErrNoHealthyBackends` when the healthy set is empty.
  - Test approach: table-driven cyclic-order test over 3 backends; empty-registry error case; concurrent test firing 1000 selects, each backend chosen within ±5% of the expected 1/3 share.

- [DONE] S1.T5 — Implement LeastConnections selector (opencode, started 2026-09-18T09:47:42Z, completed 2026-09-18T09:48:15Z)
  - Goal: pick the healthy backend with the lowest current ActiveConns.
  - Files: `internal/balancer/leastconn.go`, `internal/balancer/leastconn_test.go`
  - Depends on: S1.T4
  - Acceptance:
    - `LeastConnections` implements `Selector`.
    - Ties broken deterministically (first tied backend in registry order) so tests are reproducible.
    - Returns `ErrNoHealthyBackends` when no healthy backends.
    - Reads `ActiveConns` atomically; does not itself mutate it (mutation is the proxy's job, wired in S1.T6).
  - Test approach: table-driven test with pre-seeded `ActiveConns` values asserting the minimum is chosen; tie-break case; empty-registry case.

- [DONE] S1.T6 — Implement `internal/proxy` (opencode, started 2026-09-18T16:55:16Z, completed 2026-09-18T16:58:11Z)
  - Goal: wrap httputil.ReverseProxy so each request's Director consults the configured Selector, rewrites the target, and tracks ActiveConns around the round trip.
  - Files: `internal/proxy/proxy.go`, `internal/proxy/proxy_test.go`, `docs/adr/0007-proxy-request-lifecycle-and-exactly-once-decrement.md`, `internal/config/config.go` (stale path-prefix comment corrected), `AGENTS.md` (ADR-0007 indexed)
  - Depends on: S1.T3, S1.T4, S1.T5
  - Acceptance:
    - `New(registry *backend.Registry, selector balancer.Selector) http.Handler` wraps `ReverseProxy`.
    - `ServeHTTP` calls `selector.Select` itself — not `Director` — and short-circuits a 503 immediately when it returns `ErrNoHealthyBackends`, bypassing `ReverseProxy` entirely for that path (`Director`'s `func(*http.Request)` signature has no way to write a response). On success, `ServeHTTP` calls `IncActive()` and attaches the chosen `*backend.Backend` to the request context via an unexported context-key type; `Director` reads it back from context and only sets `req.URL.Scheme/Host` — it does not call `Select`. `ModifyResponse` and `ErrorHandler` read the same backend off `resp.Request.Context()` / `req.Context()` to drive `DecActive`.
    - No healthy backend → responds 503, no panic, no hang.
    - `ActiveConns` incremented before dispatch, decremented after response completes (success, error, or timeout) — no leak across many sequential requests.
    - `ActiveConns` decrement uses a response-body wrapper whose `Close()` decrements exactly once. Wrap the body in `ModifyResponse`. Do NOT decrement in `Director`, in `ModifyResponse` directly, or only in `ErrorHandler`. Test with 100 concurrent (not sequential) in-flight requests and assert `ActiveConns` returns to 0 within a small drain window.
    - Every request emits one "request complete" `slog` line using the canonical fields (`backend`, `method`, `status`, `latency_ms`, `remote_addr`, `path`) per `docs/design/sprint-1-contracts.md` — on the success path, the 503 short-circuit path, and the `ErrorHandler` path alike. No separate "request start" line in Sprint 1.
  - Test approach: httptest.NewServer fake backends returning an identifying body; httptest-wrapped proxy in front of them; assert distribution matches the selector; no-healthy-backend → 503; concurrent-request test asserting ActiveConns returns to 0.

- [DONE] S1.T7 — Wire `cmd/l7LoadBalancer/main.go` end-to-end (opencode, started 2026-09-18T18:13:48Z, completed 2026-09-18T18:17:45Z)
  - Goal: replace the placeholder 501 handler with the real path: load config → build registry → build selector (from cfg.Algorithm) → build proxy → serve.
  - Files: `cmd/l7LoadBalancer/main.go`, `internal/balancer/selector.go` (`NewFromConfig` implemented), `internal/balancer/factory_test.go` (new), `configs/example.yaml` (unchanged — already valid)
  - Depends on: S1.T2, S1.T3, S1.T4, S1.T5, S1.T6
  - Acceptance:
    - `main.go` calls `config.Load(*configPath)`; logs a fatal error and exits non-zero on load/validate failure (no silent fallback).
    - Selector chosen by `cfg.Algorithm` via a small factory/switch.
    - `make run` against `configs/example.yaml` starts a proxy that distributes requests across backends per the selected algorithm.
    - Existing SIGINT/SIGTERM graceful-shutdown behavior preserved.
  - Test approach: manual smoke test, documented in the Sprint 1 session log; automated coverage lives in S1.T6/S1.T4/S1.T5 tests plus the new `NewFromConfig` factory tests.
  - Deviation (documented in session log, no ADR needed): the scaffold's `-addr` flag was removed — `listen` is part of the frozen YAML schema and `config.Validate` checks it, so `cfg.Listen` is the single source of truth. The factory is implemented in `balancer` per ADR-0002, and it rejects unknown algorithms (including the empty string) rather than defaulting.

- [DONE] S1.T8 — Cross-selector regression + health-transition tests (opencode, started 2026-09-18T17:55:14Z, completed 2026-09-18T17:56:20Z)
  - Goal: cover selector behavior not already exercised by S1.T4/T5's per-selector tests — specifically dynamic health transitions and interface conformance — so both selectors are proven interchangeable.
  - Files: `internal/balancer/selector_test.go` (new, cross-cutting file)
  - Depends on: S1.T4, S1.T5
  - Acceptance:
    - Compile-time interface assertions: `var _ Selector = (*RoundRobin)(nil)` and `var _ Selector = (*LeastConnections)(nil)`.
    - Test: backend goes unhealthy mid-run → selector stops choosing it (both selectors); backend goes healthy again → resumes being chosen.
    - `go test -cover ./internal/balancer/...` recorded in the Sprint 1 session log (no enforced threshold yet — that's deferred).
  - Test approach: table-driven + a scripted health-toggle sequence per selector.

- [DONE] S1.T9 — docker-compose dummy backends (opencode, started 2026-09-18T09:53:23Z, completed 2026-09-18T09:55:57Z)
  - Goal: provide 3 lightweight backend services via docker-compose so the proxy can be exercised end-to-end, per the MILESTONES.md Sprint 1 deliverable.
  - Files: `deployments/docker/docker-compose.yml`, `deployments/docker/dummy-backend/main.go`, `deployments/docker/dummy-backend/Dockerfile`, `deployments/docker/dummy-backend/README.md`
  - Depends on: S1.T2
  - Acceptance:
    - `docker-compose.yml` defines 3 services only (no LB service — that's run locally via `make run`; Sprint 5 has its own separate "LB + Nginx + 4 backends" bench compose, kept distinct so the two don't drift against each other), each on a distinct port mapped to host `9001`/`9002`/`9003` so the existing `configs/example.yaml` works against them unmodified — no new config file.
    - Each service responds to `GET /` with a body identifying itself (e.g. `{"backend":"backend-a"}`) so distribution is observable via curl.
    - `docker compose -f deployments/docker/docker-compose.yml up -d` brings up all 3 healthy.
    - Running the proxy against `configs/example.yaml` and issuing N requests hits all 3 backends (verified by grepping response bodies).
    - Dummy backends honor two env vars: `SLEEP_MS` (int, artificial latency per request in milliseconds, default 0) and `FAIL_RATE` (float 0.0-1.0, fraction of requests returning HTTP 500, default 0). Documented in `deployments/docker/dummy-backend/README.md`. `docker-compose.yml` gives each of the 3 services distinct non-zero defaults (not all zero) so `docker compose up` demonstrates uneven latency/failure — and therefore visible `LeastConnections` vs. `RoundRobin` differences — without a manual override.
  - Test approach: no Go unit tests; a documented manual/scripted smoke test, recorded in the Sprint 1 session log.

- [DONE] S1.T10 — Sprint 1 retro / architecture doc (claude, started 2026-09-18T19:53:55Z, completed 2026-09-18T19:54:39Z)
  - Goal: document the as-built architecture so future sprints and future agents have a reference, and close out Sprint 1.
  - Files: `docs/architecture.md` (replace the placeholder Component map section); a new ADR if any deviation from plan occurred.
  - Depends on: S1.T1 through S1.T9 all [DONE]
  - Acceptance:
    - `docs/architecture.md` describes the request path: main → config → registry → selector → proxy → backend (text or Mermaid diagram).
    - Any deviations from MILESTONES.md Sprint 1 deliverables during implementation are explicitly called out.
    - Any undocumented non-trivial design decision gets a new ADR, cross-linked from architecture.md.
  - Test approach: none (docs-only); reviewed against MILESTONES.md Sprint 1 exit criteria.

## Sprint 2 — Advanced Algorithms

Scoped in `.scratch/s2-t1-t2-consistent-hash-bounded-loads/` as one spec plus three implementation tickets. The spec groups the work as S2.T1 (ring primitive + unexported naive comparator) and S2.T2 (bounded-loads selector + config wiring), but PROGRESS tracks the finer-grained tickets so each closes independently: issue 01 = ring primitive, issue 02 = `naiveConsistentHash`, issue 03 = `ConsistentHashBoundedLoads`.

- [DONE] S2.T1.1 — Consistent-hash ring primitive (issue 01) (opencode, started 2026-09-19T06:18:15Z, completed 2026-09-19T06:20:58Z, ring with fnv→fmix64 hashing, 150 vnodes/backend, index-first vnode keys, iter.Seq candidate walk; ADR-0008)
  - Goal: an unexported, deterministic hash ring in `internal/balancer` that maps a hash key to a backend via virtual nodes, exposing an ordered candidate walk as a Go 1.23 `iter.Seq[*backend.Backend]` — the shared foundation both consistent-hash selectors (issues 02 and 03) build on. Placement only: no `Select`, no health or capacity awareness.
  - Files: `internal/balancer/ring.go`, `internal/balancer/ring_test.go`, `docs/adr/0008-consistent-hash-ring-pipeline-and-vnode-layout.md`, `AGENTS.md` (ADR indexed), `docs/architecture.md` (ADR indexed + ring noted), `docs/sessions/2026-09-19-opencode.md`
  - Depends on: none
  - Acceptance:
    - Unexported `ring` type, built once at construction from `Registry.All()` (every backend, regardless of health) — no rebuild API; membership changes are Sprint 4's problem.
    - Hash pipeline: stdlib `hash/fnv` `New64a()` → `Sum64()` → fixed Murmur3-style `fmix64` finalizer; same pipeline used for vnode placement and (later tickets) request-key hashing.
    - Virtual-node keys built `<replica-index>:<backend-name>` (index first); 150 vnodes per backend.
    - Candidate walk from a key's ring position wraps around and yields each distinct backend once, as `iter.Seq[*backend.Backend]` (not a predicate callback).
    - Table-driven tests: same key maps to the same backend repeatedly; adding/removing a backend remaps only ~`1/n` of keys; vnode placement reasonably uniform under a large random-key sample.
    - ADR-0008 records the hash pipeline, vnode key order, and vnode count, each citing measured evidence.
  - Test approach: direct ring-level tests (no selector, no HTTP), `testify/require` for setup and `assert` for values, deterministic fixtures so assertions cannot flake.

- [DONE] S2.T1.2 — `naiveConsistentHash` selector (issue 02) (opencode, started 2026-09-19T06:58:41Z, completed 2026-09-19T07:45:36Z, unexported health-aware/load-blind Selector + shared requestHashKey + binary DCE guard)
  - Goal: unexported, health-aware but load-blind `Selector` over the ring, walk skipping unhealthy candidates; deliberately never reachable via `NewFromConfig`/`implementedAlgorithms`; exists as the empirical comparator for bounded-loads.
  - Files: `internal/balancer/naive_consistent_hash.go`, `internal/balancer/naive_consistent_hash_test.go`, `internal/balancer/hashkey.go`, `internal/balancer/hashkey_test.go`, `internal/balancer/ring_test.go` (`randomClientIP`→`randomIP` rename, no behavior change), `cmd/l7LoadBalancer/dce_test.go` (new), `PROGRESS.md`, `.scratch/s2-t1-t2-consistent-hash-bounded-loads/issues/02-naive-consistent-hash-selector.md`, `docs/sessions/2026-09-19-opencode.md`
  - Depends on: S2.T1.1
  - Acceptance:
    - Unexported `naiveConsistentHash` satisfies `Selector` (compile-time assertion) and holds only the immutable ring; `Select` returns the first healthy candidate from the client-IP ring walk, or `ErrNoHealthyBackends` when the walk is exhausted (including a zero-backend registry).
    - `requestHashKey(*http.Request)` derives the hash key from `RemoteAddr` with the port stripped — shared with S2.T2 — returning unparseable/empty addresses unchanged, documented as intentionally concentrating upstream bugs on one backend.
    - The type is in neither `NewFromConfig`'s switch nor `config`'s implemented set; its doc comment states why it is unwired, why it is not named `ConsistentHash`, and points at ADR-0008. No new ADR: ticket 02's decisions are recorded there and in the doc comment, with the bounded-loads evidence reserved for ADR-0009 (S2.T2).
    - `cmd/l7LoadBalancer/dce_test.go` builds the production binary for a pinned linux/amd64 target and asserts the type name is absent, with a `RoundRobin` positive control so the negative assertion cannot be vacuous — turning "the linker drops it" into a checked invariant.
    - Tests: stable affinity per client IP (incl. port-stripped), key-matters teeth check (400 octet-diverse IPs, 4 backends, each ≥10%), health transition (skip → resume), empty healthy set → `ErrNoHealthyBackends`, concurrent `-race` selects.
  - Test approach: direct instantiation against a real `*backend.Registry`, deterministic fixed seeds, table-driven where enumerable, no mocks. `go test -cover ./internal/balancer/...` → 93.9%.

- [DONE] S2.T2 — `ConsistentHashBoundedLoads` + config wiring (issue 03) (opencode, started 2026-09-19T07:53:59Z, completed 2026-09-19T07:57:56Z)
  - Goal: exported `Selector` wired to the `consistent_hash` identifier, reusing the ring with a `(1 + ε)` per-candidate capacity check (ε = 0.25), least-loaded fallback, and a fixed-seed hot-key comparative test plus a build-tagged offline reproducer; ADR-0009 records the bounded-loads decisions and evidence.
  - Files: `internal/balancer/consistent_hash.go` (stub replaced), `internal/balancer/consistent_hash_test.go`, `internal/balancer/hotkey_test.go`, `internal/balancer/hotkey_reproducer_test.go` (`//go:build offline`), `internal/balancer/selector.go` (factory case), `internal/balancer/selector_test.go` (shared `requestForAddr`/`selectAddr` helpers), `internal/balancer/factory_test.go`, `internal/config/config.go` (`implementedAlgorithms`), `internal/config/config_validate_test.go`, `docs/adr/0009-consistent-hash-bounded-loads-capacity-and-evidence.md`, `AGENTS.md` (ADR indexed + as-built bounded-loads description), `MILESTONES.md` (deliverable wording), `docs/architecture.md` (ADR indexed + Sprint 2 selection note), `PROGRESS.md`, `.scratch/s2-t1-t2-consistent-hash-bounded-loads/issues/03-consistent-hash-bounded-loads.md`, `docs/sessions/2026-09-19-opencode.md`
  - Acceptance: exported `ConsistentHashBoundedLoads` implements `Selector` (compile-time assertion) and is wired to `consistent_hash` in both `config.implementedAlgorithms` and `NewFromConfig`; same ring/iterator/hash key as issue 02; per-candidate skip is `!IsHealthy() || ActiveConns() > capacity`; capacity is `max(1, ceil(avg_active_over_healthy * 1.25))` with `<=` admission; the walk is the ring's one-pass iterator and the least-loaded-seen fallback is defensive only; `ErrNoHealthyBackends` is the sole error condition; ε/vnode count/key source stay Go constants. Tests: over-capacity skip, `<=` boundary (2 admitted at cap 2, 3 skipped at cap 2), idle-system first request, stable affinity, health transition, empty healthy set, no-mutation, concurrent `-race` select, key-matters teeth check. Fixed-seed (2253) hot-key test: naive busiest 4,005 / bounded 3,126 of 10,000, asserted in [3,800,4,200] / [3,100,3,130]. `offline`-tagged reproducer sweeps 60 seeds: naive busiest min 29.9% / p10 34.0% / median 40.0% / p90 47.8% / max 51.3% / mean 40.4%, bounded 30.0–31.3%; capacity check found an admissible candidate at every one of 600,000 selections — fallback never needed. ADR-0009 records epsilon, load metric, capacity formula, probing/fallback, and the evidence.
  - Test approach: direct instantiation against real `*backend.Registry` fixtures, deterministic fixed seeds, table-driven where enumerable, no mocks; `httptest.NewRequest` with a controlled `RemoteAddr`. `go test -cover ./internal/balancer/...` → 94.3%.
  - Depends on: S2.T1.1, S2.T1.2

- [DONE] S2.T3 — `PowerOfTwoChoicesEWMA` + latency state (issue 01) (opencode, started 2026-09-19T18:37:42Z, completed 2026-09-19T18:42:08Z, Backend EWMA latency + P2C selector + proxy recording + `p2c_ewma` wired; ADR-0010)
  - Goal: the fourth and final Sprint-1-reserved algorithm, `p2c_ewma`, end-to-end: `Backend` gains an EWMA-smoothed latency estimate (`RecordLatency`/`EWMALatency`, unexported `atomic.Int64` nanoseconds, method-only access) that the proxy updates on every request regardless of configured selector, `PowerOfTwoChoicesEWMA` samples two random healthy backends and routes to the faster one, and `p2c_ewma` becomes selectable in YAML exactly like the other three algorithms.
  - Files: `internal/backend/backend.go`, `internal/backend/backend_test.go`, `internal/balancer/p2c_ewma.go` (stub replaced), `internal/balancer/p2c_ewma_test.go`, `internal/proxy/proxy.go`, `internal/proxy/proxy_test.go`, `internal/config/config.go` (`implementedAlgorithms`), `internal/config/config_validate_test.go`, `internal/balancer/selector.go` (factory case), `internal/balancer/factory_test.go`, `docs/adr/0010-p2c-ewma-backend-latency-state-cold-start-and-failure-penalty.md`, `AGENTS.md` (ADR indexed + as-built P2C description), `docs/architecture.md` (status updated), `CONTEXT.md` (EWMA latency entry), `PROGRESS.md`, `.scratch/s2-t3-p2c-ewma/issues/01-p2c-ewma-selector.md`, `docs/sessions/2026-09-20-opencode.md`
  - Depends on: S2.T2 (and S1.T3's `Backend`, S1.T6/ADR-0007's proxy lifecycle)
  - Acceptance:
    - `Backend` gains `RecordLatency(d time.Duration)` and `EWMALatency() time.Duration`, backed by an unexported `atomic.Int64` nanoseconds field, method-only access (no exported field) — amends ADR-0002, symmetric with ADR-0006's `SetHealthy`.
    - First-ever `RecordLatency` call for a given backend sets the value directly; every subsequent call blends via `latency_new = α·observed + (1-α)·latency_old` with α = 0.1 as an unexported constant, applied through a CAS retry loop (not a mutex).
    - `errorHandler`'s failure path calls `RecordLatency` with a fixed `2 * time.Second` penalty constant rather than the real elapsed time-to-failure; `modifyResponse` and `errorHandler` call it unconditionally on every request, regardless of configured selector.
    - `reqState` gains a timestamp (distinct from the request-complete log line's `start`) set at the end of `director()`, so the recorded window is the backend round trip only, with a comment explaining why it is not the same measurement as `latency_ms`.
    - `PowerOfTwoChoicesEWMA` fills in the frozen stub's exact signature; compile-time `Selector` assertion; zero healthy → `ErrNoHealthyBackends`; exactly one healthy → returned directly with no draw; two or more → two distinct backends drawn via `math/rand/v2` package-level functions, lower `EWMALatency()` wins.
    - `config.AlgorithmP2CEWMA` added to `config.implementedAlgorithms`; `balancer.NewFromConfig`'s switch routes it to `NewPowerOfTwoChoicesEWMA`.
    - Tests: first `RecordLatency` sets directly; second call matches the α-blended expected value; concurrent `RecordLatency` is `-race`-clean; empty healthy set → `ErrNoHealthyBackends`; exactly one healthy always returned; health transition skip/resume; sustained EWMA gap → faster backend chosen measurably more often over a large sample (real recorded numbers, ADR-0009's evidentiary bar); proxy success path records a real, non-zero, round-trip-scoped latency on the serving backend; proxy failure path records the fixed 2s penalty; `config.Validate` accepts `p2c_ewma` and `NewFromConfig` returns a working `*PowerOfTwoChoicesEWMA`.
    - ADR-0010 bundles `Backend`-owned latency state (amending ADR-0002, symmetric with ADR-0006), cold-start first-sample-direct-set semantics, the fixed 2s failure penalty (including why raw elapsed time was rejected and that `SLEEP_MS` has no enforced ceiling), and one line noting `RecordLatency` runs unconditionally.
  - Test approach: direct-method seam for `Backend`; `Selector`-interface seam for the selector; the existing `httptest` proxy harness for the two wiring cases; config/factory table cases mirroring the existing per-algorithm tests. Deterministic fixtures, no mocks.

- [DONE] S2.T8 — Sprint 2 retro / architecture close-out (opencode, started 2026-09-19T19:22:15Z, completed 2026-09-19T19:25:22Z, deviations audit + ADR sweep + architecture-doc updates)
  - Goal: close Sprint 2 the way S1.T10 closed Sprint 1 — flip the stale architecture-doc header to the Sprint 1–2 reference, add Sprint 2's request-lifecycle additions to the request-path diagram (`dispatchStart`, the three `RecordLatency` call sites), record the sprint's deviations from plan, confirm via an ADR sweep that no S2.T1–T3 decision went undocumented, and append the closing session log.
  - Files: `docs/architecture.md`, `docs/sessions/2026-09-20-opencode.md`, `PROGRESS.md`
  - Depends on: S2.T1.1, S2.T1.2, S2.T2, S2.T3
  - Acceptance: header names Sprints 1–2 as the reference scope; diagram shows `reqState{backend, status, once, dispatchStart}` and the `RecordLatency` calls at `Director`/`ModifyResponse`/`ErrorHandler`; deviations audit records four Sprint 2 items (the one-planned-ADR-to-three-per-task-ADRs split, the S2.T1 ticket split, the MILESTONES evidence/ADR bullets folded into S2.T2/T3 with owner ratification, and this task's own ad-hoc addition — Sprint 2 planned no retro); the session log states the ADR-sweep result explicitly rather than silently skipping the check; docs-only, no code changes.
  - Test approach: none — docs-only (AGENTS.md TDD exception).

## Sprint 3 — Resilience & Observability

Scoped in `.scratch/s3-t1-t3-health-passive-circuit/` as one spec plus six implementation tickets; ADR-0011 composes the three subsystems (active health checks, passive outlier detection, per-backend circuit breaker) and splits the work into three mechanical prefactors (issues 01–03) that unblock three otherwise-independent subsystem tickets (issues 04–06) to be built in parallel. PROGRESS tracks the finer-grained tickets so each closes independently.

- [DONE] S3.T0.1 — Split `Backend.SetHealthy` into `MarkHealthy`/`MarkUnhealthy` (issue 01) (opencode, started 2026-09-19T21:14:35Z, completed 2026-09-19T21:20:15Z, split the single-bool setter into two named methods so the active/passive recovery asymmetry from ADR-0011 decision 2 is legible at every call site; mechanical rename, no behavior change)
  - Goal: remove `Backend.SetHealthy(bool)` and add `Backend.MarkHealthy()` / `Backend.MarkUnhealthy()` over the same `atomic.Bool`, then update every caller (S1.T8's cross-selector health-transition tests included), so S3.T1 and S3.T2 can drive health transitions by intent rather than by bool argument.
  - Files: `internal/backend/backend.go`, `internal/backend/registry.go`, `internal/backend/backend_test.go`, `internal/backend/registry_test.go`, `internal/balancer/roundrobin_test.go`, `internal/balancer/leastconn_test.go`, `internal/balancer/selector_test.go`, `internal/balancer/naive_consistent_hash_test.go`, `internal/balancer/consistent_hash_test.go`, `internal/balancer/p2c_ewma_test.go`, `internal/proxy/proxy_test.go`, `AGENTS.md`, `docs/architecture.md`
  - Depends on: none
  - Acceptance: `SetHealthy` gone; `MarkHealthy()`/`MarkUnhealthy()` added over the same field; every caller updated; no behavior change; `make test` and `make test-race` pass unchanged; PROGRESS and the issue's boxes updated. Decision already recorded in ADR-0011 decision 2 (amending ADR-0006) — no new ADR.
  - Test approach: mechanical translation of the existing direct-method tests to the new names, with explicit `MarkUnhealthy`→unhealthy and `MarkHealthy`→healthy assertions; no new behavior to test.

- [DONE] S3.T0.2 — Round-trip observer fan-out (issue 02) (opencode, started 2026-09-19T22:34:00Z, completed 2026-09-19T22:35:33Z, generalize the proxy's single hardcoded `RecordLatency` call into a `RoundTripObserver` fan-out sized for three listeners; ADR-0011 decision 9)
  - Goal: replace `modifyResponse`/`errorHandler`'s hardcoded `state.backend.RecordLatency(...)` call with a generic `RoundTripObserver` fan-out plus an additive `Proxy.RegisterObserver(...)`, so S3.T2 (passive outlier detection) and S3.T3 (circuit breaker) can each register a listener without further changes to `proxy.go`'s hook logic. Prefactor: unblocks both, neither needs the other.
  - Files: `internal/proxy/proxy.go`, `internal/proxy/proxy_test.go`, `cmd/l7LoadBalancer/main.go`, `PROGRESS.md`, `.scratch/s3-t1-t3-health-passive-circuit/issues/02-round-trip-observer-fanout.md`, `docs/sessions/2026-09-20-opencode.md`
  - Depends on: none
  - Acceptance:
    - `internal/proxy` gains a `RoundTripObserver` interface — `ObserveRoundTrip(b *backend.Backend, d time.Duration, success bool)` — per ADR-0011 decision 9; consumer-defined in `proxy`.
    - `Proxy` gains an additive `RegisterObserver(o RoundTripObserver)` method; `New(reg, sel)`'s frozen two-argument signature is untouched.
    - `modifyResponse` computes `success := resp.StatusCode < 500` and `d := time.Since(state.dispatchStart)`; `errorHandler` always passes `success = false, d = p2cFailurePenalty`; both fan out to every registered observer, unconditionally — recording stays unconditional while any future gating stays conditional, so no observer call site gains an early-return guard.
    - The hardcoded `RecordLatency` call becomes a small adapter observer (`NewLatencyObserver`) registered in `main.go` after the `Proxy` is constructed; behavior unchanged (real round-trip duration recorded on `modifyResponse`, fixed 2s penalty on `errorHandler`).
    - Tests assert invocation **count** (not just content) for every registered observer against both terminal hooks — one request through a 5xx fixture (`modifyResponse`) and one through a connection-refused fixture (`errorHandler`) each invokes every observer exactly once; a 2xx request invokes each exactly once with `success=true` and a real non-zero duration.
    - Every existing proxy latency-recording test still passes, unchanged in outcome, now routed through the fan-out.
  - Test approach: the existing `httptest`-backed `proxy.Proxy` seam (S1.T6/ADR-0007, S2.T3) with a recording spy observer wrapping the latency adapter so call counts are directly assertable; 5xx / connection-refused / 2xx fixtures cover both terminal hooks.

- [DONE] S3.T0.3 — Sprint 3 config schema: probe interval/timeout + circuit cooldown (issue 03) (opencode, started 2026-09-19T23:22:49Z, completed 2026-09-19T23:24:47Z, schema-only prefactor freezing the three new global duration fields and their validation rules before S3.T1/S3.T3 read them)
  - Goal: add the global YAML knobs Sprint 3 needs — health `probe_interval`, health `probe_timeout`, and circuit `cooldown` — with documented built-in defaults when omitted and `Validate()` rules rejecting explicitly-set non-positive durations, so S3.T1 (active health checks) and S3.T3 (circuit breaker) can build in parallel without both editing `config.go`. Schema only: no runtime behavior reads these fields yet.
  - Files: `internal/config/config.go`, `internal/config/config_validate_test.go`, `internal/config/config_load_test.go`, `PROGRESS.md`, `.scratch/s3-t1-t3-health-passive-circuit/issues/03-sprint3-config-schema.md`, `docs/sessions/2026-09-20-opencode.md`
  - Depends on: none
  - Acceptance: `Config` gains global `Health` (`probe_interval`, `probe_timeout`) and `Circuit` (`cooldown`) sections of optional `*time.Duration`; omission defaults to exported `DefaultProbeInterval`/`DefaultProbeTimeout`/`DefaultCircuitCooldown`; `Validate()` rejects a non-positive explicitly-set value for any of the three; `KnownFields(true)` strictness preserved (nested typos rejected); thresholds/window sizes/failure-to-open threshold deliberately **not** added (ADR-0011 decision 10 keeps them Go constants). Tests: omission→defaults, explicit→kept, non-positive (zero and negative) per field rejected, nested unknown-field typo rejected, `Load` leaves omitted durations nil and decodes explicit ones.
  - Test approach: table-driven cases on the existing `Load`→`Validate` seam; `configs/example.yaml` round-trip (`TestExampleConfig`) proves a currently-committed config still loads unmodified.

- [DONE] S3.T1 — Active health checks (issue 04) (opencode, started 2026-09-20T07:17:35Z, completed 2026-09-20T07:20:18Z, `internal/health.Checker` with one probe goroutine per backend and an N-consecutive-failure / M-consecutive-success state machine driving `Backend.MarkHealthy()`/`MarkUnhealthy()`, wired in `main.go` on the shared `sigCtx`)
  - Goal: build the active health-check subsystem in `internal/health` — a `Checker` constructed from the registry plus probe interval/timeout, owning one probe goroutine per backend, probing each backend's already-configured URL with a dedicated `http.Client`, and driving `MarkHealthy`/`MarkUnhealthy` through an N-consecutive-failure / M-consecutive-success state machine whose thresholds are unexported Go constants — then start the goroutines from `main.go` on the existing `sigCtx`.
  - Files: `internal/health/checker.go`, `internal/health/checker_test.go` (replaces the `health.go` stub), `cmd/l7LoadBalancer/main.go`, `PROGRESS.md`, `.scratch/s3-t1-t3-health-passive-circuit/issues/04-active-health-checks.md`, `docs/sessions/2026-09-20-opencode.md`, `docs/architecture.md`, `CONTEXT.md`
  - Depends on: S3.T0.1 (`MarkHealthy`/`MarkUnhealthy`), S3.T0.3 (probe interval/timeout config fields)
  - Acceptance: `health.New(reg, interval, timeout)` builds a `Checker` owning one goroutine per backend via `Start(ctx)`; a probe is a plain GET to the backend's configured URL via a dedicated `http.Client` (`CheckRedirect` → `http.ErrUseLastResponse`, its own timeout, no shared transport with `internal/proxy`); only 2xx is success, 3xx/4xx/5xx and transport failures are failures; `probeFailuresBeforeUnhealthy` (3) consecutive failures → `MarkUnhealthy()`, `probeSuccessesBeforeHealthy` (2) consecutive successes → `MarkHealthy()`, both unexported Go constants; `main.go` starts the checker on the existing `sigCtx` (no second shutdown primitive); per-backend `prober.probeOnce` is separate from the ticker `run` loop and driven directly by tests with no ticker or context cancellation. Tests: response-class table, 3xx-observed-not-followed, connection-refused, 5xx/refused → unhealthy after N, consecutive success → healthy after M, counter-reset consecutiveness, dedicated-client timeout, malformed-URL defensive path, and `Start` stops probing on cancellation. `go test -cover ./internal/health/...` → 100.0%.
  - Test approach: direct `Checker` constructor against `httptest.Server` fixtures (2xx, 5xx, 3xx redirect to a 2xx target, connection-refused); `prober.probeOnce` called directly rather than through a ticker, so no real timer or context cancellation is needed; `testify/require` for setup, `testify/assert` for value checks; one `Start`/cancel test covers the goroutine wiring.

- [DONE] S3.T2 — Passive outlier detection (issue 05) (opencode, started 2026-09-20T07:27:14Z, completed 2026-09-20T07:29:25Z, `internal/health.OutlierDetector` count-based sliding window registered into the round-trip observer fan-out)
  - Goal: build the passive half of `internal/health` — an `OutlierDetector` implementing `proxy.RoundTripObserver` that keeps a count-based sliding window of the last N round-trip outcomes per backend and, on the failure count within the window reaching a threshold, ejects the backend via `Backend.MarkUnhealthy()` exactly once per ejection episode. Recovery stays single-path: only a later successful active probe (detected observationally as the backend reading healthy again) starts a fresh episode; the detector has no timer of its own.
  - Files: `internal/health/outlier.go` (new), `internal/health/outlier_test.go` (new), `internal/health/checker.go` (package comment), `internal/proxy/proxy_test.go` (registration-alongside-observer integration test), `cmd/l7LoadBalancer/main.go` (`newHandler` registers the detector), `PROGRESS.md`, `.scratch/s3-t1-t3-health-passive-circuit/issues/05-passive-outlier-detection.md`, `docs/sessions/2026-09-20-opencode.md`, `CONTEXT.md`
  - Depends on: S3.T0.1 (`MarkUnhealthy`), S3.T0.2 (`RoundTripObserver`/`RegisterObserver`)
  - Acceptance: `OutlierDetector` satisfies `proxy.RoundTripObserver` (structural — `internal/health` does not import `internal/proxy`); per-backend window and failure threshold are unexported Go constants (ADR-0011 decision 10); the failure signal is `success == false`, the union of 5xx responses and `errorHandler` transport failures (ADR-0011 decision 12); on threshold breach `MarkUnhealthy()` is invoked once, not once per failure; a failure burst can be mixed with successes in the window and still eject; outcomes that fall out of the window no longer count; there is no timer-based reinstatement (ADR-0011 decision 3) — an ejected backend's episode resets only once it is observed healthy again; `main.go` registers the detector alongside `NewLatencyObserver`; tests cover threshold crossing, below-threshold, fall-out-of-window, exactly-once-per-episode with reset after recovery, concurrent `-race` observation, and the detector receiving outcomes through the real proxy fan-out next to another observer.
  - Test approach: detector's own `ObserveRoundTrip` called directly against real `*backend.Backend` fixtures from `backend.NewRegistry` (spec seam 4), with an unexported eject-function test seam so the invocation count is assertable (Backend.MarkUnhealthy is idempotent, so its call count is not otherwise observable) and a separate test that exercises the production default. Two `httptest`-backed proxy integration tests (spec seam 3): one registers the detector alongside a spy and the real latency observer and asserts all three received every request before ejection; the other drives a backend that alternately returns 500 and aborts the connection, proving the 5xx ∪ transport-failure signal. `testify/require` for setup, `testify/assert` for values; deterministic fixtures, no mocks.

- [DONE] S3.T3 — Circuit breaker (issue 06) (opencode, started 2026-09-20T07:42:29Z, completed 2026-09-20T07:58:38Z, per-backend closed/open/half-open breaker with CAS-guarded state on `Backend`, policy in `internal/circuit`, Registry-gated admission after `Select`, and the `Healthy()`→`Selectable()` rename; ADR-0012)
  - Goal: per-backend closed/open/half-open circuit breaker — state stored on `Backend` behind CAS-guarded methods, policy (failure-to-open threshold constant, config cooldown) in `internal/circuit`, gating dispatch via a pre-`IncActive()` `Allow()` check, registered as a `RoundTripObserver`, and carrying the `Registry.Healthy()` → `Registry.Selectable()` rename.
  - Files: internal/backend/backend.go, internal/backend/registry.go, internal/backend/backend_test.go, internal/backend/registry_test.go, internal/circuit/circuit.go, internal/circuit/circuit_test.go, internal/proxy/proxy.go, internal/proxy/proxy_test.go, internal/balancer/{roundrobin,leastconn,consistent_hash,p2c_ewma}.go and their tests, cmd/l7LoadBalancer/main.go, docs/adr/0012-circuit-gate-interface-and-backend-state-methods.md, AGENTS.md, docs/architecture.md, CONTEXT.md, PROGRESS.md, .scratch/s3-t1-t3-health-passive-circuit/issues/06-circuit-breaker.md, docs/sessions/2026-09-20-opencode.md
  - Depends on: S3.T0.2 (`RoundTripObserver`/`RegisterObserver`), S3.T0.3 (circuit cooldown config field)
  - Acceptance: circuit state (closed/open/half-open enum, consecutive-failure count, opened-at timestamp, half-open-trial-in-flight flag) stored in `internal/backend` as unexported CAS-guarded state reached only through exported methods; `internal/circuit` holds the consecutive-failure-to-open threshold (Go constant) and cooldown (from `Config`), exposes `Allow(b)` and implements `RoundTripObserver`; the failure counter is consecutive (reset on any success), distinct from passive detection's window, watching the same 5xx ∪ transport-failure signal; Open→Half-Open promotion is lazy and timer-free, CAS-guarded on read, no goroutine; `main.go` constructs the breaker and registers it via `RegisterObserver`; `ServeHTTP` calls `Allow(b)` after `Select()` and before `IncActive()`, answering a denial 503-shaped with a distinct WARN line, no dispatch and no active-connection accounting; `Registry.Healthy()` renamed to `Selectable()` filtering `IsHealthy() && circuit-not-open`, every selector call site and test updated (no selector logic changes); a half-open backend stays fully `Selectable()`; the half-open trial slot uses a real `CompareAndSwap`; a trial closes on one success and reopens on one failure with no inner threshold.
  - Test approach: Backend circuit state driven directly through its method API (spec seam 1); `circuit.Breaker.Allow`/`ObserveRoundTrip` and `Registry.Selectable` integration tested directly over real `*backend.Backend` fixtures (spec seam 4); proxy admission/denial and the 5xx-opens-circuit chaos case exercised through the existing `httptest`-backed `proxy.Proxy` seam (spec seam 3); cooldown timing with real, comfortably-margined sleeps (50ms cooldown / 200ms sleep). `testify/require` for setup, `testify/assert` for values; deterministic fixtures, no mocks.

- [DONE] S3.T4 — Metrics package (issue 01) (opencode, started 2026-09-20T17:56:25Z, completed 2026-09-20T18:01:18Z, build `internal/metrics` as a real leaf-only Prometheus `Collector`, add the `metrics.listen` config section, and serve `/metrics` on its own server)
  - Goal: make `internal/metrics` a real, leaf-only Prometheus `Collector` wrapping `github.com/prometheus/client_golang` over a private `prometheus.NewRegistry()` — request counter, latency histogram, backend-healthy gauge, circuit-state label-enum gauge, and the new active-connections gauge — with plain push methods, plus a `metrics.listen` config field (default `:9090`, always-on) and a `promhttp.HandlerFor`-served `/metrics` endpoint on its own `http.Server` started/shut down in `main.go` on the existing `sigCtx`. Nothing else calls into the collector yet; S3.T6 wires it in.
  - Files: `internal/metrics/metrics.go`, `internal/metrics/metrics_test.go`, `internal/metrics/doc.go`, `internal/config/config.go`, `internal/config/config_load_test.go`, `internal/config/config_validate_test.go`, `cmd/l7LoadBalancer/main.go`, `go.mod`, `go.sum`, `docs/design/sprint-1-contracts.md`, `docs/adr/0013-observability-metrics-logging-and-integration.md`, `AGENTS.md`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/01-metrics-package.md`, `docs/sessions/2026-09-20-opencode.md`
  - Depends on: none
  - Acceptance: `internal/metrics` imports no other internal package; a `Collector` owns a private `prometheus.NewRegistry()` (not `DefaultRegisterer`), is `t.Parallel()`-safe with no package-level `sync.Once`; `lb_requests_total` (`CounterVec`, labels `backend`/`method`/`status_class`) and `lb_request_duration_seconds` (`HistogramVec`, same labels, provisional `doc.go` buckets) implemented with push methods; `lb_backend_healthy` (`GaugeVec`, label `backend`) as a 0/1 gauge; `lb_circuit_state` (`GaugeVec`, labels `backend`/`state`) as a label-enum whose setter unconditionally sets the target state's series to `1` and the other two to `0` on every call; new `lb_active_connections` (`GaugeVec`, label `backend`); `Config` gains a `metrics:` section (`listen` `*string`, nil-means-omitted) defaulting to `:9090`, always-on, `KnownFields(true)` preserved and listen validated as host:port; `/metrics` served via `promhttp.HandlerFor` on a separate `http.Server` sharing `sigCtx`; tests cover each instrument's Inc/Observe/Set via the method API, the circuit-state setter's zeroing after two sequential transitions (asserting all three series), `/metrics` exposition containing every reserved-plus-new name/label after driving the collector, and `metrics.listen` omitted→`:9090`/explicit→kept/nested-typo→rejected; `go.mod`/`go.sum` updated.
  - Test approach: `metrics.Collector`'s own method API asserted via `prometheus/client_golang/prometheus/testutil` (spec seam 1); `/metrics` exposition via `httptest.NewServer` wrapping `promhttp.HandlerFor(registry, ...)` directly, no full binary (spec seam 2); config `metrics.listen` on the existing `Load`→`Validate` seam, matching S3.T0.3's omitted/explicit/rejected-typo case shape (spec seam 7).

- [DONE] S3.T5.1 — Logger test coverage (issue 02) (opencode, started 2026-09-21T03:23:00Z, completed 2026-09-21T03:26:00Z, give `internal/logger` its first test file: `logger.New` level-threshold behavior and well-formed JSON output shape)
  - Goal: close the gap that `internal/logger` has shipped since Sprint 1 with zero test coverage. Add `internal/logger/logger_test.go` asserting that `logger.New(level)` emits records at the configured `slog` level and above while suppressing those below, and that every emitted record is a well-formed JSON object with the expected top-level shape (`time`/`level`/`msg` plus attributes as top-level keys). Independent of the rest of the S3.T5–T7 batch: no shared code, no shared call sites, no transition vocabulary or metrics wiring.
  - Files: `internal/logger/logger_test.go`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/02-logger-test-coverage.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: none
  - Acceptance: `internal/logger/logger_test.go` created; a table-driven test proves `logger.New` at each of DEBUG/INFO/WARN/ERROR emits at that level and above and suppresses below; a test parses the emitted output as JSON and asserts the top-level `time`/`level`/`msg` shape plus attribute fields; `make test`/`make test-race` pass and `logger.New`'s frozen signature is untouched; `PROGRESS.md` records this ticket `[DONE]`.
  - Test approach: capture `os.Stdout` via an `os.Pipe` swap around a `logger.New` call (the handler binds `os.Stdout` at construction and the frozen `New(level)` signature takes no writer, so the swap is the only seam), then parse the captured bytes with `encoding/json`. `testify/require` for setup, `testify/assert` for value checks; no HTTP, no mocks. Test-only ticket: no production code changed; `internal/logger` coverage 100.0% of statements.

- [DONE] S3.T5.2 — Canonical transition-log vocabulary (issue 03) (opencode, started 2026-09-21T03:48:34Z, completed 2026-09-21T03:49:45Z, add the closed `event`/`reason` Go-constant vocabularies to `internal/logger` as a shared prefactor for issues 04–05)
  - Goal: freeze the two new canonical transition-scoped `log/slog` field names (`event`, `reason`) and their closed, snake_case Go-constant value sets in `internal/logger`, so S3.T5's two producer tickets (issues 04 and 05) neither invent overlapping values nor drift from the vocabulary ADR-0013 decision 12 already fixed. No behavior change: nothing logs these fields yet.
  - Files: `internal/logger/doc.go`, `internal/logger/vocab.go` (new), `internal/logger/vocab_test.go` (new), `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/03-transition-log-vocabulary.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: none
  - Acceptance: `doc.go`'s canonical field list gains `event` and `reason`, marked transition-scoped and distinct from the six existing request-scoped fields; `event` is exactly the five constants `health_ejected`, `health_reinstated`, `circuit_opened`, `circuit_closed`, `circuit_half_opened`; `reason` is exactly the seven constants `probe_failures`, `probe_recovered`, `outlier_window`, `consecutive_failures`, `trial_success`, `trial_failure`, `cooldown_elapsed`; each constant's doc comment names the subsystem/transition it belongs to and the paired `event` where one exists; `internal/logger` remains a leaf with no new import; a test pins every constant's exact value and asserts each vocabulary is duplicate-free, so a typo'd variant cannot silently enter the closed set; `PROGRESS.md` records this ticket `[DONE]` on completion.
  - Test approach: direct value-pinning table over the exported constants plus a duplicate/cardinality check per vocabulary (spec's logger seam); `testify/assert`; no HTTP, no logger construction needed. Vocabulary freeze only — no producer behavior to test (issues 04/05 own that).

- [DONE] S3.T5.3 — Health transition log lines (issue 04) (opencode, started 2026-09-21T04:43:22Z, completed 2026-09-21T04:46:20Z, give `internal/health`'s active checker and passive outlier detector their first `slog` lines, edge-triggered so a sustained streak logs once per genuine transition, not once per probe or request)
  - Goal: `health.Checker` and `health.OutlierDetector` each emit exactly one structured `slog` line per genuine ejection or reinstatement — `event=health_ejected, reason=probe_failures` (WARN) and `event=health_reinstated, reason=probe_recovered` (INFO) from the active checker, `event=health_ejected, reason=outlier_window` (WARN) from the passive detector — reusing the `event`/`reason` vocabulary frozen in issue 03. Both gain a `*slog.Logger` via their existing constructors. No metrics involvement.
  - Files: `internal/health/checker.go`, `internal/health/outlier.go`, `internal/health/checker_test.go`, `internal/health/outlier_test.go`, `internal/health/transition_log_test.go` (new), `internal/proxy/proxy_test.go`, `cmd/l7LoadBalancer/main.go`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/04-health-transition-log-lines.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T5.2 (`event`/`reason` vocabulary)
  - Acceptance: `health.New` and `health.NewOutlierDetector` each take a `*slog.Logger`; the active checker's log emission is gated on an exact-equality check against its existing consecutive counters (`== threshold`) plus a genuine-state check (`IsHealthy()`/`!IsHealthy()`), so a sustained streak logs once and an always-healthy backend emits no spurious `health_reinstated`; the passive detector reuses its existing `ejected`-per-episode guard; every line carries the `backend` field; `main.go` passes its logger to both; the new tests assert exactly one line per streak/episode and every existing S3.T1/S3.T2 test still passes.
  - Test approach: buffered `slog.JSONHandler` capture (the `proxy_test.go` pattern) around `probeOnce` cycles driven directly (the S3.T1 seam) and `ObserveRoundTrip` calls (the S3.T2 seam); assert count, level, `event`, `reason`, and `backend` per line. `testify/require` setup, `testify/assert` values.

- [DONE] S3.T5.4 — Circuit transition log lines (issue 05) (opencode, started 2026-09-21T05:18:23Z, completed 2026-09-21T05:24:13Z, `backend.CircuitTransition` + `circuit.Breaker` logging so every genuine open/close/half-open transition emits exactly one structured line; ADR-0013 amended to add the `Reopened` value)
  - Goal: give `circuit.Breaker` its first `slog` lines. `Backend.CircuitFailure`, `CircuitSuccess`, and `CircuitAllow` each return a new exported `backend.CircuitTransition` telling the caller whether *this* call changed the circuit's state; `circuit.New` gains a `*slog.Logger` and the breaker logs `circuit_opened`/`circuit_closed`/`circuit_half_opened` at its own `ObserveRoundTrip`/`Allow` call sites whenever the transition is not `NoChange`. `internal/backend` gains no logging dependency. The `backend.CircuitGate`/`proxy.RoundTripObserver` interfaces `Breaker` implements are untouched (its wrapper methods absorb the extra return). No metrics involvement.
  - Files: `internal/backend/backend.go`, `internal/backend/backend_test.go`, `internal/circuit/circuit.go`, `internal/circuit/circuit_test.go`, `internal/circuit/transition_log_test.go` (new), `cmd/l7LoadBalancer/main.go`, `docs/adr/0013-observability-metrics-logging-and-integration.md` (circuit-transition amendment), `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/05-circuit-transition-log-lines.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T5.2 (`event`/`reason` vocabulary)
  - Acceptance: `backend.CircuitTransition` (`NoChange`/`Opened`/`Closed`/`HalfOpened`/`Reopened`) returned by `CircuitFailure`/`CircuitSuccess`/`CircuitAllow` (the latter's existing `bool` plus the transition); `Reopened` distinguishes a half-open trial failure (`HalfOpen→Open`) from the `Closed→Open` `Opened` case, so `circuit.Breaker` can emit `circuit_opened`/`consecutive_failures` (WARN), `circuit_closed`/`trial_success` (INFO), `circuit_half_opened`/`cooldown_elapsed` (INFO), and `circuit_opened`/`trial_failure` (WARN) — each with the `backend` field; `internal/backend` imports no logger; the `CircuitGate`/`RoundTripObserver` signatures are unchanged; a `Selectable()`-scan-won Half-Open promotion remains unlogged, documented in code + ADR-0013; tests assert the transition values per call, exactly-once logging per cycle, no extra `circuit_opened` on a sustained open-state failure run, and every existing S3.T3 test still passing.
  - Test approach: `Backend`'s direct method API for the transition returns (spec seam 4); `circuit.Breaker`'s own method API with a buffered `slog.JSONHandler` (spec seam 5) for the log lines, including the no-additional-line-while-already-open case.

- [DONE] S3.T6.1 — Whole-request metrics: counter + histogram (issue 06) (opencode, started 2026-09-21T13:41:03Z, completed 2026-09-21T13:43:06Z, `proxy.ServeHTTP` pushes one whole-request observation into an optional `*metrics.Collector` from its existing deferred completion hook, covering every exit path)
  - Goal: give `proxy.Proxy` an optional `*metrics.Collector` and push one whole-request observation — counter increment plus duration histogram — from the same `start`/`state`/deferred-`logRequest` location `ServeHTTP` already uses, so success, no-healthy-backend 503, circuit-denied 503, and `ErrorHandler` 502 are all counted and timed with the correct `backend`/`method`/`status_class` labels (`backend=""` only when no backend was chosen).
  - Files: `internal/proxy/proxy.go`, `internal/proxy/metrics_test.go` (new), `cmd/l7LoadBalancer/main.go`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/06-whole-request-metrics.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T4 (`metrics.Collector`)
  - Acceptance: `proxy.Proxy` holds an optional concrete `*metrics.Collector` installed by an additive `SetMetrics` (frozen `New(reg, sel)` untouched; no new fan-out interface); the deferred completion hook pushes the metric and the existing `logRequest` from one place using the same `start`/`state`; `backend=""` is derived the way `logRequest` already derives it; a circuit-denied request carries the real backend label; `status_class` is derived from `state.status`; `RoundTripObserver`'s duration stays internal; `internal/balancer` unchanged; tests cover 2xx (counter increment plus non-zero histogram with the real labels), no-healthy 503 (`backend=""`, `5xx`), circuit-denied 503 (real backend, `5xx`), and `ErrorHandler` 502 (real backend, `5xx`); every existing proxy test passes unchanged.
  - Test approach: the existing `httptest`-backed proxy seam with a real `metrics.NewCollector()`; counter/histogram series read back from the collector's private registry via `Gather` (spec seam 3).

- [DONE] S3.T6.2 — Active-connections gauge (issue 07) (opencode, started 2026-09-21T13:54:12Z, completed 2026-09-21T14:18:20Z, wire `lb_active_connections` at the exact `IncActive`/`DecActive` call sites in `proxy` and seed every backend to `0` in `main.go`)
  - Goal: make `lb_active_connections` track each backend's live in-flight request count by updating the gauge at the exact call sites `proxy` already uses for `Backend.IncActive()`/`DecActive()`, and materialize every backend's series at `0` immediately after `backend.NewRegistry` succeeds — so a freshly started, never-trafficked system renders a complete dashboard on its first scrape.
  - Files: `internal/proxy/proxy.go`, `internal/proxy/metrics_test.go`, `cmd/l7LoadBalancer/main.go`, `cmd/l7LoadBalancer/main_test.go` (new), `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/07-active-connections-gauge.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T4 (`metrics.Collector`)
  - Acceptance: the gauge increments alongside `Backend.IncActive()` and decrements alongside `Backend.DecActive()` (the dispatch site and the once-guarded response-body-`Close()` release); `main.go` seeds every backend to `0` right after `NewRegistry` using `Collector.SetActiveConnections`; tests assert the gauge matches the backend count across a burst of concurrent in-flight requests and drains to `0`, and that every backend's series exists at `0` before traffic; every existing proxy test passes unchanged.
  - Test approach: S1.T6's concurrent blocked-backend leak-check seam extended with a real `metrics.NewCollector()` read via `Gather` (spec seam 3); a package-`main` test over the extracted startup seeding helper, asserting the series via `prometheus/testutil`.

- [DONE] S3.T6.3 — Backend-healthy gauge (issue 08) (opencode, started 2026-09-21T14:31:32Z, completed 2026-09-21T14:36:14Z, `health.Checker`/`OutlierDetector` drive `lb_backend_healthy` from the same edge-triggered signals their ticket-04 log lines use; `main.go` seeds every backend to `1`)
  - Goal: make `lb_backend_healthy` reflect each backend's current health, driven by the exact same edge-triggered signal ticket 04 introduced for the health transition log lines — the active checker's `==`-gated threshold crossing and the outlier detector's `ejected`-per-episode guard — not a second, independently written detection path. `health.Checker` and `health.OutlierDetector` each gain a `*metrics.Collector` alongside the `*slog.Logger` ticket 04 added, via the same constructors, and `main.go` seeds every backend's series to `1` immediately after `backend.NewRegistry` succeeds using the same `Collector.SetBackendHealthy` method real transitions use.
  - Files: `internal/health/checker.go`, `internal/health/outlier.go`, `internal/health/checker_test.go`, `internal/health/outlier_test.go`, `internal/health/transition_log_test.go`, `internal/health/metrics_test.go` (new), `internal/proxy/proxy_test.go` (constructor arity only), `cmd/l7LoadBalancer/main.go`, `cmd/l7LoadBalancer/main_test.go`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/08-backend-healthy-gauge.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T4 (`metrics.Collector`), S3.T5.3 (the edge-triggering signal)
  - Acceptance: `health.New(reg, interval, timeout, log, collector)` and `health.NewOutlierDetector(log, collector)`; the active checker sets the gauge to `0`/`1` inside the same `==`-gated, genuine-transition-guarded `if` blocks its `health_ejected`/`health_reinstated` log lines fire from; the outlier detector sets it to `0` inside its existing `ejected`-per-episode guard; `main.go` seeds every backend to `1` via `seedMetrics` using `Collector.SetBackendHealthy`; tests assert a failure-threshold crossing drives the gauge to `0` (sharing the once-per-streak signal), a success-threshold crossing after ejection drives it back to `1`, and the startup seed materializes every backend's series at `1` before any probe cycle; every existing S3.T1/S3.T2/ticket-04 test passes unchanged.
  - Test approach: the S3.T1 direct `probeOnce` seam and S3.T2 direct `ObserveRoundTrip` seam, each paired with a real `metrics.NewCollector()` read back via `prometheus/testutil` (spec seam 6); the package-`main` `seedMetrics` test extended to assert `lb_backend_healthy` at `1` via `GatherAndCompare` (spec seam 7). `testify/require` setup, `testify/assert` values; deterministic fixtures, no mocks.

- [DONE] S3.T6.4 — Circuit-state gauge (issue 09) (opencode, started 2026-09-21T20:30:00Z, completed 2026-09-21T20:58:00Z, drive `lb_circuit_state` from the exact `CircuitTransition` signal `circuit.Breaker`'s ticket-05 log lines already fire from, and seed every backend to `closed` in `main.go`)
  - Goal: make `lb_circuit_state` reflect each backend's current circuit state, driven by the exact same `CircuitTransition` signal ticket 05 introduced for the circuit transition log lines — not a second, independently written detection path. `circuit.Breaker` gains a `*metrics.Collector` alongside the `*slog.Logger` ticket 05 added, via the same `circuit.New` constructor, and `main.go` seeds every backend's series to `closed` immediately after `backend.NewRegistry` succeeds using the same `Collector.SetCircuitState` method real transitions use.
  - Files: `internal/circuit/circuit.go`, `internal/circuit/circuit_test.go`, `internal/circuit/transition_log_test.go`, `internal/circuit/metrics_test.go` (new), `internal/proxy/proxy_test.go`, `internal/proxy/metrics_test.go`, `cmd/l7LoadBalancer/main.go`, `cmd/l7LoadBalancer/main_test.go`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/09-circuit-state-gauge.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T4 (`metrics.Collector`), S3.T5.4 (`CircuitTransition`)
  - Acceptance: `circuit.New(cooldown, log, collector)`; `lb_circuit_state` is set at the same `CircuitTransition != NoChange` call sites the ticket-05 log lines already fire from in `ObserveRoundTrip`/`Allow` (open on `CircuitOpened`/`CircuitReopened`, half_open on `CircuitHalfOpened`, closed on `CircuitClosed`); `main.go` seeds every backend to `closed` via `seedMetrics` using `Collector.SetCircuitState`; tests assert the full closed→open→half-open→closed→open cycle produces exactly one series at `1` per step with the other two at `0`, that the scan-won Half-Open promotion gap (ADR-0013 decision 13) leaves the gauge at `open`, and that the startup seed materializes every backend's three series with `closed=1`; every existing S3.T3/ticket-05 test passes unchanged.
  - Test approach: `circuit.Breaker`'s own method API (the S3.T3/ticket-05 seam) paired with a real `metrics.NewCollector()` read back via `prometheus/testutil.GatherAndCompare` (spec seam 5); the package-`main` `seedMetrics` test extended to assert `lb_circuit_state` per ADR-0013 decision 9. `testify/require` setup, `testify/assert` values; deterministic fixtures, no mocks.

- [DONE] S3.T7 — Grafana dashboard + observability demo stack (issue 10) (opencode, started 2026-09-21T15:52:37Z, completed 2026-09-21T15:54:11Z, five-panel dashboard JSON + a separate Prometheus/Grafana compose stack with datasource/dashboard provisioning, no Go code)
  - Goal: the final Sprint 3 deliverable — a five-panel Grafana dashboard covering exactly the instrumented metric set, plus a new Prometheus + Grafana docker-compose stack under `deployments/docker/observability/` (separate from S1.T9's dummy-backends compose) so `MILESTONES.md`'s Sprint 3 exit criteria are demonstrable from a clean checkout rather than declared in an unloadable JSON file.
  - Files: `deployments/docker/observability/docker-compose.yml`, `deployments/docker/observability/prometheus/prometheus.yml`, `deployments/docker/observability/grafana/provisioning/datasources/prometheus.yml`, `deployments/docker/observability/grafana/provisioning/dashboards/dashboards.yml`, `deployments/docker/observability/grafana/dashboards/l7loadbalancer.json`, `deployments/docker/observability/README.md`, `deployments/docker/observability/smoke.sh`, `PROGRESS.md`, `.scratch/s3-t4-t7-observability/issues/10-grafana-dashboard-demo-stack.md`, `docs/sessions/2026-09-21-opencode.md`
  - Depends on: S3.T4 (metrics package), S3.T6 (integration)
  - Acceptance: five-panel dashboard JSON (request rate by `backend`/`status_class`, latency p50/p99 per backend via `histogram_quantile`, circuit-state label-enum read, backend-healthy, active-connections) using only instrumented names/labels; Prometheus + Grafana compose with datasource/dashboard provisioning, separate from S1.T9 and Sprint 5's bench compose; Prometheus scrape config targets the LB's `metrics.listen` port at a 15s interval; no Go unit tests (docs/infra-only, TDD-exempt per AGENTS.md and matching S1.T9); documented manual/scripted smoke test reproducing the dashboard-populated-on-first-scrape property and the chaos-test exit criterion; `PROGRESS.md` flipped to `[DONE]`.
  - Test approach: none — infra/config-only (AGENTS.md TDD exception). Compose and dashboard syntax validated locally (`docker compose config`, JSON parse); the LB binary's seeded `/metrics` output was checked against the dashboard's first-scrape expectations. The full `docker compose up` smoke was not run (Docker daemon not running this session) — procedure documented in `deployments/docker/observability/README.md` and scripted in `smoke.sh`. **Verification pending**: the owner accepted `[DONE]` on the completed, documented deliverable with the end-to-end render smoke explicitly recorded as their pending manual verification (per the two-axis review, 2026-09-21 — see session log).

- [DONE] S3.T6.5 — Active checker reinstatement gate `==` → `>=` (issue 01) (opencode, started 2026-09-22T00:26:24+05:30, completed 2026-09-22T00:34:00+05:30, close the drift between ADR-0011 decision 3's stated intent and the shipped active checker, whose reinstatement gate compared the consecutive-successes accumulator with `==` so a passively-ejected backend whose accumulator had already crossed M never fired `MarkHealthy`)
  - Goal: a backend ejected by passive outlier detection while active probes were still succeeding in the background must be reinstated on the next successful probe — `MarkHealthy()`, exactly one `health_reinstated`/`probe_recovered` log line, and `lb_backend_healthy=1` — rather than leaving `IsHealthy()` true while the gauge stays `0` until process restart.
  - Files: `internal/health/checker.go`, `internal/health/checker_test.go`, `docs/adr/0011-health-passive-outlier-and-circuit-breaker-composition.md`, `PROGRESS.md`, `.scratch/s3-t6-5-t8-t9-chaos/issues/01-s3-t6-5-reinstatement-fix.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: none (unblocks S3.T8's combined-signal recovery arc)
  - Acceptance: the active checker's reinstatement gate compares `successes >= probeSuccessesBeforeHealthy` (not `==`), keeping the `!IsHealthy()` genuine-state guard; a new `internal/health/checker_test.go` test seeds a backend `healthy=false` after its accumulator has already crossed M (simulating a passive-detection ejection under background probing), drives successful probes, and asserts `IsHealthy()` reads true with exactly one `health_reinstated`/`probe_recovered` slog record — a test that fails under `==` and passes under `>=`; ADR-0011 gains a dated Amendment section; no metrics collector, observer fan-out, or proxy wiring touched; `make test`, `make test-race`, `go vet`, `make fmt` clean.
  - Test approach: the S3.T1 direct `probeOnce` seam against a real `*backend.Registry` and a 200 `httptest.Server`, with the existing buffered `slog.JSONHandler` capture (`captureLogger`/`recordsWithEvent` from `transition_log_test.go`) doing structured field-allowlist matching on `event`/`reason`/`backend`; `testify/require` setup, `testify/assert` values; deterministic, no mocks.

- [DONE] S3.T8 — Chaos test: backend eviction and recovery (issue 02) (opencode, started 2026-09-22T00:39:45+05:30, completed 2026-09-22T00:45:38+05:30, lands the shared `test/chaos/` harness + arcs (i)/(iii) + the `docker stop` eviction smoke)
  - Goal: verify the `Backend.healthy` axis end-to-end against the full Sprint 3 assembly (registry + selector + proxy + `health.Checker` + outlier + `circuit.Breaker` + `metrics.Collector` + logger) with two in-process chaos arcs — active-only kill/restart (connection-refused, active probes drive ejection and reinstatement) and combined 500-signal (passive + active eject, then health-only recovery while the circuit stays open, mechanically proving the two-gate orthogonality of ADR-0011 decision 1) — plus a shell-scripted docker-compose smoke closing MILESTONES.md's literal "`docker stop` a backend" wording. Lands the shared `test/chaos/` external test package, `assemble(t, cfg)` wiring duplicate, and `flippableBackend` fault injector that S3.T9 reuses.
  - Files: `test/chaos/helpers_test.go`, `test/chaos/assemble_test.go`, `test/chaos/eviction_test.go`, `test/chaos/metrics_test.go` (gauge helper), `test/chaos/assertlog_test.go`, `deployments/docker/chaos/eviction.sh`, `deployments/docker/chaos/README.md`, `PROGRESS.md`, `.scratch/s3-t6-5-t8-t9-chaos/issues/02-s3-t8-eviction-chaos.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T6.5 (issue 01, the reinstatement-gate fix its combined-signal recovery arc verifies)
  - Acceptance: new `test/chaos/` package in `package chaos_test`; `assemble(t, cfg)` duplicates `main.go`'s Sprint 3 wiring and returns the proxy handler, `metrics.Collector`, and a captured `slog.Handler`; `flippableBackend` with `Serve200()`/`Serve500()`/`Kill()`/`Restart()`/`URL()` (`Kill` closes the server so probes and dispatch get real ECONNREFUSED; handler swaps go through `atomic.Pointer[http.Handler]`); fast-path config `probeInterval=20ms`/`probeTimeout=100ms`/`circuitCooldown=200ms`; every timed assertion `require.Eventually` with a ≥2s deadline; gauge assertions via the collector's private registry (S3.T6.3 `testutil` precedent); log assertions via a field-allowlist `assertTransitionLogged`/transition counter over captured `slog.Record`s; arc (i) kill→gauge 0 + one `health_ejected` + selector avoids X, restart→gauge 1 + one `health_reinstated` + selector resumes X; arc (iii) 500s→gauge 0 + one `health_ejected` + `circuit_state{open}=1` + one `circuit_opened`, then 200s→gauge 1 + one `health_reinstated` while `circuit_state{open}` stays `1`; existing `internal/proxy` chaos tests untouched; no prod code outside `internal/health` (from issue 01) edited; `eviction.sh` `set -euo pipefail`, `bash -n`-clean, documented Prometheus HTTP API queries and expected values, full compose execution `[MANUAL VERIFICATION PENDING]` per S3.T7; `make test`/`make test-race`/`go vet`/`make fmt` clean.
  - Test approach: real `*backend.Registry` + real `health.Checker`/`circuit.Breaker`/`metrics.Collector`/`proxy.Proxy` composed by `assemble` exactly as `main.go` wires them; `httptest`-served proxy in front of `flippableBackend` servers; structured `slog.Record` capture instead of the JSON-buffer seam (external package, no unexported access); deterministic fixtures, no mocks.

- [DONE] S3.T9 — Chaos test: circuit breaker trip and half-open recovery (issue 03) (opencode, started 2026-09-22T01:06:32+05:30, completed 2026-09-22T01:10:47+05:30, verifies the circuit-gate axis end-to-end: trip on repeated 5xx, cooldown denial, half-open trial admitted on the first post-cooldown request, both trial resolutions, and the lazy/timer-free promotion property)
  - Goal: verify the circuit-gate axis against the Sprint 3 assembly, reusing S3.T8's `test/chaos/` harness — trip a backend's circuit via repeated 5xx, assert the cooldown denies routing, assert the first request after cooldown is admitted as the half-open trial and that its outcome alone decides closed-vs-reopened, and mechanically prove no premature Half-Open promotion fires without a real request driving the lazy read-time check (ADR-0011 decision 6) — plus a shell-scripted docker-compose smoke closing MILESTONES.md's literal "injecting 500s on one backend eventually opens its circuit" wording.
  - Files: `test/chaos/circuit_test.go` (arcs + T9 config helper), `test/chaos/assertlog_test.go` (negative-assert helper), `test/chaos/assemble_test.go` (shared `backendConfigs` builder), `deployments/docker/chaos/circuit.sh`, `deployments/docker/chaos/README.md`, `PROGRESS.md`, `.scratch/s3-t6-5-t8-t9-chaos/issues/03-s3-t9-circuit-chaos.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T8 (issue 02, the shared `test/chaos/` harness)
  - Acceptance (as adapted this session after tracing the frozen architecture — see deviations): a T9-specific assembly that isolates the circuit axis from active health ejection (single backend + a long `probe_interval` so the 500-serving backend stays `IsHealthy()`, since active probes also treat 5xx as failure and would otherwise eject it before the half-open trial); arc (iv) happy path — blast 5xx → `lb_circuit_state{open}=1` + exactly one `circuit_opened`/`consecutive_failures` log, one request during cooldown denied 503 with no active-connection accounting, flip 200 then the first post-cooldown request is admitted as the trial → 200 → `closed=1` + one `circuit_closed`/`trial_success` log; arc (v) trial failure — leave 500 through cooldown, one post-cooldown request → trial fails → `open=1` + one `circuit_opened`/`trial_failure` log and no `circuit_closed`; arc (vi) zero traffic during cooldown — `require.Never` confirms no promotion log and the gauge stays `open=1`, then the first request after cooldown drives the resolution (lazy, timer-free); all timed assertions `require.Eventually`/`require.Never` with ≥2s deadlines; `circuit.sh` `set -euo pipefail`, `bash -n`-clean and `shellcheck`-clean, documented Prometheus HTTP API queries and expected values, full compose execution `[MANUAL VERIFICATION PENDING]` per S3.T7; `make test`/`make test-race`/`go vet`/`make fmt` clean.
  - Deviations from issue 03's literal wording (owner-approved 2026-09-22, recorded per AGENTS.md Step 2.5): (1) the `circuit_half_opened`/`cooldown_elapsed` log and the `lb_circuit_state{half_open}` gauge are **not asserted** — `Select()` calls `Registry.Selectable()` which wins the lazy Open→Half-Open promotion CAS via `CircuitOpen` before `Allow` runs, so `Allow` reports `NoChange`; this is ADR-0013 decision 13's explicitly accepted permanent gap, and closing it needs an ADR out of this bundle's scope. (2) The "503 via `Registry.Allow` denial during cooldown" is unreachable through the proxy — `Selectable()` filters open circuits so `Select` never returns an open backend and `Allow` is never consulted; the reachable cooldown denial is `ErrNoHealthyBackends` (503) with a single backend, asserted as the denial without `IncActive` accounting. (3) The active checker is neutralized for T9 so the circuit axis is isolated; issue 03's `probeInterval=20ms` fast path would health-eject the 500-serving backend and make the half-open trial unreachable.
  - Test approach: real `*backend.Registry` + real `circuit.Breaker`/`metrics.Collector`/`proxy.Proxy` composed by `assemble` exactly as `main.go` wires them, with the active checker's cadence pushed out of the test window; gauge assertions via the collector's private registry (`gaugeValueOK`), structured `slog.Record` field-allowlist capture (`assertTransitionLogged`/`transitionCount`); deterministic fixtures, no mocks.

- [DONE] S3.T10–T13.D0 — MILESTONES.md amendment: add S3.T10–T13 deliverables and exit criteria (opencode, started 2026-09-22T04:17:27+05:30, completed 2026-09-22T04:17:57+05:30)
  - Goal: amend `MILESTONES.md`'s Sprint 3 deliverables and exit criteria to cover the four demo-stack tickets (S3.T10–T13) before any of their code lands, establishing the "amendment-first" precedent the S3.T13 retro cites (spec D0).
  - Files: `MILESTONES.md`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/01-milestones-amendment.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: none
  - Acceptance: Sprint 3 deliverables add the multi-stage LB Dockerfile, repo-root `docker-compose.yml` (LB + backends + Prometheus + Grafana), the orchestrator-probe-shaped health endpoint contract, and the Sprint 3 retro; exit criteria add (i) containers up via repo-root `docker compose up`, (ii) health endpoint structured JSON with liveness/readiness semantics, (iii) retro written; the Sprint 4 deferred-deployment-target paragraph is preserved verbatim; lands as `docs(milestones): add S3.T10–T13 deliverables and exit criteria` touching only `MILESTONES.md`.
  - Test approach: none — docs-only (AGENTS.md TDD exception).

- [DONE] S3.T12.0 — Health-endpoint prefactor APIs (issue 02) (opencode, started 2026-09-22T04:57:38Z, completed 2026-09-22T10:30:13+05:30, `Checker.ProbeRoundComplete()` one-shot latch + `Collector.RecordProbe()` `lb_health_probe_total` counter + `health_endpoint.listen` config block, the three additive surfaces ticket 03 consumes)
  - Goal: land the three pure additive APIs ticket 03 (the `/livez`/`/readyz`/`/startupz` handlers) consumes — `health.Checker.ProbeRoundComplete() bool` (one-shot `atomic.Bool` latch flipped after the first full probe sweep in which every configured backend has answered at least one probe), `metrics.Collector.RecordProbe(endpoint, statusCode)` (`lb_health_probe_total{endpoint,status}` with status-class labels, mirroring `ObserveRequest`), and the `health_endpoint: { listen }` config block defaulting to `:8081` — following the S3.T0.1/T0.2/T0.3 prefactor pattern. Nothing user-visible; ADR-0014 is written by ticket 03 (spec D8, D9, D11).
  - Files: `internal/health/checker.go`, `internal/health/checker_test.go`, `internal/metrics/metrics.go`, `internal/metrics/metrics_test.go`, `internal/metrics/doc.go`, `internal/config/config.go`, `internal/config/config_load_test.go`, `internal/config/config_validate_test.go`, `configs/example.yaml`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/02-t12-0-health-endpoint-prefactors.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T10–T13.D0 (issue 01)
  - Acceptance: `Checker.ProbeRoundComplete()` is `atomic.Bool`-backed, race-safe under `-race`, false before the first full sweep, true once every backend has been probed at least once, and latched true thereafter; `Collector.RecordProbe(endpoint string, statusCode int)` increments `lb_health_probe_total{endpoint,status}` on the collector's private registry with status-class labels (`"2xx"`/`"5xx"`), never raw codes; `Config` gains `health_endpoint.listen` defaulting to `DefaultHealthEndpointListen` (`:8081`), rejecting an explicitly-set empty or non-host:port value, with `KnownFields(true)` still rejecting nested typos; `configs/example.yaml` documents the block with the default; `internal/proxy`, `internal/circuit`, and `cmd/l7LoadBalancer` untouched; `make test`/`make test-race`/`go vet`/`make fmt` clean.
  - Test approach: the S3.T1 direct `probeOnce` seam over a real `*backend.Registry` with `httptest` 200 fixtures and an enforced per-backend probe order (the latch must only flip on the last backend's first probe), plus a concurrent `-race` variant; `metrics.Collector`'s own method API read back via `prometheus/testutil`/registry `Gather` (the S3.T4 seam); the existing config `Load`→`Validate` table seam for omitted/explicit/invalid/nested-typo cases. `testify/require` setup, `testify/assert` values; deterministic fixtures, no mocks.

- [DONE] S3.T12 — Health endpoint handlers, third listener, ADR-0014 (issue 03) (opencode, started 2026-09-22T10:38:17+05:30, completed 2026-09-22T10:43:28+05:30, the demoable health-endpoint vertical slice: `health.NewHandler` serving `/livez`/`/readyz`/`/startupz` with the pinned JSON envelope, a third always-on `http.Server` on `health_endpoint.listen`, and ADR-0014's ten decisions)
  - Goal: the demoable health-endpoint vertical slice — a third `http.Server` on `health_endpoint.listen` serving `/livez` (unconditional 200), `/readyz` (200 iff config-loaded, first probe round complete, and `Registry.Selectable() >= 1`, checked live), and `/startupz` (one-shot gate on the first two) with the pinned structured-JSON envelope, every hit recorded by `Collector.RecordProbe`, plus ADR-0014's ten decisions (spec D2–D12).
  - Files: `internal/health/endpoint.go`, `internal/health/endpoint_test.go`, `internal/health/doc.go`, `cmd/l7LoadBalancer/main.go`, `docs/adr/0014-health-endpoint-contract-and-probe-semantics.md`, `AGENTS.md`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/03-t12-health-endpoint-handlers.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T12.0 (issue 02)
  - Acceptance: `health.NewHandler(checker, reg, configLoaded, collector) http.Handler` returns a mux for `/livez` (200 `{"status":"alive"}` in every state), `/readyz` (200 iff config-loaded ∧ `Checker.ProbeRoundComplete()` ∧ live `Registry.Selectable() >= 1`, else 503 with the failing check(s) visible), and `/startupz` (503 until the first two hold, then permanently 200 — including under full fleet eviction); every hit calls `Collector.RecordProbe(endpoint, statusCode)` on `lb_health_probe_total{endpoint,status}` (status-class labels) and never touches `lb_requests_total`/the latency histogram; `main.go` constructs the third `http.Server` on `*cfg.HealthEndpoint.Listen`, shares `sigCtx`, logs `"health endpoint started"`, and joins the graceful-shutdown sequence; ADR-0014 is Accepted and records all ten decisions plus the explicit Docker→`/livez`, Fly.io→`/readyz`, K8s→all-three mapping; the S3.T12.0 ADR-0014 forward-references now resolve to a real file; `make test`/`make test-race`/`go vet`/`make fmt` clean.
  - Test approach: table-driven `httptest`-recorder tests over a real `*backend.Registry` + `Checker` + `metrics.Collector` (the S3.T1 `newProber`/`probeOnce` seam drives the probe-round latch and `MarkHealthy`/`MarkUnhealthy` drives the selectable set); `lb_health_probe_total` asserted via `prometheus/testutil.GatherAndCompare` and probes-never-count-as-client-traffic via `CollectAndCount(..., "lb_requests_total") == 0`. `testify/require` setup, `testify/assert` values; deterministic fixtures, no mocks. Manual `make run` smoke recorded in the session log (livez/readyz/startupz before and after the first round, the empty-selectable 503 after stopping the dummy backends, and the recovery back to 200).

- [DONE] S3.T10.0 — `probe` subcommand + version/commit injection (issue 04) (opencode, started 2026-09-22T05:20:21Z, completed 2026-09-22T05:24:57Z, two purely additive CLI changes the Dockerfile ticket consumes: the `l7lb probe <url>` subcommand and the ldflags-injectable `main.version`/`main.commit` startup log line; spec D16/D17)
  - Goal: add the `probe` subcommand (`l7lb probe <url>`, a positional-argument early branch before `flag.Parse` that GETs the URL with a ~2s timeout and exits 0 on 2xx / non-zero otherwise) so the distroless image (issue 05) has a shell-free `HEALTHCHECK`, and add package-level `main.version`/`main.commit` vars defaulting to `dev`/`unknown`, injectable via `-ldflags -X`, with a top-of-`main` `slog.Info("starting", ...)` line that logs both on every process start (spec D16, D17).
  - Files: `cmd/l7LoadBalancer/main.go`, `cmd/l7LoadBalancer/main_test.go`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/04-t10-0-probe-subcommand.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T12 (issue 03 — the `/livez` target the probe tests and the ADR-0014 file its doc comment cites)
  - Acceptance: `probeCommand(args)` is an early branch in `main` that never runs `flag.Parse` (so `l7lb probe <url>` cannot trigger `-config`'s default file lookup); `runProbe` GETs with a 2s client timeout and maps 2xx → 0, any non-2xx or transport error → non-zero; `main.version`/`main.commit` exist as package-level vars with defaults `dev`/`unknown` and appear in a JSON startup log line; a bare `l7lb` invocation (no args) is not intercepted by the probe branch; the doc comment points at ADR-0014; `make test`/`make test-race`/`go vet`/`make fmt` clean.
  - Test approach: table-driven `probeCommand` tests over an `httptest` status matrix (200/204 → 0, 301/404/500/503 → non-zero) and a connection-refused case, both with stubbed `os.Args`; a non-probe args case proving the empty/other invocations are not intercepted; a `t.TempDir()` `go build -ldflags` test running the produced binary from a directory with no config (proving the injected values reach the JSON startup log *and* that the probe branch never falls through to config loading). Deterministic fixtures, no mocks.

- [DONE] S3.T10 — Multi-stage LB Dockerfile, `.dockerignore`, baked config, OCI labels, ADR-0005 amendment (issue 05) (opencode, started 2026-09-22T16:30:00Z, completed 2026-09-22T21:37:00+05:30, multi-stage distroless image whose HEALTHCHECK self-probes via the S3.T10.0 `probe` subcommand; spec D13–D25; two build-forced deviations from the spec's literal text recorded below)
  - Goal: produce a minimal, non-root, distroless image of the load balancer so `docker build .` at the repo root yields an image that runs standalone against a baked compose-network default config and transitions to `healthy` via its own `HEALTHCHECK` — plus the handful of supporting artifacts it needs (repo-root `.dockerignore` allowlist, `configs/docker.yaml`, the ADR-0005 amendment), with the only Go change being the new config's parsing test (spec D13–D25).
  - Files: `Dockerfile`, `.dockerignore`, `configs/docker.yaml`, `internal/config/docker_config_test.go`, `docs/adr/0005-scope-of-production-grade.md`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/05-t10-dockerfile.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T10.0 (issue 04 — the `probe` subcommand the HEALTHCHECK runs and the `main.version`/`main.commit` ldflags targets)
  - Acceptance: builder `golang:1.25.1-bookworm` (spec wrote `1.23` — see deviation 1), `CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w -X main.version=$VERSION -X main.commit=$COMMIT" -o /out/l7lb ./cmd/l7LoadBalancer`; runtime `gcr.io/distroless/static-debian12:nonroot` pinned by its live index digest with the human-readable tag preserved as an adjacent comment (spec wrote "same-line" — see deviation 2); `HEALTHCHECK --interval=5s --timeout=3s --retries=3 CMD ["/l7lb", "probe", "http://127.0.0.1:8081/livez"]`; `EXPOSE 8080 8081 9090`; four OCI labels sourced from `go.mod`'s module path; baked `configs/docker.yaml` copied to `/etc/l7lb/config.yaml` with compose service-name backend URLs; `.dockerignore` strict allowlist (`*` plus `go.mod`/`go.sum`/`cmd/`/`internal/`/`configs/`); ADR-0005 gains a dedicated `## Amendment (2026-09-22) — demo-stack containerization does not settle deployment target` section; `configs/example.yaml` untouched; `make test`/`make test-race`/`go vet`/`make fmt` clean.
  - Test approach: one new Go test, `TestDockerConfig` in `internal/config/docker_config_test.go`, mirroring `TestExampleConfig`'s `Load`+`Validate` round-trip over `configs/docker.yaml`, additionally asserting the backend hosts are the compose service names (`backend-a`/`b`/`c`) and not `127.0.0.1`; every other acceptance item is a Docker-level manual smoke captured in the session log (matching S3.T8/T9's manually-verified Docker smokes).
  - Deviations from the issue's literal text (both build-forced, purpose-preserving; recorded per AGENTS.md Step 2.5, precedent S3.T9): (1) **Builder image Go version** — the spec says `golang:1.23-bookworm`, but `go.mod` declares `go 1.25.1`, so a 1.23 toolchain refuses the build ("go.mod requires go >= 1.25.1"). Pinned to `golang:1.25.1-bookworm`: Debian-based as D14 intended, and the patch-exact tag prevents a mid-build toolchain download. (2) **Digest's human-readable tag** — D13 wants the tag as a same-line `# nonroot` comment on `FROM`; BuildKit rejects a trailing comment on `FROM` ("FROM requires either one or three arguments", reproduced on a minimal Dockerfile). The tag is preserved on the line directly above the digest instead, with a comment explaining why.
  - Verification (captured in the session log): `docker build --build-arg VERSION=1.2.3 --build-arg COMMIT=deadbeef -t l7lb:test .` succeeds (context ~4 KB, allowlist active); `docker inspect` shows all four OCI labels and `User=65532`; `docker run -d l7lb:test` logs `"starting","version":"1.2.3","commit":"deadbeef"` from `/etc/l7lb/config.yaml`, and `docker inspect --format '{{.State.Health.Status}}'` reports `healthy`. Image removed after the smoke. `make test`/`go test -race`/`go vet`/`make fmt` clean.

- [DONE] S3.T11 — Root `docker-compose.yml`, `prometheus-stack.yml`, ADR-0013 decision 18 (issue 06) (opencode, started 2026-09-22T22:33:42+05:30, completed 2026-09-22T22:55:08+05:30, repo-root compose composing the LB image with the three dummy backends, Prometheus, and Grafana; per-stack scrape config; ADR-0013 decision 18; spec D26–D38)
  - Goal: a single `docker-compose.yml` at the repository root that brings the whole demonstrable system up under one command — LB image (S3.T10) + three dummy backends + Prometheus + Grafana — additive to the two existing composes under `deployments/docker/`, which stay untouched and canonical for their specialised flows. A new `prometheus-stack.yml` scrape config targets `l7lb:9090`; the existing `prometheus.yml` gains only a header cross-reference comment. ADR-0013 gains decision 18. Every design decision is spec D26–D38.
  - Files: `docker-compose.yml`, `deployments/docker/observability/prometheus/prometheus-stack.yml`, `deployments/docker/observability/prometheus/prometheus.yml` (header comment only), `docs/adr/0013-observability-metrics-logging-and-integration.md`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/06-t11-root-compose.md`, `docs/sessions/2026-09-22-opencode.md`
  - Depends on: S3.T10 (issue 05 — the LB image the `l7lb` service builds)
  - Acceptance: `docker compose -f docker-compose.yml config` parses listing five services; `docker compose up -d --build` converges with `l7lb` `Up (healthy)`; `:8080` proxies to a live backend; `:8081` serves `/livez`/`/readyz`/`/startupz` with the pinned JSON envelope; Grafana at `:3000` renders the provisioned dashboard; Prometheus stays unpublished; `docker stop backend-a` leaves `/readyz` 200 and `:8080` serving, and stopping all three yields `/readyz` 503 with `"selectable_backends": 0`, recovering on `docker start backend-a`; `prometheus-stack.yml` names `l7lb:9090`; the existing `prometheus.yml` differs only by its added header cross-reference comment; both existing composes and both chaos scripts untouched; ADR-0013 decision 18 appended with decisions 1–17 unchanged; manual smoke recorded in the session log; this entry links spec decisions D26–D38.
  - Test approach: none — compose/infra-only (AGENTS.md TDD exception, matching S1.T9/S3.T7). Full end-to-end smoke executed and recorded in the session log: parse (`config --services` → five), `up -d --build` converge, all five curl/probe checks, Prometheus target `up` and seeded gauges visible from inside the container, the `docker stop`/`docker start` chaos-and-recovery flow, and `down` teardown (no containers or network left).

- [DONE] S3.T13 — Sprint 3 retro and additive `docs/architecture.md` update (issue 07) (opencode, started 2026-09-23T00:00:00+05:30, completed 2026-09-23T00:30:00+05:30, new `docs/sprint-3-retro.md` (five sections mirroring S2.T8) plus an additive-only `docs/architecture.md` update covering the six Sprint 3 subsystems; docs-only, no new ADR; spec D39–D44)
  - Goal: write the Sprint 3 closeout artifacts — a new `docs/sprint-3-retro.md` (Deliverables shipped / Deviations from MILESTONES-spec / ADR sweep / Architectural gaps left open / Sprint 4 handoff) and an additive-only update to `docs/architecture.md` covering active health checking, passive outlier detection, the circuit breaker, the observability pipeline, the health endpoint, and the container demo stack, with cross-references to ADR-0011–0014 and the two amendments.
  - Files: `docs/sprint-3-retro.md`, `docs/architecture.md`, `PROGRESS.md`, `.scratch/s3-t10-t13-demo-stack/issues/07-t13-sprint-3-retro.md`, `docs/sessions/2026-09-23-opencode.md`
  - Depends on: S3.T10 (issue 05), S3.T11 (issue 06), S3.T12 (issue 03) — all [DONE]
  - Acceptance: `docs/sprint-3-retro.md` exists with the five sections in order; the Deliverables section covers S3.T0.1–T9 plus T6.5 and T10–T13 with one-line summaries and commit links; the Deviations section has exactly five paragraphs including the amendment-first precedent naming; the ADR-sweep table lists every Sprint 3 ADR (0004–0014) plus both amendments with no blank cells or undeferred gaps and verifies inverse coverage; the Architectural Gaps section names the Half-Open scan-promotion log gap, the deferred deployment-target ADR, and the two manual-verification-pending shell smokes; the Sprint 4 Handoff section enumerates every spec D43 item; `docs/architecture.md` gains six new/refreshed Sprint 3 sections with ADR cross-references and its Sprint 1/2 sections are unchanged; no new ADR; no code files touched; `PROGRESS.md` marks this ticket `[DONE]`, declares Sprint 3 complete, and links spec decisions D39–D44.
  - Test approach: none — docs-only (AGENTS.md TDD exception). Verification is the D41 ADR-sweep checklist itself (`git log --grep`/`grep -r` per ADR).
  - Completion: wrote `docs/sprint-3-retro.md` — *Deliverables shipped* covering S3.T0.1–T9, T6.5, and T10–T13 with commit links; the five *Deviations* paragraphs (S3.T6.5 mid-sprint fix; T8/T9 `test/chaos/assemble` duplication converging on Sprint 4's `Run(ctx, cfg)`; the permanent Half-Open scan-promotion log gap; the T10–T13 amendment-first precedent named verbatim; T9's three owner-approved spec-to-implementation conflicts); the *ADR sweep* table over ADR-0004–0014 plus all four amendments (the two the ticket named — ADR-0005 and ADR-0013 decision 18 — and the ADR-0011 2026-09-22 and ADR-0013 2026-09-21 amendments), every row matched or explicitly justified (ADR-0006 superseded by ADR-0011 decision 2), plus inverse coverage over all four bundle specs' decisions; the *Architectural gaps* section (Half-Open log gap; deferred deployment-target ADR; the two `[MANUAL VERIFICATION PENDING]` chaos shell smokes); and the *Sprint 4 handoff* debt inventory enumerating spec D43. Refreshed/added `docs/architecture.md`'s six Sprint 3 subsystem sections (active health checking refreshed for the `>=` gate, transition logs/gauge, and `ProbeRoundComplete`; passive outlier detection new; circuit breaker refreshed for `CircuitTransition` + `lb_circuit_state` + the scan-promotion gap; observability pipeline new; health endpoint new; container demo stack new), indexed ADR-0013/ADR-0014, and removed circuit breaking and Prometheus metrics from the "deliberately not here yet" list. Sprint 1/2 request-path and selection-seam sections untouched. No new ADR, no code touched. Spec decisions D39–D44 are the provenance.

