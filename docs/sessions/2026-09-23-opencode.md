# Session: 2026-09-23 — opencode — S3.T13

## Goal

S3.T13 (issue 07 of `.scratch/s3-t10-t13-demo-stack/`) — the Sprint 3
close-out artifacts. Two docs touched, no code, no new ADR: a new
`docs/sprint-3-retro.md` in the S2.T8 five-section shape and an additive-only
update to `docs/architecture.md` covering the six Sprint 3 subsystems. Last
ticket of Sprint 3; completing it flips the sprint to complete. Spec
decisions D39–D44.

## Orientation

Read `AGENTS.md`, `PROGRESS.md`, `MILESTONES.md`, `docs/architecture.md`,
every Sprint 3 ADR (ADR-0004 through ADR-0014 plus the four amendments), the
four bundle specs' Implementation Decisions sections
(`.scratch/s3-t1-t3-health-passive-circuit/`, `.scratch/s3-t4-t7-observability/`,
`.scratch/s3-t6-5-t8-t9-chaos/`, `.scratch/s3-t10-t13-demo-stack/`), the
S3.T8/T9 session-log review notes, and the four issue files. Confirmed no
`[IN_PROGRESS]` task was held by another agent; T10, T11, T12 were all
`[DONE]`, so ticket 07's blockers (03, 05, 06) were clear.

## Scope boundary (AGENTS.md Step 2.5)

- **In scope:** the new `docs/sprint-3-retro.md`; the additive
  `docs/architecture.md` Sprint 3 sections; the `PROGRESS.md` close-out; the
  session log; the issue-file close-out.
- **Out of scope, deliberately not built:** any production or test code; a
  new ADR; edits to Sprint 1/2 request-path or selection-seam sections;
  closing any gap the retro documents (Half-Open scan-promotion logging, the
  `Run(ctx, cfg)` seam, the two manual-verification-pending shell smokes,
  test-coverage gaps). Each is a future ticket's job, named in the retro's
  Sprint 4 handoff.

## Done

- Task: S3.T13 (issue 07), recorded in `PROGRESS.md`.
- Claim commit: `fd7bb85` (`chore(progress): start S3.T13 sprint 3 retro`);
  completion commit: `cf21b22` (`docs(retro): add Sprint 3 retro and
  architecture-doc update (S3.T13)`).
- `docs/sprint-3-retro.md` (new): the five sections in order.
  - *Deliverables shipped* — a table covering S3.T0.1–T9, S3.T6.5, and
    T10–T13 (plus the prefactor sub-tickets T10.0/T12.0/T10–T13.D0), each
    with a one-line summary and the implementation + review commit SHAs.
  - *Deviations from MILESTONES/spec* — exactly five paragraphs: the
    mid-sprint S3.T6.5 `==`→`>=` fix (`301dd95`, ADR-0011 2026-09-22
    amendment); the T8/T9 `test/chaos/assemble` duplication converging on
    Sprint 4's `Run(ctx, cfg)` seam; the permanent Half-Open
    scan-promotion log gap (ADR-0013 decision 13); the T10–T13 mid-sprint
    scope additions with the amendment-first precedent named verbatim
    (*"mid-sprint scope additions require a `MILESTONES.md` amendment commit
    before any code lands, per S3.T10–T13's amendment-first ordering (ticket
    01 of this bundle)"* — the issue's exact wording; spec D40 phrases the
    same precedent as `(D0)`); T9's three owner-approved
    spec-to-implementation conflicts.
  - *ADR sweep* — a per-ADR table over ADR-0004–0014 plus all four
    amendments (the two the ticket named — ADR-0005 and ADR-0013 decision
    18 — plus the ADR-0011 2026-09-22 and ADR-0013 2026-09-21 amendments),
    every row "code exists and matches" or explicitly justified (ADR-0006
    superseded in place by ADR-0011 decision 2; ADR-0005 has no code by
    design); plus an inverse-coverage table over all four bundle specs'
    decisions. No blank cells, no undeferred gaps.
  - *Architectural gaps left open* — the Half-Open scan-promotion log gap,
    the deferred deployment-target ADR (ADR-0005 + its amendment), and the
    two `[MANUAL VERIFICATION PENDING]` shell smokes (`eviction.sh`,
    `circuit.sh`).
  - *Sprint 4 handoff* — the spec D43 debt inventory, worded for a Sprint 4
    kickoff prompt to quote verbatim.
- `docs/architecture.md` (additive): header flipped to "Sprints 1–3 are
  complete … the Sprint 1–3 reference"; refreshed the active-health-checking
  section (`>=` reinstatement gate, transition log/gauge signal,
  `ProbeRoundComplete`); added a passive-outlier-detection section; refreshed
  the circuit-breaker section (`CircuitTransition`, `lb_circuit_state`, the
  scan-promotion gap); added observability-pipeline, health-endpoint, and
  container-demo-stack sections; added ADR-0013/0014 rows to the decision
  index; refreshed the component-map Sprint 3 rows (`cmd`, `proxy`,
  `config`, `logger`, `metrics`, `health`, `circuit`); and removed circuit
  breaking + Prometheus metrics from "deliberately not here yet". The
  Sprint 1/2 request-path diagram, selection-seam section, and deviations
  audit are untouched.
- `.scratch/.../07-t13-sprint-3-retro.md`: `Status: done`, all acceptance
  boxes ticked.

## Decisions

- **The ADR sweep includes all four Sprint 3 amendments, not only the two
  the ticket names.** The ticket's "two amendments" wording covers the
  demo-stack bundle's two, but the ADR-0011 2026-09-22 amendment (the
  S3.T6.5 fix) and the ADR-0013 2026-09-21 amendment (`CircuitTransition`
  gains `Reopened`) are code-bearing Sprint 3 amendments; omitting them
  would have left the "every ADR has shipped code" claim incomplete. Extra
  rows are additive and cost nothing.
- **ADR-0006 is marked superseded, not a gap.** Its `SetHealthy` contract
  was split in place by ADR-0011 decision 2; the two named methods are the
  live code. Recording it as a gap would be false.
- **No ADR for T13** (spec D44): the file's shape becomes the template by
  existing, exactly as S2.T8's did.
- **Component-map rows refreshed, request-path diagram untouched.** The map
  rows are per-package as-built status and already carried Sprint 3 content;
  the diagram and selection-seam prose are the frozen Sprint 1–2 reference
  and stay put, satisfying the ticket's "Sprint 1/2 sections unchanged"
  acceptance item.

## Verification

- Docs-only: no Go code or tests changed.
- `make test` and `go vet ./...` clean (sanity, not required).
- `git diff --stat` limited to `docs/sprint-3-retro.md`,
  `docs/architecture.md`, `PROGRESS.md`, the issue file, and this log.
- Anchor links in the retro and architecture doc checked against the ADR
  headings they target.

## Review (two-axis, post-implementation)

Both axes ran against `17020a4...cf21b22`. Findings acted on: the
amendment-first precedent quote in §2.4 used spec D40's `(spec D0)` phrasing
instead of the issue's exact `(ticket 01 of this bundle)`; corrected to the
issue's wording with the D40 phrasing noted alongside. The T13 row in §1 now
cites the completion commit `cf21b22` rather than only the claim SHA. The
`internal/proxy` and `internal/logger` component-map rows were reverted to
their original Sprint 1 text so no row labelled purely "Sprint 1" carries a
hunk, tightening the "Sprint 1/2 sections unchanged" acceptance item (the
`cmd`, `config`, `health`, `metrics`, and `circuit` rows stay refreshed —
they were already Sprint-3-labelled). Deliberately not acted on: the §4/§5
restatement of the debt inventory and the session-log/PROGRESS duplication —
both are mandated by the issue's five-section shape and AGENTS.md Step 6, so
the documented standard overrides the baseline smell. Spec axis found no
missing requirement, no harmful scope creep, and no implemented-but-wrong
claim; all cited SHAs and ADR facts verified against the repo.

## Open items / handoff notes

- **Sprint 3 is complete.** All `MILESTONES.md` Sprint 3 exit criteria are
  met or demonstrably covered; the two `[MANUAL VERIFICATION PENDING]` shell
  smokes await a Docker daemon.
- **Sprint 4's starting inventory** is `docs/sprint-3-retro.md` §5, which
  enumerates the `Run(ctx, cfg)` seam + chaos-assembly convergence, the
  Half-Open log gap (pinned at two test layers), the deferred
  deployment-target ADR, the two manual shell smokes, and the ADR sweep's
  finding of no further coverage gaps.
