# Session: 2026-09-25 — opencode — S4.D0

## Goal

S4.D0 (issue 01 of `.scratch/s4-t0-t4-reload/`) — the tracking amendment that
opens Sprint 4. Amend `MILESTONES.md`'s Sprint 4 deliverables with the
programmatic application seam and add the S4.T0–T4 entries plus the four
deferred follow-ups to `PROGRESS.md`, before any reload code lands, following
the S3.T10–T13.D0 amendment-first precedent. Docs-only, TDD-exempt. Spec
stories 46–47, decisions D1–D2.

## Orientation

Read `AGENTS.md`, `PROGRESS.md`, `MILESTONES.md`, the S4 reload bundle spec
(`.scratch/s4-t0-t4-reload/spec.md`), and all eight ticket files. Confirmed no
task in `PROGRESS.md` was `[IN_PROGRESS]` by another agent; the Sprint 4
section did not yet exist, so this amendment is the first Sprint 4 entry.
`git log` on the analogous S3.T10–T13.D0 amendment (`e8f56fb`, `6a85c36`,
`fec54e9`) was read to mirror the claim / amendment / close-out commit shape.

## Scope boundary (AGENTS.md Step 2.5)

- **In scope:** one new `MILESTONES.md` Sprint 4 deliverable bullet (the
  application seam); `[TODO]` `PROGRESS.md` entries for S4.T0, S4.T1, S4.T2,
  S4.T3.0, S4.T3, S4.T4.0, S4.T4 with Goal/Depends on/Acceptance/Test approach
  drawn from tickets 02–08 and a spec pointer; the four proposed tickets; the
  status line; this session log; the issue-file close-out.
- **Out of scope, deliberately not built:** any production or test code; any
  ADR (ADR-0015/0016 belong to T1/T4.0); starting T0. The spec's ticket map is
  the scope boundary, and D0 builds only the tracking artefacts.

## Done

- Task: S4.D0 (issue 01), recorded in `PROGRESS.md` as `[DONE]`.
- Claim commit: `bae71fc` (`chore(progress): start S4.D0 tracking amendment`).
- Amendment commit: `ba1d232` (`docs(milestones): add S4 reload tickets and
  seam deliverable`) — touches only `MILESTONES.md` and `PROGRESS.md`, per the
  ticket's mandated message and file list.
- `MILESTONES.md`: Sprint 4 deliverables gain one bullet, placed first — the
  programmatic application seam (`internal/app` build/run, reload added
  in place) sourced from the Sprint 3 retro handoff. The existing SIGHUP
  reload bullet, the connection-lifecycle and transport bullets, the three
  ADR bullets, the exit criteria, and the deferred-deployment-target paragraph
  are unchanged.
- `PROGRESS.md`:
  - Status section gains a **Sprint 4 in progress** paragraph naming the
    bundle and its eight serial tickets.
  - New **Sprint 4 — Hard Subsystems** section with the S4.D0 `[DONE]` entry
    plus `[TODO]` entries for S4.T0, S4.T1, S4.T2, S4.T3.0, S4.T3, S4.T4.0,
    S4.T4. Each carries Goal / Depends on / Acceptance / Test approach drawn
    from the matching issue file and a pointer to the bundle spec; the two
    named hand-offs (T2→T3, T3→T4/T4.0) are recorded in their entries.
  - *Proposed tickets (awaiting owner approval)* replaces "none outstanding"
    with the four S4 follow-ups: `lb_config_reloads_total{result}`;
    client-cancellation misclassified as backend failure; non-backend
    hot-reload (algorithm/health/circuit/listen/drain-window); pre-warming
    added backends.
  - A **Sprint 5** pointer section replaces the old combined "Sprint 4, 5".
- `.scratch/.../01-d0-tracking-amendment.md`: `Status: done`, all acceptance
  boxes ticked.

## Decisions

- **S4.D0 gets its own Sprint 4 entry.** The ticket lists only T0–T4 as new
  entries, but the claim protocol requires the claimed task be visible in
  `PROGRESS.md`; the S3.T10–T13.D0 precedent likewise recorded the amendment
  task in its sprint list. The entry is additive.
- **The seven S4.T0–T4 `PROGRESS.md` entries carry Goal/Depends on/Acceptance/
  Test approach but no `Files` line.** The ticket names exactly those four
  lines; tickets 02–08 themselves do not enumerate files, so adding a `Files`
  line would invent detail the bundle does not yet fix. (Only the S4.D0 entry
  lists `Files`, since its deliverable is fixed at this point.)
- **The amendment commit touches both files.** The ticket's mandated commit
  message names the seam deliverable and the progress tickets; the S3
  precedent split the MILESTONES-only commit from the PROGRESS close-out, but
  this ticket's acceptance states the substantive amendment touches
  `MILESTONES.md` and `PROGRESS.md`, so they land together. The claim commit
  is separate, and the session log is a follow-up docs commit.

## Verification

- Docs-only: no Go code or tests changed.
- `make fmt`, `go vet ./...`, and `make test` clean (sanity, not required);
  `git status` confirmed only `MILESTONES.md` and `PROGRESS.md` modified for
  the amendment commit.
- Acceptance checklist walked item by item against the issue file.

## Review (two-axis, post-implementation)

Both axes ran against `9fa1146...HEAD`, on the docs-only diff.

- **Standards** found no hard violations. Acted on: the session log claimed the
  new `PROGRESS.md` entries carry no `Files` line while the S4.D0 entry does;
  the wording now scopes the claim to the seven S4.T0–T4 entries and notes why
  S4.D0 alone lists `Files`. Judgement calls left as-is: the claim commit
  bundles the new Sprint 4 section scaffolding (necessary — the section did not
  exist), `Status: done` in the issue file follows the repo's established
  issue-file convention rather than the triage-label vocabulary, and the
  `/tmp` designation is not used. Baseline *Duplicated Code* between the
  entries and their scratch tickets is mandated by the ticket ("drawn from
  tickets 02–08").
- **Spec** found the per-entry "pointer to this spec" was only at section
  level; each Goal now names `.scratch/s4-t0-t4-reload/spec.md` explicitly.
  The proposed-tickets preamble had dropped the S3 promotion history while
  adding the four entries; the original history is restored alongside the S4
  sentence. Deliberately not acted on as scope creep: replacing the
  `## Sprint 4, 5` placeholder heading with a real Sprint 4 section plus a
  Sprint 5 pointer (the placeholder's own text said "Tasks added per sprint"),
  and the protocol-mandated claim/session-log commits beyond the two files.
  The Spec axis verified all eight serial dependencies, all story/decision
  tags, and all three named hand-offs are exact against tickets 02–08.

## Open items / handoff notes

- **Sprint 4 is in progress.** Next ticket is S4.T0 (issue 02), blocked only
  by this amendment. ADR-0015 lands in S4.T1's design step, ADR-0016 in
  S4.T4.0's.
- The four proposed tickets are recorded but unapproved; do not build them
  inline.
