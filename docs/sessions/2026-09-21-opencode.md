# Session: 2026-09-21 — opencode

## Goal

S3.T5.1 (issue 02 of `.scratch/s3-t4-t7-observability/`) — Logger test
coverage. Give `internal/logger`, which has shipped since Sprint 1 with zero
test coverage, its first test file: `logger.New`'s level-threshold behavior
and the JSON shape of its output. Test-only ticket; no production code and
no other ticket in the S3.T4–T7 batch is touched.

## Done

- Task: S3.T5.1 (issue 02), recorded in `PROGRESS.md`.
- Commits: `a896be4` (claim), `9c280b4` (tests + close-out).
- `internal/logger/logger_test.go` (new):
  - `captureStdout` helper — swaps `os.Stdout` for an `os.Pipe` around a
    callback and reads the pipe back, restoring `os.Stdout` on return and via
    `defer`.
  - `parseRecords` helper — decodes newline-delimited output into one
    `map[string]any` per record, failing on any malformed line.
  - `TestNewEmitsAtConfiguredLevelAndAbove` — table over DEBUG/INFO/WARN/ERROR;
    for each configured level, logs one record at every level and asserts the
    emitted message set is exactly the configured level and above.
  - `TestNewWritesWellFormedJSON` — two subtests: a bare record has exactly
    the top-level `time`/`level`/`msg` keys, and attributes (`backend`,
    `status`) appear as top-level keys with a string `time`.
- `.scratch/s3-t4-t7-observability/issues/02-logger-test-coverage.md`:
  `Status: done`, boxes ticked, completion comment.
- `PROGRESS.md`: S3.T5.1 claimed (`[IN_PROGRESS]`) then flipped `[DONE]`;
  session-log entry added.

## Decisions

- **Test `logger.New` as-is; do not add a writer parameter.** `New(level)` is
  a frozen Sprint 1 signature (S1.T0.5), and the ticket asks only for
  coverage. Adding an `io.Writer` parameter would be a contract deviation
  requiring an ADR for a test-only need. The `os.Pipe` swap observes the
  real production handler on the real production writer.
- **No new ADR.** The only choice here is a test technique, not a system
  design decision; it is documented in the helper's comment and above.
- **`require` for setup, `assert` for values**, matching every existing test
  package.

## Scope boundary (AGENTS.md Step 2.5)

- **In scope:** `internal/logger/logger_test.go` only; the three acceptance
  boxes of issue 02.
- **Out of scope, deliberately not built:** the `event`/`reason` transition
  vocabulary (issue 03), the health/circuit transition log lines (issues
  04/05), the whole-request metrics hook and gauge wiring (issues 06–09), and
  the Grafana/demo stack (issue 10).

## Open items / handoff notes

- The S3.T5 logger batch's remaining tickets (03–05) are unstarted. Tickets
  04 and 05 both require `Backend.CircuitTransition`/transition logging on
  top of issue 03's vocabulary.
- `logger.New` still has no callers using a non-default level; the tests are
  the only exercise of the parameter so far.

## Verification

- Red/green note: this is a test-coverage ticket for an already-implemented
  function, so the tests pass on first run — there is no implementation
  change to drive a Red phase.
- `go test ./internal/logger/... -v` — all 6 cases pass.
- `go test -race ./internal/logger/...` green.
- `go test -cover ./internal/logger/...` → 100.0% of statements.
- `go vet ./...` clean; `gofmt -l internal/logger` empty.

---

## S3.T5.2 — Canonical transition-log vocabulary (issue 03)

### Goal

Freeze the two new transition-scoped `log/slog` fields (`event`, `reason`) and
their closed, snake_case Go-constant value sets in `internal/logger`, so
S3.T5's producer tickets (issues 04/05) reference one shared vocabulary instead
of inventing overlapping values. No behavior change — nothing logs these fields
yet. ADR-0013 decision 12 already fixes the value sets; this ticket only
implements them.

### Done

- Task: S3.T5.2 (issue 03), recorded in `PROGRESS.md`.
- Commits: `9321d4f` (claim), `d8410c2` (tests), then this close-out.
- `internal/logger/vocab.go` (new): `Event*` (`EventHealthEjected`,
  `EventHealthReinstated`, `EventCircuitOpened`, `EventCircuitClosed`,
  `EventCircuitHalfOpened`) and `Reason*` (`ReasonProbeFailures`,
  `ReasonProbeRecovered`, `ReasonOutlierWindow`, `ReasonConsecutiveFailures`,
  `ReasonTrialSuccess`, `ReasonTrialFailure`, `ReasonCooldownElapsed`), each
  with a doc comment naming its subsystem/transition and its paired value where
  one exists.
- `internal/logger/doc.go`: field list split into request-scoped (the original
  six) and transition-scoped (`event`, `reason`, pointing at `vocab.go`).
- `internal/logger/vocab_test.go` (new): table-pins every constant's exact
  string plus a snake_case shape check, and asserts each vocabulary is
  duplicate-free with an explicit cardinality (5 events / 7 reasons).
- `.scratch/s3-t4-t7-observability/issues/03-transition-log-vocabulary.md`:
  `Status: done`, boxes ticked, completion comment.
- `PROGRESS.md`: S3.T5.2 claimed (`[IN_PROGRESS]`) then flipped `[DONE]`.

### Decisions

- **Constants live in `internal/logger`, producers import it.** `internal/logger`
  is a leaf (no internal deps), so `internal/health`/`internal/circuit` gaining
  an import of it adds no cycle risk. This is where ADR-0013 decision 12 locates
  the vocabulary.
- **Value constants only; field names stay string literals.** The six existing
  request-scoped fields have no Go constants for their names, and the ticket
  asked only for the value sets. Introducing field-name constants now would be a
  new convention outside this ticket's scope.
- **No new ADR.** ADR-0013 decision 12 records the decision; this is its
  implementation.
- **Test the closed set, not just the values.** A typo'd or colliding value is
  exactly the failure this ticket exists to prevent, so the test pins values and
  asserts duplicate-freedom — cheap, and turns the "closed vocabulary" claim
  into a checked invariant.

### Scope boundary (AGENTS.md Step 2.5)

- **In scope:** `internal/logger/doc.go` field list; the two Go-constant value
  sets in `internal/logger/vocab.go`; a value-pinning test; the issue/PROGRESS
  bookkeeping. Nothing else.
- **Out of scope, deliberately not built:** the health/circuit transition log
  lines and `Backend.CircuitTransition` (issues 04/05), the whole-request
  metrics hook and gauge wiring (issues 06–09), the Grafana/demo stack (issue
  10), and any edit to `docs/design/sprint-1-contracts.md` (issue 03 named
  `doc.go` only; the spec's ADR-0013 decision 12 locates the vocabulary there).

### Verification

- Red: `go test ./internal/logger/ -run 'TestEventVocabulary|TestReasonVocabulary|TestVocabularies'`
  failed to build against undefined constants before `vocab.go` existed.
- Green: `go test ./internal/logger/...` passes; `make test` green.
- `make test-race` green (pre-existing macOS `ld: malformed LC_DYSYMTAB`
  warnings on `cmd`/`metrics` link steps are unrelated to this change).
- `go vet ./...` clean; `make fmt` produced no diff.
- `go test -cover ./internal/logger/...` → 100.0% of statements.

### Review

Two-axis review (`git diff 2473620...HEAD`), addressed in a follow-up commit:

- **Standards (Duplicated Code).** `vocab_test.go` listed every constant a
  second time in a standalone closed-set test; refactored to one `[]vocabCase`
  table per vocabulary, with `assertClosed` deriving the duplicate/cardinality
  check from the same table. Renamed the table field `subsyst` → `subsystem`.
- **Standards (Mysterious Name / honesty of the guard).** Reworded
  `assertClosed`'s comment: it forces a deliberate edit when the vocabulary
  size changes; it cannot detect a constant added to `vocab.go` but omitted
  from the test table (Go offers no built-in constant enumeration).
- **Spec (ambiguity in ADR-0013 decision 12).** The ADR's phrase
  "`circuit_half_opened`-via-`trial_failure`" does not match the state machine
  a failed trial returns the circuit to Open, not Half-Open. `EventCircuitHalfOpened`'s
  doc comment now states explicitly that a failed trial reopens with
  `EventCircuitOpened`/`ReasonTrialFailure`. The ADR itself was not edited —
  outside this ticket's scope; flagged here for the owner.
- **Spec (`backend` scope).** `doc.go` now notes that transition lines reuse the
  request-scoped `backend` field, not just the two new fields.
- **Deliberately not changed:** WARN/INFO levels are not frozen on the
  constants. The ticket scoped doc comments to subsystem/transition mapping,
  and pinning levels here would require resolving the above ambiguity in a
  second place. Levels stay in ADR-0013 decision 12 / spec story 22, where
  issues 04/05 consume them. `docs/design/sprint-1-contracts.md` still lists
  only the six request-scoped fields; the ticket named `doc.go` only, and the
  session log records this as deliberate.
- Re-verified after the refactor: `make test` and `make test-race` green,
  `go vet ./...` clean, `gofmt -l internal/logger` empty.

---

## S3.T5.3 — Health transition log lines (issue 04)

### Goal

Give `internal/health`'s two subsystems their first `slog` lines, edge-triggered
so a sustained failure/success streak logs exactly once per genuine transition
rather than once per probe or per request: the active checker emits
`event=health_ejected, reason=probe_failures` (WARN) and
`event=health_reinstated, reason=probe_recovered` (INFO); the passive outlier
detector emits `event=health_ejected, reason=outlier_window` (WARN). Both gain a
`*slog.Logger` through their existing constructors. No metrics involvement
(that is S3.T6).

### Done

- Task: S3.T5.3 (issue 04), recorded in `PROGRESS.md`.
- Commits: `bad8fed` (claim), `d6f9644` (Red tests + constructor call sites),
  then this close-out.
- `internal/health/checker.go`: `Checker` gains an unexported `log`
  `*slog.Logger`; `New(reg, interval, timeout, log)`. `probeOnce` now reads the
  backend's atomic health before each `Mark*` call and logs only on a genuine
  transition: ejection at `failures == probeFailuresBeforeUnhealthy &&
  target.IsHealthy()`, reinstatement at `successes == probeSuccessesBeforeHealthy
  && !target.IsHealthy()`.
- `internal/health/outlier.go`: `OutlierDetector` gains an unexported `log`
  field; `NewOutlierDetector(log)`. The ejection line lands inside the existing
  `!w.ejected && failures >= outlierFailuresBeforeEject` block, immediately
  after `d.eject(b)` — the same flag that already makes `MarkUnhealthy` fire
  once per episode.
- `internal/health/transition_log_test.go` (new): `captureLogger` /
  `logRecord` / `recordsWithEvent` helpers (the `proxy_test.go` buffered
  `slog.JSONHandler` pattern) plus `discardLogger`, and six tests asserting
  exactly-one-line-per-streak/episode, the WARN/INFO level, the
  `event`/`reason` pair, and the `backend` field — including the two
  genuine-transition guards.
- Existing call sites updated for the new constructor arity:
  `internal/health/checker_test.go` / `outlier_test.go` pass `discardLogger()`;
  `internal/proxy/proxy_test.go` passes `slog.Default()`;
  `cmd/l7LoadBalancer/main.go` passes its `log` into `health.New` and
  `newHandler` → `health.NewOutlierDetector`.
- `.scratch/s3-t4-t7-observability/issues/04-health-transition-log-lines.md`:
  `Status: done`, boxes ticked, completion comment.
- `PROGRESS.md`: S3.T5.3 claimed (`[IN_PROGRESS]`) then flipped `[DONE]`; current
  status and session-log entries updated.

### Decisions

- **The log gate is exact-equality AND genuine-state.** Issue 04 named the
  `== threshold` gate (distinct from the `>=` the `Mark*` calls use). On its own
  that gate still emits `health_reinstated` for a backend that never left the
  healthy state (every backend's first M successful probes at startup, since
  `NewRegistry` starts healthy) and `health_ejected` for a backend already
  ejected by passive detection. This was raised with the owner during the
  session; the agreed resolution was to add a state guard
  (`IsHealthy()`/`!IsHealthy()`, read before `Mark*` mutates it) so the line
  describes a genuine transition, matching ADR-0013 decision 11's "once per
  genuine transition" wording. The exact-equality gate is retained as
  specified. The guard reads `Backend`'s existing atomic health, so no new
  prober field is added, and because `Mark*` is idempotent no genuine
  transition can be missed (an episode that never changed state has no
  transition to log).
- **Log after `d.eject(b)` in the detector, before `Mark*` in the checker.**
  In the detector the shared `ejected` flag is already the edge trigger, so the
  line sits in that block; in the checker the state guard must read health
  before `Mark*` mutates it, so the line is emitted before the idempotent
  store.
- **Field names stay string literals; values come from `internal/logger`.** The
  `event`/`reason` values use the `logger.Event*`/`logger.Reason*` constants
  from issue 03; the field names (`backend`, `event`, `reason`) follow the
  existing literal convention, as in `proxy`. `internal/health` importing the
  `internal/logger` leaf adds no cycle risk.
- **No new ADR.** ADR-0013 decision 11 records the edge-triggered health
  logging decision; this ticket implements it. The state guard is the
  "genuine transition" clause of that same decision, not a new one.
- **`log` as the field name, `log` as the constructor parameter.** `internal/health`
  imports the `internal/logger` package for constants, so a `logger` field
  would be shadowed/confusing at the call site; `log` matches `main.go`'s local.

### Scope boundary (AGENTS.md Step 2.5)

- **In scope:** `internal/health`'s two constructors and their transition log
  lines; the new health tests; the mechanical call-site updates the constructor
  arity forces (`checker_test.go`, `outlier_test.go`, `proxy_test.go`,
  `main.go`); the issue/PROGRESS bookkeeping.
- **Out of scope, deliberately not built:** `backend.CircuitTransition` and the
  circuit transition log lines (issue 05); any metrics wiring — the collector,
  gauges, and the whole-request hook (issues 06–09); the Grafana/demo stack
  (issue 10); and any edit to `docs/design/sprint-1-contracts.md` or ADR-0013.

### Verification

- Red: `go test ./internal/health/...` failed to build — every updated caller
  passed 4 args to `New` / 1 arg to `NewOutlierDetector` before the production
  signatures changed.
- Green: `make test` passes; `make test-race` passes (pre-existing macOS
  `ld: malformed LC_DYSYMTAB` warnings on the `cmd`/`metrics` link steps are
  unrelated).
- `go vet ./...` clean; `make fmt` produced no diff; `make build` clean.
- Existing S3.T1/S3.T2 assertions are unchanged; only the constructor calls
  were updated for arity. The two new tests that pin the genuine-transition
  guards (`TestCheckerDoesNotLogReinstatementWhileAlreadyHealthy`,
  `TestCheckerDoesNotLogEjectionWhileAlreadyUnhealthy`) are additional coverage
  of the agreed behaviour.

### Review

Two-axis review (`git diff c66c5ad...HEAD`). No code changes resulted; the
findings and dispositions:

- **Standards — non-compiling Red commit `d6f9644` (hard).** A signature change
  cannot have a Red test commit that both exercises the new arity and compiles;
  the repo's own precedent is S3.T5.2's `d8410c2` (tests referencing undefined
  `Event*`/`Reason*` constants). The tests were then made Green by the very next
  commit. Acknowledged as the established pattern for signature-level changes,
  not rewritten (history is not rewritten here).
- **Standards — `logRecord`/`captureLogger` duplicated from `proxy_test.go`
  (judgement).** Cross-package unexported helpers cannot be shared without a new
  `testutil` package, which ADR-0005's scope posture discourages; deliberate,
  documented in the helper comment.
- **Standards — `backend`/`event`/`reason` tuple repeated three times
  (judgement).** Not extracted to a helper: three sites in one small package,
  and `internal/proxy`'s existing log sites use the same inline-attributes
  style. Left for consistency rather than adding a one-consumer logging
  abstraction.
- **Standards — `proxy_test.go` passing `slog.Default()` (judgement).** Not
  output pollution in practice: that package's `TestMain` sets the default to a
  discard JSON handler. Left as-is; those tests assert behaviour, not logs.
- **Spec — passive path has no genuine-state guard, so a backend the active
  checker already ejected can still produce a second `health_ejected`
  (`outlier_window`) for the same down-episode (judgement).** This is
  spec-literal: issue 04 says the detector "reuses its existing
  `ejected`-per-episode guard — no new edge-triggering logic", and its verified
  property ("exactly one line per ejection episode, not one per failure in the
  burst") holds. Extending the active path's owner-approved state guard to the
  passive path would be a further unapproved change to a second subsystem's
  semantics; recorded here as a known, accepted behaviour (two detectors may
  independently report the same down-episode with their own reasons) rather
  than changed. If the owner wants cross-subsystem single-reporting, that is a
  follow-up ticket.
- Everything else matched: levels, `event`/`reason` pairs, `backend` field,
  once-per-streak/episode, the ten acceptance boxes, and the out-of-scope
  boundaries (issues 05–10 untouched).

---

## S3.T5.4 — Circuit transition log lines (issue 05)

### Goal

Give `circuit.Breaker` its first `slog` lines, edge-triggered so each genuine
open/close/half-open transition logs exactly once and a sustained failure run
logs once, not once per request. `Backend`'s circuit methods must report
whether *this* call changed state without `internal/backend` gaining a logging
dependency; the breaker logs at its own `ObserveRoundTrip`/`Allow` call sites.
No metrics involvement (that is S3.T6).

### Done

- Task: S3.T5.4 (issue 05), recorded in `PROGRESS.md`.
- Commits: `f3cea91` (claim), `a6945c3` (ADR amendment), `23754e6` (Red tests
  + call-site arity updates), then this close-out.
- `internal/backend/backend.go`: exported `CircuitTransition` enum
  (`CircuitNoChange`/`CircuitOpened`/`CircuitReopened`/`CircuitClosed`/
  `CircuitHalfOpened`). `currentCircuit` now returns `(circuitSnapshot, bool)`
  where the bool reports whether *this* call performed the Open→Half-Open
  promotion. `CircuitAllow` returns `(bool, CircuitTransition)`, reporting
  `CircuitHalfOpened` only when it both promoted and won the trial.
  `CircuitSuccess` returns `CircuitClosed` only for Half-Open→Closed.
  `CircuitFailure` returns `CircuitOpened` (Closed→Open) or `CircuitReopened`
  (Half-Open→Open). `CircuitOpen` stays `bool` (the scan read path).
- `internal/circuit/circuit.go`: `Breaker` gains an unexported `log` field;
  `New(cooldown, log)`. `Allow` logs `circuit_half_opened`/`cooldown_elapsed`
  (INFO) on `CircuitHalfOpened`; `ObserveRoundTrip` logs
  `circuit_opened`/`consecutive_failures` and `circuit_opened`/`trial_failure`
  (WARN) and `circuit_closed`/`trial_success` (INFO) on the matching
  transitions, each with the `backend` field. The `CircuitGate` and
  `RoundTripObserver` signatures are unchanged.
- `cmd/l7LoadBalancer/main.go`: `circuit.New(*cfg.Circuit.Cooldown, log)`.
- `internal/circuit/transition_log_test.go` (new): buffered
  `slog.JSONHandler` seam (the `proxy_test.go`/`health` pattern) plus five
  tests — open-on-threshold with no re-log while open, half-open on trial
  admission, closed on trial success, reopened/trial_failure on trial failure,
  and the scan-won-promotion gap.
- `internal/backend/backend_test.go`: a `circuitAllow` helper plus five new
  transition-return tests; existing S3.T3 assertions unchanged, call sites
  updated mechanically for the new `CircuitAllow` arity.
- `internal/circuit/circuit_test.go`, `internal/proxy/proxy_test.go`: `New`
  call sites updated for the logger argument (`discardLogger()` /
  `slog.Default()`); assertions unchanged.
- `.scratch/s3-t4-t7-observability/issues/05-circuit-transition-log-lines.md`:
  `Status: done`, boxes ticked, completion comment.
- `PROGRESS.md`: S3.T5.4 claimed then flipped `[DONE]`; current status and
  session-log entry updated.
- `docs/adr/0013-observability-metrics-logging-and-integration.md`: decision
  10 amended to five values; decision 12's WARN list corrected; a dated
  Amendment section records why.

### Decisions

- **Add `Reopened` as a fifth `CircuitTransition` value (owner-approved).**
  The ticket and ADR-0013 decision 10 named four values, but the ticket's four
  log reasons include two that both end in Open (`consecutive_failures` from
  `Closed→Open`, `trial_failure` from `HalfOpen→Open`). A four-value enum
  carrying only the resulting state cannot tell the breaker which reason to
  log; ADR-0013's own "`circuit_half_opened`-via-`trial_failure`" phrase was
  the same inconsistency. Raised with the owner before coding; the agreed fix
  is a fifth value so each reason maps 1:1 to a transition. ADR-0013 was
  amended in place (no new ADR number) with a dated amendment section.
- **`CircuitAllow` reports `CircuitHalfOpened` only when it performs the
  promotion.** The promotion flag from `currentCircuit` is local to the call,
  so a `Registry.Selectable()`-scan-won promotion (the common production case,
  since every `Select()` scans before `Allow`) is never logged — preserving
  ADR-0013 decision 13's documented, permanent gap. Only the call that both
  promotes and takes the trial logs. A lock-free race where the promoter loses
  the trial CAS to another goroutine can also miss the line; this is the same
  class of accepted CAS race as ADR-0012's stale-while-Half-Open note.
- **`internal/backend` gains no import.** `CircuitTransition` is a plain
  `int`-backed type; the logging stays in `circuit`, which already imports
  `backend`. The `circuit` package imports the `internal/logger` leaf for the
  `Event*`/`Reason*` constants, adding no cycle risk.
- **Levels follow ADR-0013 decision 12.** `circuit_opened` (both reasons) at
  WARN; `circuit_closed` and `circuit_half_opened` at INFO.
- **No new ADR number.** ADR-0013 decisions 10/12 are the design; the
  fifth-value correction is an amendment to decision 10, not a new decision.

### Scope boundary (AGENTS.md Step 2.5)

- **In scope:** `backend.CircuitTransition` and the `Circuit*` method returns;
  `circuit.Breaker`'s logger and log lines; `circuit.New` arity; `main.go`
  wiring; the new tests; the mechanical call-site arity updates; the ADR-0013
  amendment and the issue/PROGRESS/session bookkeeping.
- **Out of scope, deliberately not built:** any metrics wiring — the
  collector, gauges, and the whole-request hook (issues 06–09); the
  Grafana/demo stack (issue 10); `internal/health` and `internal/balancer`
  changes; and any edit to `docs/design/sprint-1-contracts.md`.

### Verification

- Red: `go test ./internal/backend/... ./internal/circuit/...` failed to build
  against the not-yet-existing `CircuitTransition` constants, the two-value
  `CircuitAllow`, and the two-argument `circuit.New`.
- Green: `make test` passes; `make test-race` passes (pre-existing macOS
  `ld: malformed LC_DYSYMTAB` warnings on the `cmd`/`metrics` link steps are
  unrelated).
- `go vet ./...` clean; `make fmt` produced no diff.
- `go test -cover` → circuit 100.0%, backend 98.9%.
- Existing S3.T3 assertions unchanged; only `CircuitAllow`/`New` call sites
  were updated for arity.

### Review

Two-axis review (`git diff 2e39077...HEAD`, run as parallel Standards/Spec
sub-agents). Findings and dispositions:

**Standards**

- **Stale frozen contract (hard).** ADR-0012 decision 4 lists the old method
  signatures verbatim (`CircuitAllow(...) bool`, `CircuitSuccess()`,
  `CircuitFailure(threshold int)`); ADR-0013 decision 10 was amended but
  ADR-0012 — the ADR that actually froze those signatures — was not. Fixed: a
  pointer was added to ADR-0012 decision 4 naming the extended signatures and
  cross-referencing ADR-0013 decision 10's amendment.
- **Mangled ADR-0013 decision 12 sentence.** A stray run of spaces
  (`(passive), and     \`consecutive_failures\``) from an earlier edit. Fixed.
- **Undocumented nil contract on `circuit.New`.** `Breaker.log` is
  dereferenced on every genuine transition, so a nil logger panics at first
  transition. Fixed by documenting the non-nil precondition (consistent with
  `health.New`/`NewOutlierDetector`, which also take a logger without a nil
  guard).
- **Missing concurrency note for the new shared `log` field.** Fixed: the
  `Breaker` doc now states it is immutable after construction and that all
  mutable state lives on the `*Backend`.
- **Duplicated test seams** (`logRecord`/`captureLogger`/`discardLogger`
  re-declared from `proxy_test.go`/`health`) and **duplicated log-attribute
  tuple** (four `br.log.*` sites). Both already dispositioned in S3.T5.1–T5.3:
  no shared `testutil` per ADR-0005, and the inline-attributes style matches
  `internal/proxy`. No action.
- **Middle Man `circuitAllow` test helper.** Flagged, not changed: it is an
  in-comment-justified adapter that keeps the pre-existing S3.T3 assertions
  unchanged; the new transition returns are covered by the new dedicated tests.

**Spec**

- **Exactly-once violated by a concurrent-`Allow` race (real).** In the first
  cut, `CircuitAllow` reported `HalfOpened` only when the call both promoted
  *and* won the trial-slot CAS. If the promoter lost the trial CAS to a
  goroutine that saw an already-half-open circuit, both returned `NoChange`
  and the genuine promotion logged zero lines — an undocumented hole beyond
  the named `Selectable()` gap. Fixed: the transition now tracks the promotion
  itself (`currentCircuit`'s CAS has exactly one winner), so exactly one
  concurrent caller reports `HalfOpened` regardless of the trial outcome. New
  `TestBackendCircuitAllowReportsPromotionOnceConcurrently` pins it; the
  `CircuitAllow` doc and the ADR amendment record the rationale.
- **Identifier prefix divergence.** The ticket/ADR name bare values
  (`NoChange`, ...) while the Go constants are `Circuit`-prefixed to avoid
  generic package-scope names. Reconciled by documenting the prefix in the ADR
  amendment and the issue comment; the identifiers themselves are correct.
- **"Every existing S3.T3 test continues to pass unchanged" is only partly
  literal.** Assertions are unchanged, but `backend_test.go`, `circuit_test.go`,
  and `proxy_test.go` needed mechanical arity edits for the new
  `CircuitAllow`/`New` signatures — disclosed in the ticket comment, matching
  the S3.T5.3 precedent.
- **Stale-while-Half-Open now visible as a misleading `circuit_closed`/
  `trial_success` line.** A success from a request admitted while Closed that
  lands after a Half-Open promotion closes the circuit and logs
  `trial_success`, though no trial caused it. This is ADR-0012's already-known
  stale-while-Half-Open limitation (no per-request trial marker in the frozen
  `RoundTripObserver`), now newly observable in a log line; recorded here as an
  inherited, accepted limitation rather than fixed (fixing it is the
  cross-interface change ADR-0012 already rejected). No metrics or other
  out-of-scope work was touched.

Re-verified after the fixes: `make test` and `make test-race` green, `go vet`
clean, `make fmt` no diff, circuit coverage 100.0%.


