# Session: 2026-09-19 — claude

## Goal

S1.T10 (issue 01 of `.scratch/s1-t10-retro-architecture-doc/`) — Sprint 1
retro / architecture doc. Rewrite `docs/architecture.md` with an as-built
request path, component map, decision index, deviations audit, contract
amendment forward-pointers, and a "not here yet" boundary; record the Sprint
1 retro in this session log; flip S1.T10 to `[DONE]` in `PROGRESS.md`; land
all three in one atomic commit. Docs-only, so no Red-Green-Refactor phase
(AGENTS.md TDD exception) — this log and the doc rewrite are the
deliverable.

## Done

- `docs/architecture.md` — full rewrite, replacing the three-section
  placeholder. New/updated sections:
  - **Overview** — refined; stdlib-only request path, role split
    (proxy = lifecycle, selector = choice, registry = shared state, config =
    validation), and a boundary pointer to ADR-0005.
  - **Request path** — ASCII diagram (matching the two existing ASCII
    diagrams in `AGENTS.md`), from `http.Server` through
    `proxy.ServeHTTP`/`selector.Select`/`IncActive`/`ReverseProxy`/body-wrapper
    `Close()`/`DecActive`, plus five lifecycle notes citing ADR-0007.
  - **Component map** — the package dependency graph plus an as-built
    status table (Sprint 1 vs. Sprint 3 stub) for every `internal/` package,
    the `backend`-does-not-import-`balancer` rule, and a "where new selection
    algorithms plug in" subsection pointing at `Selector`, `NewFromConfig`,
    and the contract algorithm-identifier table.
  - **Decision index** — all 7 accepted ADRs plus the tracked-but-unwritten
    Sprint 2–4 TBD rows.
  - **Deviations from plan** — exactly two entries (below).
  - **Later amendments to Sprint 1 contracts** — forward-pointers to
    ADR-0006 (`Backend.healthy` row / `SetHealthy`) and ADR-0007
    (`Backend.active` row / proxy lifecycle). `docs/design/sprint-1-contracts.md`
    is left unedited, preserving its freeze guarantee.
  - **Deliberately not here yet** — subsystem + owning sprint list (Sprint 2
    selectors; Sprint 3 health/circuit/metrics; Sprint 4 reload,
    connection lifecycle, transport tuning, retry policy, deployment target;
    Sprint 5 HTTP/2 and benchmarks).
- `docs/sessions/2026-09-19-claude.md` (this file) — Sprint 1 retro in full.
  No standalone retro document, per the frozen S1.T10 acceptance criteria.
- `PROGRESS.md` — S1.T10 flipped `[TODO]` → `[DONE]`.
- **Zero new ADRs.** Three candidate undocumented decisions were evaluated
  against the project's three-part bar (hard to reverse / surprising without
  context / real trade-off — all three must hold) and none qualified:
  - `LeastConnections` tie-break rule (first-in-registry-order wins):
    already documented with rationale as key decision #10 in `AGENTS.md`;
    trivially reversible.
  - docker-compose `SLEEP_MS`/`FAIL_RATE` defaults: test-harness tuning
    knobs, trivially reversible, no real alternatives analysis.
  - S1.T7 `-addr` flag removal: surprising and a real trade-off, but
    trivially reversible — fails the bar on that leg alone.
  This is the intended outcome of applying the bar rigorously, not an
  incomplete audit.

### Deviation 1 (recorded in `architecture.md`)

S1.T7's `-addr` flag removal is now formally **owner-signed-off during the
S1.T10 retro**. Original reasoning is in `docs/sessions/2026-09-18-opencode.md`
(S1.T7 → Decisions); the sign-off gap was the S1.T7 code-review follow-up's
*Flagged for owner awareness* bullet in the same file. No ADR (fails the
reversibility leg of the bar).

### Deviation 2 (recorded in `architecture.md`)

`MILESTONES.md:10`'s "custom `Transport` pattern" Sprint 1 deliverable
wording is recorded as a **pre-implementation scope clarification**, not a
mid-implementation deviation. Supporting evidence cited in the doc:
`.scratch/s1-t3-t9-backend-selector-proxy/spec.md:115` (and `:171`) deferred
`http.Transport` tuning to Sprint 4 *before* S1.T6 was coded;
`AGENTS.md:291` and `MILESTONES.md:63` both place connection-pool tuning in
Sprint 4; "RoundTripper" appears nowhere in the source or git history prior
to this task. The
`MILESTONES.md` bullet (unchanged since the initial scaffold commit) was
simply never updated to match.

## Decisions

- **Corrected a citation carried by the issue.** Issue 01 asked the
  deviations section to cite `docs/design/sprint-1-contracts.md:115` as the
  Transport deferral. That line is in fact the error-handling convention's
  "config has no exported sentinels" paragraph; the contracts doc never
  mentions `http.Transport` at all (verified by `rg -i transport
  docs/design/sprint-1-contracts.md` and by reading the frozen file). The
  doc therefore cites only the verifiable sources
  (`.scratch/s1-t3-t9-backend-selector-proxy/spec.md:115,171`,
  `AGENTS.md:291`, `MILESTONES.md:63`) and the substantive claim is
  unchanged. The issue's filename `docs/sessions/2026-09-19-claude.md` was
  kept as specified; per the owner, `CLAUDE.md` and `AGENTS.md` are
  symlinked, so the agent label is conventional.
- **Audit scope.** The deviations / undocumented-decision audit covered
  `MILESTONES.md`, all 7 accepted ADRs, `docs/design/sprint-1-contracts.md`,
  and `AGENTS.md`'s key-decisions index. No undocumented non-trivial decision
  survived it beyond the three rejected candidates below.
- **ASCII over Mermaid.** The request-path diagram fits ASCII at the same
  granularity as `AGENTS.md`'s existing diagrams, so no new diagramming
  convention is introduced and the Deviations section needs no diagram-format
  entry.
- **Deployment-target decision confirmed intact.** Checked and confirmed
  **not** lost or orphaned: it is tracked consistently in
  `docs/adr/0005-scope-of-production-grade.md:61-64`, `MILESTONES.md:66`, and
  `AGENTS.md:321`, all pointing forward to Sprint 4. Logged here as a closed
  non-issue; deliberately **no** footnote added to `architecture.md` (the
  issue's Out of Scope explicitly declines that).
- **`docs/design/sprint-1-contracts.md` not edited.** The amendment
  forward-pointer lives only in `architecture.md`, so the frozen doc stays
  byte-identical.

## Open items / handoff notes

- Sprint 1 is closed. Next: **Sprint 2 — Advanced Algorithms**
  (`ConsistentHashBoundedLoads`, `PowerOfTwoChoicesEWMA`), scoped in
  `MILESTONES.md` with two ADRs required (bounded-loads vs. naive CH;
  P2C-EWMA vs. least-connections under latency skew).
- Sprint 2 also inherits one flagged item from the S1.T7 review: the
  accepted-algorithm set is encoded in two places (`config`'s
  `implementedAlgorithms` map and `balancer.NewFromConfig`'s switch), so
  adding a Sprint 2 selector needs two edits. Left for Sprint 2 because that
  is when the second edit first appears.
- `architecture.md` is now the onboarding reference; keep it current as each
  sprint lands (the "Deliberately not here yet" list is the checklist of
  things to move out of that section).

### Code-review follow-up

Two-axis review of `61c28ff...HEAD` (Standards + Spec, parallel) found no
missing acceptance bullets and no hard standards breach. Three actionable
findings were applied:

- **fix(docs): make the "RoundTripper" claim non-self-refuting.** The doc and
  this log said "RoundTripper appears nowhere in the project's git history";
  once this task's own docs landed, that became literally false. Reworded to
  "nowhere in the project's source or git history prior to this task."
- **fix(docs): resolve the contradictory Transport citation in the spec.**
  `spec.md:45` still asserted the wrong `sprint-1-contracts.md:115` citation
  the ticket carried, contradicting the correction in `architecture.md` and
  this log within the same commit. `spec.md:9`/`:45` now cite the real
  sources and carry an inline dated correction. (The ticket was already
  corrected; the spec now matches.)
- **fix(progress): widen the Sprint 1 completion claim.** `PROGRESS.md`'s
  status line said "S1.T1–S1.T10 are all `[DONE]`", omitting S1.T0 and
  S1.T0.5; now "S1.T0 through S1.T10 (including S1.T0.5)".

Dismissed as judgement calls (documented, not acted on): committing
`.scratch/s1-t10-retro-architecture-doc/` alongside the task is the
established repo convention (every prior task committed its spec + issues);
the "As-built package status" table is part of the requested component map,
not scope creep; the session log header is `## Open items / handoff notes` to
match the referenced prior art rather than the ticket's shorthand
`## Open items`.

Commit: `docs: address S1.T10 code-review findings`.
