# Session: 2026-09-22 — opencode

## Goal

S3.T6.5 (issue 01 of `.scratch/s3-t6-5-t8-t9-chaos/`) — Active checker
reinstatement gate `==` → `>=`. Close the shipped-code drift between
ADR-0011 decision 3's stated intent (a passively-ejected backend recovers
via the next successful active probe) and the active checker, whose gate
compared the consecutive-successes accumulator with `==` so an accumulator
already past M never fired `MarkHealthy`'s co-located log line and gauge
write. First of a three-ticket chaos bundle; ships standalone so S3.T8's
combined-signal recovery assertion can rely on it.

## Done

- Task: S3.T6.5 (issue 01), recorded in `PROGRESS.md`.
- Commits: `788e02f` (claim), `301dd95` (code + tests + ADR amendment +
  PROGRESS + this session log).
- `internal/health/checker.go`: reinstatement gate changed from
  `p.successes == probeSuccessesBeforeHealthy` to `>=`, keeping the
  `!p.target.IsHealthy()` genuine-state guard. `probeOnce`'s doc comment
  updated to explain the asymmetry (failure gate stays `==`, success gate
  is `>=`) and why, pointing at the ADR-0011 amendment.
- `internal/health/checker_test.go`:
  `TestProberReinstatesPassivelyEjectedBackendWithAccumulatorPastThreshold`
  — drives a real prober's accumulator past M while the backend is healthy
  (asserting no reinstatement line for an always-healthy backend), ejects it
  via `MarkUnhealthy` to simulate passive detection, then drives M successful
  probes and asserts `Backend.IsHealthy()` reads `true` with exactly one
  `health_reinstated`/`probe_recovered` slog record. Red-first: the test
  fails on the `==` gate (log count 0), passes on `>=`.
- `docs/adr/0011-health-passive-outlier-and-circuit-breaker-composition.md`:
  new "Amendment (2026-09-22): Reinstatement gate uses `>=`, not `==`"
  section after "Alternatives considered" — one paragraph, scenario + fix,
  in-place drift correction (ADR-0006 pattern), no new ADR.
- `.scratch/s3-t6-5-t8-t9-chaos/issues/01-s3-t6-5-reinstatement-fix.md`:
  boxes ticked, `Status: done`.
- `PROGRESS.md`: S3.T6.5 claimed then flipped `[DONE]`; the Sprint 3 status
  line moved from "Sprint 3 complete" to "Sprint 3 in progress — T1–T7 done,
  T8–T9 chaos tests remaining"; the old `[PROPOSED] S3.T6.5` bullet removed
  from the proposed section.

## Decisions

- **One-line fix, no new ADR.** The gate is a shipped-code drift from
  ADR-0011 decision 3, not a new decision; the amendment records it in place,
  exactly the pattern ADR-0006 set. The ADR table in `AGENTS.md` is unchanged
  (an amendment section is not a new row).
- **Failure gate deliberately left at `==`.** The `>=` change applies only to
  the success path. The failure gate's `== probeFailuresBeforeUnhealthy` was
  not part of the reported defect, and the consecutive-failure counter resets
  to zero on any success, so it cannot drift past N the way the uncapped
  success accumulator can. Changing it would be scope creep beyond the ticket.
- **Test asserts the log record, not the gauge.** The fix is at the checker
  layer; `lb_backend_healthy` is written from the same guarded block, and
  S3.T6.3 already verifies the observer→gauge wiring. The new test uses the
  existing `captureLogger`/`recordsWithEvent` seam in the same package and
  does structured field checks on `event`/`reason`/`backend`, never substring.
- **`require` for setup, `assert` for values**, matching every existing
  package.

## Scope boundary (AGENTS.md Step 2.5)

- **In scope:** `internal/health/checker.go`'s success gate, the one new test
  in `internal/health/checker_test.go`, the ADR-0011 amendment, and the
  PROGRESS/session-log tracking.
- **Out of scope, deliberately not built:** the S3.T8 eviction/recovery chaos
  package and its `docker stop` smoke; the S3.T9 circuit-trip/half-open chaos
  package and its smoke; `main.go`'s `Run(ctx, cfg)` refactor (Sprint 4);
  extending `deployments/docker/dummy-backend/`; any metrics collector,
  observer fan-out, or proxy wiring (the ticket forbids touching them); the
  `failure == N` gate.

## Review (two-axis, post-implementation)

Standards axis found no hard violations; the issue file's `Status: done` and
the `require`-for-log-count choice both follow established repo precedent
(prior closed tickets use `Status: done`; existing transition-log tests use
`require.Len`). It flagged the now-identical `>= M` condition appearing in two
adjacent blocks as a drift surface, and one test comment overstating when
reinstatement fires. The comment was corrected. The duplicated condition was
kept deliberately: it mirrors the failure path's two-block shape exactly
(`== N && healthy` for the log, `>= N` for `MarkUnhealthy`), and folding it
would break that symmetry for no behavioural gain.

Spec axis confirmed the gate, ADR amendment, and no-metrics/no-proxy scope all
match. Two findings acted on or resolved:
- **Missing "no-stall" assertion** (spec asked that the accumulator's
  post-transition state not stall further reinstatements) — added a second
  ejection episode to the test, asserting a fresh reinstatement line.
- **Literal "seeded `healthy=false` at zero-M" setup not followed** — the
  spec and ticket are self-contradictory here: a backend seeded unhealthy with
  a zero accumulator would reinstate at exactly `== M`, so the test would pass
  under the buggy gate and fail the ticket's own criterion ("under the current
  `==` gate, the test would fail"). The implementation keeps the scenario that
  actually reproduces the drift (accumulator past M at ejection). The ticket's
  `event=health.transition` wording is stale; the frozen S3.T5.2 value
  `health_reinstated` is used.

## Open items / handoff notes

- **S3.T8/T9 unstarted.** The chaos bundle's remaining two tickets
  (`02-s3-t8-eviction-chaos.md`, `03-s3-t9-circuit-chaos.md`) are the only
  Sprint 3 work left. T8's combined-signal recovery arc now has the fix it
  depends on.
- **Sprint 4 convergence pointer (from the spec).** The chaos tickets'
  local `assemble(t, cfg)` helper duplicates `main.go`'s wiring; when Sprint 4
  introduces a programmatic `Run(ctx, cfg)` seam for SIGHUP orchestration, it
  should converge with `assemble()` or render it dead code. Flagged here so
  the next orient step sees it.
- **ADR-0011 line 313 cross-reference unaffected.** The contract change in
  ADR-0012 decision 15 (`Selectable()` shipping with S3.T3) is untouched by
  this amendment.

---

# Session (continued): 2026-09-22 — opencode — S3.T8

## Goal

S3.T8 (issue 02 of `.scratch/s3-t6-5-t8-t9-chaos/`) — the backend
eviction/recovery chaos test. Verify the `Backend.healthy` axis end-to-end
against the full Sprint 3 assembly and land the shared `test/chaos/` harness
that S3.T9 reuses, plus the `docker stop` eviction smoke closing
`MILESTONES.md`'s literal wording. Second of the three-ticket chaos bundle;
depends on S3.T6.5's reinstatement-gate fix, which arc (iii) verifies.

## Done

- Task: S3.T8 (issue 02), recorded in `PROGRESS.md`.
- Commits: `d36c40e` (claim), and the atomic completion commit carrying the
  tests, the smoke, `PROGRESS.md`, and this log.
- `test/chaos/` — new external test package (`package chaos_test`, no
  non-test files, so the import graph is exactly a consumer's):
  - `helpers_test.go`: `flippableBackend` (`Serve200`/`Serve500` via an
    `atomic.Pointer[http.Handler]`; `Kill` closes the `http.Server` so probes
    and dispatch get real `ECONNREFUSED`; `Restart` rebinds the same address
    with a brief retry; `URL` stable across both), `newFlippableBackends`,
    and `doRequest` (drives the proxy handler directly, no client hop, no
    `*testing.T`).
  - `assertlog_test.go`: `captureHandler` (mutex-guarded `slog.Handler`
    retaining `slog.Record`s), `transitionCount`/`transitionRecords`, and
    `assertTransitionLogged` — a field-allowlist match on exactly
    `event`/`backend`/`reason`, never substring/regex.
  - `metrics_test.go`: `gaugeValueOK`/`assertGauge`, reading the collector's
    private registry via `Registry().Gather()`.
  - `assemble_test.go`: `assembly` handle, `chaosConfig` (fast-path
    `probeInterval=20ms`/`probeTimeout=100ms`/`cooldown=200ms`), `assemble(t,
    cfg)` duplicating `main.go`'s Sprint 3 wiring (collector, seeded registry,
    breaker as gate + observer, selector, proxy + observers, active checker on
    a cancelable context), and a local `seedMetrics`.
  - `eviction_test.go`: arc (i) `TestChaosActiveOnlyEvictionAndRecovery`
    (kill → gauge 0 + one `health_ejected`/`probe_failures` + selector avoids
    X; restart → gauge 1 + one `health_reinstated`/`probe_recovered` + selector
    resumes X) and arc (iii)
    `TestChaosCombined500SignalHealthOnlyRecovery` (500s → gauge 0 +
    `circuit_state{open}=1` + one `health_ejected` + one
    `circuit_opened`/`consecutive_failures`; then 200s → gauge 1 + one
    `health_reinstated` while `circuit_state{open}` stays `1`).
- `deployments/docker/chaos/eviction.sh`: `set -euo pipefail`, builds the LB
  as a host process, brings up the S1.T9 compose, polls
  `lb_backend_healthy{backend="backend-a"}` through `:9090/metrics` across
  `docker stop` → `0` and `docker start` → `1`. Passes `bash -n` and
  `shellcheck` (0.11.0).
- `deployments/docker/chaos/README.md`: shared prereqs + conventions, the
  exact Prometheus queries and expected values for `eviction.sh`, and a note
  that `circuit.sh` (S3.T9) shares this README.
- `.scratch/.../02-s3-t8-eviction-chaos.md`: all 26 boxes ticked,
  `Status: done`, completion comment.

## Decisions

- **Log capture is a `slog.Handler`, not the JSON-buffer seam.** The existing
  packages capture JSON text because they are internal test packages; an
  external package cannot reach unexported seams, and `slog.Record` gives
  structured field access directly. `assertTransitionLogged` asserts exactly
  one match (the arcs' "exactly one" wording), with `transitionCount` exposed
  for the negative/baseline case.
- **Gauge reads go through `Registry().Gather()`, not `testutil.ToFloat64`.**
  `internal/metrics` keeps its instruments unexported, so the external package
  cannot name the target `GaugeVec`; gathering the registry is the same
  S3.T6.3-family path `testutil` wraps, and is the only read the collector
  exposes. No production accessor was added (the ticket forbids prod-code
  edits outside `internal/health`).
- **`Kill` closes the whole `http.Server`, not just the listener.** Closing
  only the listener leaves pooled keep-alive connections serving, so the
  health client and proxy transport would keep succeeding through a dead
  backend. `Server.Close` drops the listener and all live connections, giving
  the real `ECONNREFUSED` the arc needs.
- **`assemble` routes `slog.Default()` at the capture handler for the test's
  duration.** `proxy.New`'s frozen two-arg signature captures `slog.Default()`
  at construction, so request lines would otherwise spam the test output; the
  default is restored in cleanup.
- **Arc (iii) is deterministic because the circuit opens at 3 failures, below
  passive detection's 5-in-window threshold.** Three sequential round-robin
  selections of the 500 backend open its circuit and exclude it before passive
  detection can reach its own threshold, so the only ejection line is the
  active checker's `probe_failures` — the "one health transition log" the arc
  asserts.
- **`shellcheck` installed via Homebrew** to satisfy the ticket's shell-half
  `[DONE]` gate (it was not present).

## Scope boundary (AGENTS.md Step 2.5)

- **In scope:** the shared `test/chaos/` harness, arcs (i) and (iii), the
  `eviction.sh` + shared `README.md` smoke, and the tracking updates.
- **Out of scope, deliberately not built:** S3.T9's circuit chaos arcs and
  `circuit.sh` (issue 03); `main.go`'s `Run(ctx, cfg)` refactor (Sprint 4);
  containerizing the LB (S3.T10); extending `deployments/docker/dummy-backend/`;
  migrating the existing `internal/proxy` unit chaos tests; any new ADR (the
  bundle's only ADR change was S3.T6.5's in-place ADR-0011 amendment).

## Verification

- `make test`, `make test-race`, `go vet ./...`, `make fmt` (no diff),
  `go mod tidy` (no diff), `make build` — all clean. The macOS `ld: malformed
  LC_DYSYMTAB` linker warning is the known, unrelated one.
- `test/chaos` run 40× under `-race` without flaking.
- `bash -n` and `shellcheck` clean on `eviction.sh`.
- End-to-end `docker compose up` execution is `[MANUAL VERIFICATION PENDING]`
  (Docker daemon availability), per S3.T7 precedent.

## Open items / handoff notes

- **S3.T9 remains.** Issue 03 (`03-s3-t9-circuit-chaos.md`) owns the
  circuit-gate axis and `circuit.sh`; it reuses this ticket's `assemble`,
  `flippableBackend`, and log/gauge helpers unchanged.
- **Sprint 4 convergence pointer (repeated from the spec).** `test/chaos/`'s
  `assemble(t, cfg)` duplicates ~20 lines of `main.go`'s wiring; when Sprint 4
  introduces a programmatic `Run(ctx, cfg)` seam it should converge with or
  replace `assemble()`.

## Review (two-axis, post-implementation)

Standards axis found no hard violations. It flagged the `net.Listen` +
`http.Server` harness (AGENTS prefers `httptest.NewServer`) as a justified
deviation — `httptest.Server` cannot rebind a fixed address, and the arcs need
a stable URL across Kill/Restart and a real `ECONNREFUSED` — plus baseline
smells: the `stop()` middle-man, the four-times-repeated
`require.Eventually(gauge→want)` shape, and a gauge-rationale comment that
overstated what `testutil` can reach.

Spec axis confirmed no scope creep and the smoke's bar met, and found:
- **Arc (iii) does not exercise passive ejection.** The circuit opens at 3
  failures, below passive detection's 5-in-window threshold, so the circuit
  excludes the bad backend before passive can eject. This is a genuine
  inconsistency in the bundle spec's prose ("passive detection and active
  checker both eject on the same signal") rather than a test defect: with the
  frozen constants (circuit 3, passive 5) passive can never reach its
  threshold on a constant-500 backend. Arc (iii)'s own bullet — "one health
  transition log" — is only satisfiable by the active checker logging and
  passive being suppressed by the `IsHealthy()` guard. Passive detection stays
  covered at unit scope (S3.T2 and `internal/proxy/proxy_test.go`). The commit
  message's "combined passive/active ejection" wording overstated this; the
  test's actual claim (one signal drives both the health ejection and the
  circuit open) is accurate.
- **"Exactly one health transition" was reason-specific only.** A second
  ejection line with a different reason would have passed. Added
  `eventCount(event, backend)` and asserted exactly one `health_ejected` and
  exactly one `circuit_opened` for the backend regardless of reason.
- **Arc (i)'s "selector no longer chooses X" was vacuous.** It only checked
  the body was not `x.id`, which a still-selected dead backend (502) also
  satisfies. Strengthened to require status 200 on every request in the burst,
  which a selected dead backend cannot produce.

Acted on: the strengthened arc (i) and (iii) assertions, the
`requireGaugeEventually` helper collapsing the repeated poll shape, removal of
the `stop()` middle-man (`t.Cleanup(fb.Kill)` directly), and the corrected
gauge-rationale comment. The passive-ejection gap is left as the spec's own
inconsistency, documented here rather than papered over.

## Follow-up commit

`refactor(test/chaos): address S3.T8 review findings` — strengthened arc (i)
and (iii) assertions (`eventCount`, status-200 burst), added
`requireGaugeEventually`, dropped the `stop()` middle-man, corrected the gauge
comment. Re-verified: `make test`, `make test-race`, `go vet`, `gofmt` clean;
`test/chaos` 20× under `-race` without flaking.

---

# Session (continued): 2026-09-22 — opencode — S3.T9

## Goal

S3.T9 (issue 03 of `.scratch/s3-t6-5-t8-t9-chaos/`) — the circuit-breaker
trip and half-open recovery chaos test. Verify the circuit-gate axis
end-to-end against the Sprint 3 assembly, reusing S3.T8's `test/chaos/`
harness, plus the 5xx-injection `circuit.sh` smoke closing `MILESTONES.md`'s
literal wording. Last of the three-ticket chaos bundle; completing it closes
Sprint 3.

## Spec/architecture conflicts surfaced before implementation (owner-approved)

Orienting per AGENTS.md Steps 0–2 surfaced three of the ticket's acceptance
bullets that are **not reachable** against the frozen architecture. Each was
confirmed by tracing the code and by throwaway diagnostic tests before any
deliverable was written, then put to the owner, who chose "adapt to real
behavior" over closing the gaps in production code (which would need an ADR
outside the bundle's stated scope):

1. **`circuit_half_opened` + `lb_circuit_state{half_open}` never fire via the
   proxy.** `ServeHTTP` calls `Select()` first; `RoundRobin.Select` →
   `Registry.Selectable()` calls `gate.Open()` on every backend, which wins
   the lazy Open→Half-Open promotion CAS in `Backend.CircuitOpen`. By the
   time `Registry.Allow(b)` → `Breaker.Allow` → `Backend.CircuitAllow` runs,
   the promotion already happened and `promoted` is false, so it reports
   `CircuitNoChange` and writes nothing. This is exactly ADR-0013 decision
   13's documented, accepted, permanent gap. The tests therefore **pin** the
   gap (assert zero `circuit_half_opened` records) and prove the lazy,
   timer-free property through the trial's resolution — `circuit_closed`/
   `trial_success` or `circuit_opened`/`trial_failure` — firing on the
   post-cooldown request.
2. **"503 via `Registry.Allow` denial during cooldown" is unreachable through
   the proxy.** `Selectable()` filters open circuits, so `Select` never
   returns an open backend and `Allow` is never consulted for it. With the
   single test backend, a cooldown request is denied by
   `ErrNoHealthyBackends` — still a 503, still without touching
   `IncActive`/`lb_active_connections`, which is the contract ADR-0012
   actually guarantees. The test asserts that observable contract and does
   not claim the `Allow` path.
3. **The ticket's `probeInterval=20ms` fast path makes arcs (iv)/(v)
   impossible.** Active probes treat a 5xx as failure, so the 500-serving
   backend is health-ejected and drops out of `Selectable` before a half-open
   trial can ever be taken. T9 neutralizes the checker with a one-hour
   `probe_interval` (a config knob, ADR-0011 decision 10 — no production
   constant bent) and uses a **single backend** so round-robin's post-cooldown
   selection of the trial is deterministic.

No deviation warrants a lazy S3.T10 close-out, so Sprint 3 flips to complete.

## Done

- Task: S3.T9 (issue 03), recorded in `PROGRESS.md`.
- Commits: `07b8fbf` (claim), and the atomic completion commit carrying the
  tests, the smoke, `PROGRESS.md`, the issue close-out, and this log.
- `test/chaos/circuit_test.go`:
  - `circuitChaosConfig` — single backend, round-robin, one-hour
    `probe_interval`, and a T9-specific `circuitChaosCooldown` (500ms, wider
    than the shared 200ms fast path so arc (iv)'s post-trip request lands
    inside the cooldown under `-race` scheduling).
  - `tripCircuit` — blasts until `lb_circuit_state{open}=1`; post-open
    requests are denied 503 and never reach the breaker, so the opening line
    fires exactly once.
  - Arc (iv) `TestChaosCircuitTripCooldownAndTrialSuccess` — trip → `open=1`
    + one `circuit_opened`/`consecutive_failures`; cooldown request → 503 with
    `lb_active_connections` at `0`; flip 200, poll the outcome → trial → 200
    → `closed=1` + one `circuit_closed`/`trial_success`; zero
    `circuit_half_opened` (gap pinned).
  - Arc (v) `TestChaosCircuitTrialFailureReopens` — leave 500 through
    cooldown → trial fails → `open=1` + one
    `circuit_opened`/`trial_failure`, no `circuit_closed`.
  - Arc (vi) `TestChaosCircuitNoPromotionWithoutTraffic` — `require.Never`
    through `chaosCooldown+100ms` asserts the gauge stays `open=1` and no
    promotion log fires with zero traffic; the first request after the window
    is the trial and its failure reopens the circuit, proving the promotion
    happened on that request (lazy, timer-free).
- `deployments/docker/chaos/circuit.sh`: `set -euo pipefail`; builds the LB as
  a host process; brings up the S1.T9 compose; recreates `backend-a` with
  `BACKEND_A_FAIL_RATE=1` (a `docker exec` cannot change a running process's
  env, so the compose per-backend override is used with `--force-recreate`),
  blasts traffic until `lb_circuit_state{state="open"}=1`; recreates with
  `FAIL_RATE=0`, blasts until `state="closed"=1`. Passes `bash -n` and
  `shellcheck` (0.11.0).
- `deployments/docker/chaos/README.md`: the circuit scenario section — exact
  recreate recipe, Prometheus queries, expected values, and the note that
  traffic must keep flowing for the trial to fire (no timer).
- `.scratch/.../03-s3-t9-circuit-chaos.md`: boxes ticked, completion section
  documenting the three deviations.

## Decisions

- **Single backend, not three.** With three backends a post-cooldown
  round-robin request may select a healthy peer, so "the first request is the
  trial" would be probabilistic; with one backend the trial is deterministic.
  The cost — the cooldown denial comes from `ErrNoHealthyBackends` rather than
  the `Allow` gate — is the second recorded deviation, and the
  active-connection-accounting contract still holds.
- **Checker neutralized via config, not by not starting it.** Keeping
  `assemble()` unchanged and pushing `probe_interval` to an hour reuses the
  shared harness exactly as the ticket asks, isolates the circuit axis, and
  bends no production constant.
- **The half-open gap is asserted, not ignored.** Following S3.T6.4's
  precedent (`internal/circuit/metrics_test.go` pins the scan-won promotion
  gap), the chaos tests assert zero `circuit_half_opened` records so the
  documented limitation is mechanically visible from the chaos layer too.
- **No production code changed.** All three conflicts are architecture facts,
  not bugs; the tests are adapted to the contract the ADRs actually specify.

## Scope boundary (AGENTS.md Step 2.5)

- **In scope:** `test/chaos/circuit_test.go`, the `circuit.sh` + README smoke,
  the issue close-out, and the tracking updates.
- **Out of scope, deliberately not built:** closing the ADR-0013 decision 13
  logging gap or making the `Allow`-denial path observable (would need an ADR,
  explicitly outside the bundle's scope); `main.go`'s `Run(ctx, cfg)` refactor
  (Sprint 4); containerizing the LB (S3.T10); extending
  `deployments/docker/dummy-backend/`; migrating the existing `internal/proxy`
  unit chaos tests.

## Verification

- `make test`, `make test-race`, `go vet ./...`, `make fmt` (no diff) — all
  clean. The macOS `ld: malformed LC_DYSYMTAB` linker warning is the known,
  unrelated one.
- `test/chaos` circuit arcs run 10× under `-race` without flaking.
- `bash -n` and `shellcheck` clean on `circuit.sh`.
- End-to-end `docker compose up` execution is `[MANUAL VERIFICATION PENDING]`
  (Docker daemon availability), per S3.T7 precedent.

## Open items / handoff notes

- **Sprint 3 is complete.** All of `MILESTONES.md`'s Sprint 3 exit criteria
  now have in-process chaos coverage plus documented shell smokes; the two
  `[MANUAL VERIFICATION PENDING]` smokes (`eviction.sh`, `circuit.sh`) await a
  Docker daemon.
- **Sprint 4 convergence pointer (repeated from the spec).** `test/chaos/`'s
  `assemble(t, cfg)` duplicates ~20 lines of `main.go`'s wiring; when Sprint 4
  introduces a programmatic `Run(ctx, cfg)` seam it should converge with or
  replace `assemble()`.
- **ADR-0013 decision 13's gap is now pinned at two layers.** The unit test in
  `internal/circuit/metrics_test.go` and the chaos test in
  `test/chaos/circuit_test.go` both assert the half-open promotion is never
  logged through the `Selectable` path. If a future ticket closes the gap,
  both assertions change together.

## Review (two-axis, post-implementation)

Standards axis found no hard violations. Judgement-call smells acted on:
`circuitChaosConfig` duplicated `chaosConfig`'s body (extracted a shared
`backendConfigs` builder), the pinned-gap assertion was repeated three times
(added the negative twin `assertTransitionNotLogged`), and the three tests
repeated their preamble (extracted `newCircuitAssembly`). Deliberately not
acted on: raw state strings (`circuitStateLabels(id, "open")`) rather than
`metrics.CircuitStateOpen` — this matches the existing S3.T8 harness exactly,
and changing it would churn the shared helper for no behavioural gain; the
shell `wait_for_value_while_blasting`/`wait_for_value` overlap — no shared-lib
precedent exists and the scripts are intentionally self-contained.

Spec axis confirmed the adapted arcs, the `circuit.sh` recipe, the README
documentation, and the Sprint 3 flip are all coherent. Findings acted on:
issue 03's `Status` still read `ready-for-agent` (flipped to `done`); the
PROGRESS "Files" list named `assemble_test.go` as the T9 config helper when it
actually lives in `circuit_test.go`; and arc (iv)'s cooldown request could
race the 200ms window under `-race` (widened to a T9-specific 500ms cooldown).
Not acted on: arc (iv)'s "no `IncActive` accounting" gauge check is weak
(a completed dispatch also drains to 0) but the 503 already proves
non-dispatch; and the trial is found by polling rather than "exactly one
request", which the timing decision sanctions.

## Follow-up commit

`refactor(test/chaos): address S3.T9 review findings` — shared
`backendConfigs` builder, `assertTransitionNotLogged` helper,
`newCircuitAssembly` fixture, corrected `tripCircuit` comment, T9 cooldown
widened to 500ms, issue `Status: done`, PROGRESS Files list corrected.
Re-verified: `make test`, `make test-race`, `go vet`, `gofmt` clean;
`test/chaos` circuit arcs 10× under `-race` without flaking.

---

# Session (continued): 2026-09-22 — opencode — S3.T10–T13.D0

## Goal

Issue 01 of `.scratch/s3-t10-t13-demo-stack/` — the precondition for the
demo-stack bundle (S3.T10–T13): amend `MILESTONES.md`'s Sprint 3 deliverables
and exit criteria so the sprint's "done" definition covers the four new
tickets *before* any of their code lands. Docs-only; establishes the
"amendment-first" precedent the S3.T13 retro cites (spec D0).

## Done

- Task: S3.T10–T13.D0, recorded in `PROGRESS.md`.
- Commits: `e8f56fb` (claim), `6a85c36` (MILESTONES amendment), plus this
  close-out commit (PROGRESS `[DONE]` + session-log line, issue 01 boxes
  ticked, this section).
- `MILESTONES.md` Sprint 3 "Deliverables" gains four bullets (multi-stage LB
  Dockerfile; repo-root `docker-compose.yml`; orchestrator-probe-shaped health
  endpoint contract; Sprint 3 retro). "Exit criteria" gains three (LB runs as a
  container via repo-root `docker compose up` alongside backends, Prometheus,
  and Grafana; health endpoint returns structured JSON with liveness and
  readiness semantics; retro written).
- `PROGRESS.md` status line reopened from "Sprint 3 complete" to "Sprint 3 in
  progress — T0.1–T9 done, T10–T13 remaining"; the scoping list extended to the
  fourth pass; the `[DONE] S3.T10–T13.D0` entry added.
- Issue 01 `Status: done`, boxes ticked.

## Verification

- The amendment commit `6a85c36` touches only `MILESTONES.md` — verified with
  `git show --stat`. No code touched; docs-only, TDD-exempt (AGENTS.md).
- The Sprint 4 deferred-deployment-target paragraph in `MILESTONES.md` was not
  modified (the amendment is confined to lines 47–62 of the Sprint 3 section).
- `make test` clean.

## Notes

- **Ticket-id choice.** Issue 01 carried no `S3.T…` number — its acceptance
  line writes `[DONE] S3.T…` as a placeholder. Recorded as `S3.T10–T13.D0`
  after the spec's own D0 label: it is the precondition for the whole
  T10–T13 bundle, and the form is distinguishably not issue 04's `S3.T10.0`
  (the probe-subcommand prefactor). A future reader grepping either `D0` or
  `S3.T10.0` lands on the right record.

## Open items / handoff notes

- The next ticket in the bundle is issue 02 (`S3.T12.0`, health-endpoint
  prefactors), now unblocked. Sequence per spec D1: T12 → T10 → T11 → T13.

## Review (two-axis, post-implementation)

Fixed point `01c5322`; diff `01c5322...HEAD`. Both axes returned **pass**.

- **Spec**: the amendment commit is byte-exact (`docs(milestones): add
  S3.T10–T13 deliverables and exit criteria`) and touches only `MILESTONES.md`
  (7 insertions, 0 deletions — the Sprint 4 paragraph is provably untouched);
  all four deliverable bullets and all three exit-criteria items are present;
  the D0 ordering (amendment before ticket code) holds. Two observations, both
  deliberately not acted on: (1) the historical S3.T9 changelog line and T9
  session section still say Sprint 3 "flipped to complete" — accurate at that
  instant, and point-in-time logs should not be rewritten; the authoritative
  status line now reads "in progress"; (2) the `S3.T10–T13.D0` id is invented
  but self-documented in the Notes section above.
- **Standards**: commit messages, claim/`[DONE]` protocol, session append, and
  the docs-only TDD exception all match `AGENTS.md`. One licensed deviation:
  `AGENTS.md` Step 6 asks for code + `PROGRESS.md` in one atomic commit, but
  issue 01 explicitly demands the amendment be a standalone `MILESTONES.md`-only
  commit, so the `[DONE]` mark lands separately. Noted as sanctioned, not
  fixed. Judgement-call prose duplication across the `PROGRESS.md` entry, its
  activity-log line, and the session log is inherent to the repo's audit-trail
  convention and matches every prior ticket.

## S3.T12.0 — Health-endpoint prefactor APIs (issue 02)

**Ticket.** `.scratch/s3-t10-t13-demo-stack/issues/02-t12-0-health-endpoint-prefactors.md`.
The three additive surfaces ticket 03 (`S3.T12` handlers) consumes, following
the S3.T0.1/T0.2/T0.3 prefactor pattern. Spec provenance: decisions **D8**
(`ProbeRoundComplete`), **D9** (`RecordProbe` / `lb_health_probe_total`), and
**D11** (`health_endpoint` config block). ADR-0014 is ticket 03's to write; this
ticket only forward-references it in comments.

**Scope boundary (AGENTS.md Step 2.5).**

- *In scope:* `Checker.ProbeRoundComplete()`, `Collector.RecordProbe`,
  `health_endpoint.listen` config + `configs/example.yaml`, tests, the
  ADR-0014 forward-reference comments, and the `PROGRESS.md` entry.
- *Out of scope:* the `/livez`/`/readyz`/`/startupz` handlers and their
  `http.Server` wiring, and ADR-0014 itself (all ticket 03); the Dockerfile
  (T10), root compose (T11), retro (T13); and any change to `internal/proxy`,
  `internal/circuit`, or `cmd/l7LoadBalancer` — the issue forbids the latter
  outright, and none was needed.

**What landed.**

- `internal/health/checker.go` — `Checker.backendCount` fixed at `New`, a
  shared `atomic.Int32` (`probed`) counting distinct backends that have
  completed a probe, and a one-shot `atomic.Bool` (`probeRoundComplete`) set by
  whichever prober's first `probeOnce` is the last of the set. `probeOnce` was
  restructured from early returns to `ok := probe(...)` with a single
  `markProbed()` tail so both outcomes count and the state-machine branches keep
  their existing bodies. A per-`prober` `counted` bool makes the fold idempotent
  per backend. The `Checker` concurrency doc comment now says the checker is
  immutable *except* the atomic first-round latch, rather than fully immutable.
- `internal/metrics/metrics.go` — `lb_health_probe_total{endpoint,status}`
  (`CounterVec`) registered on the private registry, and `RecordProbe(endpoint
  string, statusCode int)`. The `status` label is a class derived by a
  deliberate one-line duplicate of `internal/proxy`'s `statusClass` helper — the
  leaf may not import `internal/proxy` (ADR-0013 decision 1). `doc.go` reserves
  the metric name.
- `internal/config/config.go` — `HealthEndpoint HealthEndpointConfig`
  (`health_endpoint.listen`, `*string`, nil-means-omitted) and
  `DefaultHealthEndpointListen = ":8081"`. The metrics-listen
  normalize/validate logic was folded together with the new one into a single
  `normalizeListen(field, **string, def)` helper, a net reduction replacing
  `normalizeAndValidateMetrics`; an explicit empty or non-host:port value is
  rejected with the field named, and `KnownFields(true)` still rejects nested
  typos.
- `configs/example.yaml` — documents the block with the default `":8081"`.

**Tests (Red-first).** All three additions were written failing (compile-level
Red) before implementation:

- `TestProbeRoundCompleteLatchesAfterEveryBackendProbed` — table over 1/2/3
  backends, driving probers one at a time so the latch is asserted false until
  the last backend's first probe and true thereafter; plus
  `TestProbeRoundCompleteConcurrent`, the `-race` teeth check with one goroutine
  per backend.
- `TestCollectorRecordProbe` — counter series per endpoint/status class,
  including an explicit "raw `503` is never a label value" assertion; the
  exposition-endpoint test gained the new series line.
- Config: load cases (decode / omitted-nil / nested typo) and validate cases
  (omitted default / explicit kept / empty / non-host:port / out-of-range),
  mirroring the existing `metrics.listen` case shapes.

**Verification.** `make test`, `make test-race`, `go vet ./...`, and `make fmt`
all clean (the macOS `ld: malformed LC_DYSYMTAB` warnings are pre-existing
toolchain noise). `internal/proxy`, `internal/circuit`, and `cmd/l7LoadBalancer`
untouched.

## Review (two-axis, post-implementation) — S3.T12.0

Fixed point `4d2208b`; diff `4d2208b...HEAD` (commits `a0bc8c7` claim,
`4c59bc5` implementation). Both axes returned findings; the actionable ones
were fixed in a follow-up commit.

**Standards — findings.**

1. *Hard:* the diff added `lb_health_probe_total` and new label names but did
   not amend the frozen reservation in `docs/design/sprint-1-contracts.md`,
   matching the S3.T4 precedent (`lb_active_connections`). **Fixed** — both the
   name and the probe-scoped `endpoint`/`status` labels are now recorded there.
2. ADR-before-code (Step 2 #8): comments cite `ADR-0014` seven times before the
   file exists. Sanctioned explicitly by issue 02 (ticket 03 writes the ADR), so
   recorded, not changed.
3. *Minor:* `validateHostPort`'s doc comment still listed only `"listen"` /
   `"metrics listen"`; **fixed** to include `"health_endpoint listen"`.
4. Two new exported config symbols (`DefaultHealthEndpointListen`,
   `HealthEndpointConfig`) lacked the required `// ADR-0014 (S3.T12).`
   forward-reference; **fixed**.
5. Smells, all accepted: the one-line `statusClass` duplicate in `metrics.go`
   is forced by the leaf-import rule (ADR-0013 decision 1); `prober.markProbed`
   feature-envies `Checker` but reads naturally next to `probeOnce`;
   `prober.counted`/string-typed endpoints match existing precedent.

**Spec — findings.**

1. Same missing ADR-0014 forward-reference on the two config symbols → fixed.
2. D8 says the latch is *"backed by a single `atomic.Bool`"*; the
   implementation also adds a shared `atomic.Int32` to detect when the sweep is
   complete. **Accepted deviation, not a defect:** a single bool cannot know
   "every backend has been probed"; the *reported* latch is the one
   `atomic.Bool`, and the counter is the detection mechanism. ADR-0014 is the
   place ticket 03 can restate this precisely.
3. Ticket asks for a "fake probe transport"; the tests drive real
   `httptest` servers (the `statusServer`/`probeOnce` seam every S3.T1 test
   already uses). **Accepted:** consistency with the package's existing seam
   beats a one-off fake `RoundTripper`; observable behavior is identical.
4. Ticket says to verify `lb_health_probe_total` via
   `promtestutil.CollectAndCount`/`.ToFloat64` "exactly the pattern the
   existing `internal/metrics` tests use" — but those tests actually use the
   local `findMetric` + registry `Gather` helper, not `promtestutil`.
   **Accepted:** the implementation follows the *real* existing pattern, which
   is what the ticket's intent names.
5. `configs/example.yaml` edit: ticket 02 explicitly requires it, while bundle
   spec D20 says example.yaml "is unchanged". **Resolved in favour of the
   ticket** (it is the authoritative task scope and `TestExampleConfig`
   round-trips the result); the host-process nature of the file is preserved.
6. The `normalizeListen` refactor is technically more than "purely additive"
   but is behaviour-identical and a net line reduction; accepted.

**Net:** two of the spec/standards findings shared a root cause (missing
ADR-0014 forward-refs, fixed); the reservation gap was the only hard violation
(fixed). Everything else is a documented, accepted judgement call.

## S3.T12 — Health endpoint handlers, third listener, ADR-0014 (issue 03)

Bundle `.scratch/s3-t10-t13-demo-stack/`, ticket 03, consuming the S3.T12.0
prefactors (`Checker.ProbeRoundComplete`, `Collector.RecordProbe`,
`health_endpoint.listen`). Commits: `58123c7` claim, `b10fb80` ADR-0014,
`1806894` red tests + panic stub, and the atomic implementation commit.

### Design

- **Handlers live in `internal/health`** (`endpoint.go`) as
  `NewHandler(checker, reg, configLoaded, collector) http.Handler`. The package
  already imports `backend` and `metrics`, so no new dependency edge is added;
  the probe endpoint is part of the health subsystem it reports on. The
  constructor returns an `http.Handler` (backed by a `*http.ServeMux`) — no
  exported endpoint type, because `main` only ever needs the `http.Handler`.
- **`configLoaded` is a constructor bool**, not a hardcoded `true`: production
  exits before serving if `Load`/`Validate` failed, so it is always true in
  practice, but the contract (`/startupz`, `/readyz` checks) must be exercisable
  and a future reload path should report it honestly.
- **`selectable_backends` is a `*int`** so `/startupz` omits it while `/readyz`
  always includes it, even at zero — an absent field and a zero count must not
  look the same to an orchestrator.
- **Probe recording is inline in the handler** (`writeJSON` calls
  `RecordProbe(probe, code)` after writing the response), not via a
  `ResponseWriter` wrapper. There is no third-party middleware and no need to
  re-derive a status the handler already knows.
- **No new ADR for the package placement** — ADR-0014 records the ten contract
  decisions; placement follows the existing dependency graph.

### Red → Green

- Red: `endpoint_test.go` + a `panic("not implemented: S3.T12")` stub, run to
  confirm the panic (`TestLivezAlwaysAlive/fresh…`), committed as `1806894`.
- Green: replaced the stub with `endpoint.go`; `go test ./internal/health/...`
  passes, then `main.go` grew the third server.
- Tests cover: `/livez` 200/alive in four states; `/startupz` gate (pre-round,
  config-not-loaded, post-round, post-eviction one-way); the `/readyz` matrix
  (all-true, config-false, pre-round, empty-selectable, both-failing);
  `/readyz` live 200→503→200 via `MarkUnhealthy`/`MarkHealthy`; the
  `lb_health_probe_total{endpoint,status}` exposition for 2xx and 5xx, plus a
  `CollectAndCount(..., "lb_requests_total") == 0` assertion that probes never
  touch client traffic.

### Manual verification (`make run`, recorded)

Backends: `docker compose -f deployments/docker/docker-compose.yml up -d`
(all three answering 200 on 9001–9003). LB: `make build && ./bin/l7LoadBalancer
-config configs/example.yaml`.

Before the first probe round (t≈3s):

```
/livez    -> {"status":"alive"}
/startupz -> {"status":"not_ready","checks":{"config_loaded":true,"initial_probe_complete":false}}
/readyz   -> {"status":"not_ready","checks":{"config_loaded":true,"initial_probe_complete":false,"selectable_backends":3}}
```

After the first round (t≈9s):

```
/startupz -> {"status":"ready","checks":{"config_loaded":true,"initial_probe_complete":true}}
/readyz   -> {"status":"ready","checks":{"config_loaded":true,"initial_probe_complete":true,"selectable_backends":3}}
GET :8080/ -> 200
```

`docker compose ... stop`, then poll `/readyz` — 503 after ~16s (3 consecutive
probe failures × 5s interval), with the failing check visible:

```
/readyz   -> {"status":"not_ready","checks":{"config_loaded":true,"initial_probe_complete":true,"selectable_backends":0}}
/startupz -> {"status":"ready","checks":{"config_loaded":true,"initial_probe_complete":true}}   # one-way, stays ready
/livez    -> {"status":"alive"}
GET :8080/ -> 503
```

`docker compose ... start`, `/readyz` returns to 200 within ~12s. Probe counter
on `:9090/metrics` showed `lb_health_probe_total{endpoint,status}` per path
(2xx and 5xx cells), while `lb_requests_total` held only the client request
(`backend=""` on the no-healthy 503, the real backend on the 200). Startup log
carried `"health endpoint started","listen":":8081"`; SIGTERM produced
`shutdown signal received` → `shutdown complete` with a clean exit.

### Notes / scope

- `docs/architecture.md` is deliberately **not** touched: its Sprint 3
  subsystem write-up and decision index (which still stops at ADR-0012) are
  S3.T13's additive update per spec D42. AGENTS.md is updated (ADR-0014 row,
  key decision 21, `internal/health` component note) because it is the
  canonical guidance index.
- ADR-0014 records the `ProbeRoundComplete` implementation precision the
  S3.T12.0 review asked for: the reported latch is one `atomic.Bool`; the
  detection counter is an implementation detail.

## Review (two-axis, post-implementation) — S3.T12

Fixed point `18c8d8f`; diff `18c8d8f...HEAD` (commits `58123c7` claim, `b10fb80`
ADR, `1806894` red, `2c048ba` implementation). Both axes found no hard
violations; the actionable judgement calls were fixed in a follow-up commit.

**Standards — findings.**

1. *Hard:* none. TDD order, commit convention, package boundary, concurrency
   doc, metric reservation, and ADR↔code all conform.
2. *Judgement:* `initial := e.checker.ProbeRoundComplete()` read vaguely; renamed
   to `initialProbeComplete` in both handlers.
3. *Judgement:* the probe path travelled as `probe string` in
   `writeStatus`/`writeJSON` while the metric label and `RecordProbe` call it
   `endpoint`; unified on `endpoint`.
4. *Judgement:* the doc prose said `NewHandler` "returns a `ServeMux`" while the
   signature returns `http.Handler`; corrected in `AGENTS.md` (two places) and
   this log to "returns an `http.Handler` (a `ServeMux`)".
5. *Judgement:* the `checksBody` literal is built in both `startupz` and
   `readyz`. Accepted — the two gates genuinely differ and a shared helper would
   save two lines while obscuring that.

**Spec — findings.**

1. *Missing/partial:* none substantive. The `promtestutil.ToFloat64` wording and
   the "table-driven `/startupz`" wording are cosmetic; the tests use the
   project's actual `GatherAndCompare`/`CollectAndCount` seam and cover every
   envelope state.
2. *Scope creep:* AGENTS.md indexing (row, key decision 21, component note) is
   not in the ticket's acceptance list but follows the repo's own ADR-index
   rule; `Content-Type: application/json` and `ReadHeaderTimeout: 5s` mirror
   `metricsSrv`. Accepted.
3. *Latent correctness:* `/startupz` re-evaluates `configLoaded &&
   ProbeRoundComplete()` per request, so its D5 "stays 200 permanently"
   guarantee rests on both inputs being monotone (`ProbeRoundComplete` latches;
   `configLoaded` is fixed at construction) rather than on a handler-owned
   latch. **Resolved by documentation, not a latch:** adding a latch would be
   untestable with the frozen constructor signature and would need a dynamic
   `configLoaded` that no ticket asks for. The handler comment and ADR-0014
   decision 4 now state the permanence source explicitly and require a future
   dynamic config signal to be latched at the source.

**Net:** one latent correctness note closed by precision rather than machinery;
three naming/doc fixes; nothing behavioural changed after the implementation
commit.
