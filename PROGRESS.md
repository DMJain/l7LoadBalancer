# PROGRESS

Live state of the project. Every agent updates this file per the protocol in `AGENTS.md`.

## Current status

**Sprint 1 complete.** S1.T0 through S1.T10 (including S1.T0.5) are all [DONE].

**Sprint 2 complete.** All four algorithms are implemented and config-selectable: S2.T1.1 (ring), S2.T1.2 (`naiveConsistentHash` comparator), S2.T2 (`ConsistentHashBoundedLoads`), S2.T3 (`PowerOfTwoChoicesEWMA`), closed out by S2.T8 (retro / architecture close-out: deviations audit, ADR sweep, architecture-doc updates).

**Sprint 3 complete.** All of Sprint 3 (S3.T0.1–T13, including T6.5 and the four demo-stack tickets T10–T13) is [DONE]. Scoped in four passes: `.scratch/s3-t1-t3-health-passive-circuit/` (one spec + six tickets), `.scratch/s3-t4-t7-observability/` (one spec + ten tickets), `.scratch/s3-t6-5-t8-t9-chaos/` (one spec + three tickets: the reinstatement-gate fix plus the T8/T9 chaos tests), and `.scratch/s3-t10-t13-demo-stack/` (one spec + seven tickets: this MILESTONES amendment, the T12 prefactors + handlers, the T10 probe subcommand + Dockerfile, the T11 root compose, and the T13 retro). Done: S3.T0.1 (`MarkHealthy`/`MarkUnhealthy`), S3.T0.2 (round-trip observer fan-out), S3.T0.3 (probe/cooldown config schema), S3.T1 (active health checks), S3.T2 (passive outlier detection), S3.T3 (circuit breaker + `Registry.Selectable()` rename), S3.T4 (metrics package + `metrics.listen` endpoint), S3.T5.1 (logger test coverage), S3.T5.2 (`event`/`reason` transition vocabulary), S3.T5.3 (health transition log lines), S3.T5.4 (circuit transition log lines), S3.T6.1 (whole-request counter + histogram wired into the proxy), S3.T6.2 (active-connections gauge + its startup seeding), S3.T6.3 (backend-healthy gauge + its startup seeding), S3.T6.4 (circuit-state gauge + its startup seeding), S3.T7 (Grafana dashboard + Prometheus/Grafana observability demo stack), S3.T6.5 (active-checker reinstatement gate `==` → `>=`), S3.T8 (backend eviction/recovery chaos test + `docker stop` smoke), S3.T9 (circuit-breaker trip/half-open chaos test + 5xx-injection smoke), S3.T10–T13.D0 (MILESTONES.md amendment), S3.T12.0 (health-endpoint prefactor APIs: `ProbeRoundComplete`/`RecordProbe`/`health_endpoint.listen`), S3.T12 (health endpoint handlers + third listener + ADR-0014), S3.T10.0 (probe subcommand + version/commit injection), S3.T10 (multi-stage distroless Dockerfile + `.dockerignore` allowlist + baked `configs/docker.yaml` + OCI labels + ADR-0005 amendment), S3.T11 (repo-root `docker-compose.yml` + `prometheus-stack.yml` + ADR-0013 decision 18), S3.T13 (Sprint 3 retro + additive `docs/architecture.md` update, spec D39–D44).

**Sprint 4 in progress.** The zero-downtime reload bundle is scoped in `.scratch/s4-t0-t4-reload/` as one spec plus eight strictly serial tickets (each `Blocked by:` the one before): S4.D0 (tracking amendment), S4.T0 (application seam), S4.T1 (config diffing + ADR-0015), S4.T2 (registry snapshot swap), S4.T3.0 (reload hook-up APIs), S4.T3 (SIGHUP orchestration), S4.T4.0 (drain-cancel join + ADR-0016), and S4.T4 (drain lifecycle). S4.D0, S4.T0, S4.T1, S4.T2, S4.T3.0, S4.T3, S4.T4.0, and S4.T4 are [DONE]. The bundle's Sprint 4 exit criterion — SIGHUP with 1000 in-flight requests drops zero — is met, demonstrated by `TestChaosReloadDrainExitCriterion1000` (see the S4.T4 entry).

## Proposed tickets (awaiting owner approval)

Not approved, not claimed, not implemented. Surfaced by the S4 reload grilling (`.scratch/s4-t0-t4-reload/spec.md`, *Out of Scope*; decisions D1–D2) and recorded per AGENTS.md Step 2.5. (Prior S3 entries were approved and promoted into the `.scratch/s3-t6-5-t8-t9-chaos/` bundle: S3.T6.5, S3.T8 landed from that bundle and live in the Sprint 3 list above, and S3.T9 was approved and promoted into the same bundle and is claimed above.)

- A reload-outcome counter metric, `lb_config_reloads_total{result}`.
- Fix the pre-existing misclassification where a client cancellation is reported to observers as a backend failure (same cancellation-cause mechanism; belongs to Sprint 4's connection-lifecycle deliverable). On removed backends T2's suppression already hides it; on live backends it is unchanged.
- Hot-reload of the algorithm, health timing, circuit cooldown, listen addresses, or the drain window — changing them is rejected, not ignored.
- Pre-warming added backends (probing before the swap, so a blue/green reload has no empty-selectable window).

## Sprint 4 — Hard Subsystems

Scoped in `.scratch/s4-t0-t4-reload/` as one bundle spec plus eight strictly serial tickets (each `Blocked by:` the one before), with the `.0` split-outs following the S3.T12.0 precedent so each fits one context. The bundle's `spec.md` is the authoritative scope boundary; every story and implementation decision is tagged with exactly one ticket there.

- [DONE] S4.D0 — Tracking amendment: MILESTONES + PROGRESS (opencode, started 2026-09-25T00:58:00+05:30, completed 2026-09-25T01:05:00+05:30)
  - Goal: make the Sprint 4 reload work auditable before any of it is built, following the amendment-first precedent set by S3.T10–T13.D0: amend `MILESTONES.md`'s Sprint 4 deliverables with the application seam and add the S4.T0–T4 entries plus the four deferred follow-ups to `PROGRESS.md`. Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 46–47, decisions D1–D2.
  - Files: `MILESTONES.md`, `PROGRESS.md`, `.scratch/s4-t0-t4-reload/issues/01-d0-tracking-amendment.md`, `docs/sessions/2026-09-25-opencode.md`
  - Depends on: none
  - Acceptance: Sprint 4 deliverables gain one bullet — the programmatic application seam (build/run/reload) sourced from the Sprint 3 retro handoff — with the existing SIGHUP reload bullet, exit criteria, and deferred-deployment-target paragraph unchanged; `PROGRESS.md` gains `[TODO]` entries S4.T0, S4.T1, S4.T2, S4.T3.0, S4.T3, S4.T4.0, S4.T4 each with Goal/Depends on/Acceptance/Test approach drawn from tickets 02–08 and a pointer to `.scratch/s4-t0-t4-reload/spec.md`; four proposed tickets recorded (reload-outcome counter; client-cancellation-misclassified-as-backend-failure; non-backend hot-reload; pre-warming added backends); the status line reads Sprint 4 in progress; the substantive amendment lands as `docs(milestones): add S4 reload tickets and seam deliverable` touching only `MILESTONES.md` and `PROGRESS.md`.
  - Test approach: none — docs-only (AGENTS.md TDD exception).

- [DONE] S4.T0 — Application seam (`internal/app`: build + run) (opencode, started 2026-09-25T01:12:46+05:30, completed 2026-09-25T01:18:39+05:30) (issue 02)
  - Goal: a behaviour-preserving prefactor moving the whole wiring graph out of `main` into a new, importable `internal/app` package so production, the chaos harness, and tests build the system the same way, closing the Sprint 3 retro's `assemble` duplication debt. Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 48–50, decisions D3–D4.
  - Files: `internal/app/app.go`, `internal/app/app_test.go` (new); `cmd/l7LoadBalancer/main.go`, `cmd/l7LoadBalancer/main_test.go`, `test/chaos/assemble_test.go`, `test/chaos/helpers_test.go`, `AGENTS.md`, `PROGRESS.md`, `.scratch/s4-t0-t4-reload/issues/02-t0-application-seam.md`, `docs/sessions/2026-09-25-opencode.md`
  - Depends on: S4.D0
  - Acceptance: `internal/app` exposes **build** (validated config + logger → application value, or error) and **run** (serve until the context is cancelled, then today's graceful shutdown of the client, metrics, and health-endpoint servers in the current order and timeout); the application value exposes the client handler, metrics collector, and backend registry — what the chaos tests read today — and has **no reload operation yet** (T3 adds it); build performs today's wiring unchanged: collector; registry with every backend's series seeded; breaker installed as registry gate and registered as observer; selector from config; proxy with metrics and the latency, outlier, and circuit observers; active checker; health-endpoint handler; `main` becomes flags → `probe` short-circuit → load → validate → build → run on the shared SIGINT/SIGTERM context with the `starting`, `l7LoadBalancer starting`, `health checker started`, `metrics endpoint started`, and `health endpoint started` startup lines unchanged; the chaos-test assembly delegates to build with every chaos assertion unchanged; a build-level test proves the handler routes to configured backends and every backend's seeded gauge series exists; `AGENTS.md`'s package dependency graph and directory map show `internal/app` above proxy/health/circuit/metrics/balancer/backend/config, imported only by `main` and tests, the graph still acyclic; no ADR (the package decision is recorded here and in `AGENTS.md`); `make test`, `make test-race`, `go vet ./...`, `make fmt` clean, with a manual `make run` smoke recorded in the session log.
  - Test approach: the full existing suite stays green; a build-level test over the application value asserts routing and seeded series (spec seam 1); the chaos suite is the regression net for the assembly swap, with unchanged assertions. Prior art: the `test/chaos` harness, `cmd/l7LoadBalancer` main tests.

- [DONE] S4.T1 — Config diffing by backend identity + ADR-0015 (issue 03) (opencode, started 2026-09-25T01:27:00+05:30, completed 2026-09-25T01:31:00+05:30)
  - Goal: the pure comparison a reload is decided on — which backends are added, removed, and unchanged by **backend identity** (name, URL), and which non-backend fields differ — with **ADR-0015 (reload architecture)** written and committed before any code. Nothing calls the functions in production yet (T3 does). Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 5, 51–52, decisions D5–D7.
  - Files: `internal/config/diff.go`, `internal/config/diff_test.go` (new); `docs/adr/0015-reload-architecture.md` (new); `AGENTS.md`, `PROGRESS.md`, `.scratch/s4-t0-t4-reload/issues/03-t1-config-diffing.md`, `docs/sessions/2026-09-25-opencode.md`
  - Depends on: S4.T0
  - Acceptance: ADR-0015 is written and committed in the design step before any code, covering in-process registry snapshot swap vs SO_REUSEPORT process handoff; (name, URL) identity; the backends-only reject rule; the single-load versioned snapshot; pull-based ring rebuild keyed on version; the removed flag set before the swap; removed-backend observer suppression; the new-file-order rule; added backends starting unhealthy and admitted by one successful probe plus the asymmetry with startup; and the permitted empty-selectable blue/green window with its WARN — and `AGENTS.md`'s ADR table and key-decisions list gain its row; a pure diff function over (old, new) configs returns added and unchanged in the new file's order and removed in the old file's order; a name kept with a changed URL yields one removed plus one added entry, never an "updated" one; a pure non-backend-change function returns the names of differing fields among listen, algorithm, health, circuit, metrics, and health_endpoint, comparing resolved values so an omitted field and an explicit default compare equal; neither function logs, errors, or touches runtime state; Red-first table-driven tests cover identical, pure add, pure remove, a URL change under one name, reorder only, mixed, one case per non-backend field, and omitted-vs-explicit-default equality; `make test`, `make test-race`, vet, fmt clean.
  - Test approach: table-driven tests over the two pure functions (spec seam 3). Prior art: config validate/load table tests.

- [DONE] S4.T2 — Registry snapshot swap, removed-backend suppression, ring rebuild (issue 04) (opencode, started 2026-09-25T01:42:07+05:30, completed 2026-09-25T02:05:00+05:30)
  - Goal: make the registry's backend set replaceable while traffic flows — every selector sees the new set on its next `Select`, unchanged backends keep their instance and all their state, removed backends stop being selected and report nothing to any round-trip observer from the instant they are removed, the consistent-hash ring follows the new set, and the application keeps an atomic record of the loaded config. Verified through tests; nothing in production calls apply yet. Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 4, 7, 20, 21, 35, 39–43, 53–55, decisions D8–D12, design per ADR-0015.
  - Depends on: S4.T1
  - Acceptance: the registry holds one atomic pointer to an immutable snapshot of a monotonically increasing version and the ordered backend slice; `All`, `Selectable`, `Allow`, and `SetCircuitGate` keep their contracts, with an exported version accessor and version + backend set readable from one load; **apply** takes a diff, builds the next snapshot in the new file's order (reusing the existing instance for each unchanged identity, constructing a fresh one for each added), swaps it in, and returns the added and removed instances — single-writer, lock-free readers; apply marks each removed backend removed **before** the swap, via `Backend`'s new unexported atomic removed flag with a read accessor (method-only, amending ADR-0002 decision 5 via ADR-0015); removed backends leave `All`/`Selectable` at the swap; the proxy skips the whole observer fan-out when the request's backend is removed while the active-connection slot and gauge accounting are unaffected (`proxy.New(reg, sel)` unchanged); the consistent-hash bounded-loads selector caches (version, ring) and rebuilds from the snapshot's full backend list on a version mismatch via CAS (redundant concurrent builds are fine) while the naive comparator stays static; the application holds an atomic loaded-config record, set by build, with a read accessor; the sprint-1 contracts doc's concurrency-ownership rows for the registry backend set and the loaded config describe the as-built swap; Red-first tests prove unchanged instances keep pointer identity and state (health, active connections, EWMA, circuit), added are fresh, removed are marked and absent from `All`/`Selectable`, the new-file order, the version increases, a re-added identity gets a fresh instance, a request selected before removal and completing after it reaches no observer yet releases its slot, the ring reflects the new set after a version change with concurrent selects race-free under `-race`, and the loaded-config accessor works; the hand-off is recorded (apply is test-only until T3; fresh backends start healthy until T3); `make test`, `make test-race`, vet, fmt clean.
  - Test approach: registry apply and views through the package API; proxy observer suppression through the existing `httptest` proxy seam; consistent-hash selection across a version change plus a concurrent `-race` test (spec seam 3). Prior art: registry tests, proxy observer tests, ring and consistent-hash tests.
  - Files: `internal/backend/backend.go`, `internal/backend/backend_test.go`, `internal/backend/registry.go`, `internal/backend/registry_test.go`, `internal/balancer/consistent_hash.go`, `internal/balancer/consistent_hash_test.go`, `internal/balancer/hotkey_test.go`, `internal/proxy/proxy.go`, `internal/proxy/proxy_test.go`, `internal/app/app.go`, `internal/app/app_test.go`, `docs/design/sprint-1-contracts.md`, `PROGRESS.md`, `.scratch/s4-t0-t4-reload/issues/04-t2-registry-snapshot-swap.md`, `docs/sessions/2026-09-25-opencode.md`
  - Completion: `Registry` holds one `atomic.Pointer[registrySnapshot]` (`(version, ordered []*Backend)`, version 1 at construction, `Apply` increments); `All`/`Selectable` load it once, and new `Version()`/`Snapshot()` expose the version (Snapshot returns version + fresh slice from one load). `Apply(diff config.BackendDiff, newBackends []config.BackendConfig) (added, removed []*Backend, err error)` walks `newBackends` in new-file order and classifies each entry by `diff.Added`/`diff.Unchanged` (both keyed on the shared exported `config.BackendIdentity`, so the diff and the registry cannot restate the identity rule differently): an added identity is built fresh via the new shared `newBackend` helper, an unchanged one reuses the current instance, and an entry in neither set — or an unchanged entry absent from the snapshot — is reported as an error rather than published as a nil or mis-applied. It marks each `diff.Removed` instance removed **before** storing the next snapshot and returns the added/removed instances. `Backend` gains an unexported `atomic.Bool removed` with `IsRemoved()` (exported read) and unexported `markRemoved()` (Apply-only write). The proxy's `observe` fan-out returns early for a removed backend, leaving `activate`/`release` accounting untouched; the whole-request metrics hook is deliberately not part of that rule (see the session log decision). `ConsistentHashBoundedLoads` caches `(version, ring)` behind one `atomic.Pointer[ringCache]`, and `currentRing()` takes a no-allocation `Version()` fast path on the common case and rebuilds from `Snapshot()`'s backend list on a version mismatch via CAS (concurrent redundant builds are fine); `admits` also rejects `IsRemoved()` so a stale ring can never admit a backend removed between the ring load and the selectable load; the naive comparator is untouched. `App` gains `atomic.Pointer[config.Config] loadedCfg` as its **only** config record — `Build` stores it, `LoadedConfig()` reads it, and `Run`'s startup log reads it, so there is no second `cfg` field to diverge after a reload. The contracts doc's registry-backend-set and loaded-config rows now describe the as-built swap, and a `Backend.removed` row was added.
  - Hand-off (recorded per the issue): after T2, `Apply` exists and is exercised only by tests — nothing in production calls it; `App.loadedCfg` is read only by tests. Backends created by `Apply` start **healthy**, because `newBackend` still marks them healthy; S4.T3 changes reload-added backends to start unhealthy and be admitted by one successful probe. The diff/apply classification keys on the same `(name, URL)` identity as `config.DiffBackends`.
  - Deviation (judgement call, no ADR — ADR-0015 already records the decisions): `Apply` takes the ordered `newBackends` slice in addition to the diff, because `config.BackendDiff`'s separate `Added`/`Unchanged` slices preserve each group's order but not how the two interleave, so the new file's order is not recoverable from the diff alone. It also returns an `error` (unparseable URL) mirroring `NewRegistry`, even though `Validate` makes it unreachable for production callers; the alternative — panicking or silently publishing a nil instance — was judged worse.

- [DONE] S4.T3.0 — Reload hook-up APIs: checker add/remove, outlier forget, series deletion, initial-probe admission (issue 05) (opencode, started 2026-09-26T05:58:16Z, completed 2026-09-26T06:08:17Z)
  - Goal: the additive package-level APIs T3's reload operation calls, each verifiable on its own — the active checker can start and stop probing individual backends, a newly added backend is admitted after one successful probe with an `initial_probe` reinstatement, a probe in flight at remove never produces a transition, the outlier detector can forget a backend, and the collector can delete a backend's healthy and circuit-state series. Nothing calls these yet; the S3.T12.0 prefactor pattern. Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 27–31 (mechanism), 36, 58, decisions D15–D16, design per ADR-0015.
  - Depends on: S4.T2
  - Acceptance: the active checker gains **add** (start a prober for a backend) and **remove** (stop it), each prober owning its cancel function in a mutex-guarded map off the request path, with existing `Start` behaviour and unchanged backends' probers unaffected; a prober checks its own context after a probe returns and before applying any outcome, so a probe in flight at remove never marks health, logs, or writes a gauge (race test with a probe blocked on a channel gate: remove, release, assert no transition log and no gauge write); a backend started via add as "added" is unhealthy until **one** successful probe, then marked healthy with exactly one `health_reinstated` line with reason `initial_probe` (not `probe_recovered`) and its healthy gauge flipped 0 → 1, a failing first probe emits nothing, added probers probe immediately, and recovery after ejection still needs two successes; the probe-round-complete latch is never cleared by add/remove (ADR-0014 decision 4); the log vocabulary gains the `initial_probe` reason constant and the health-reinstated event's documentation is widened to cover first admission; the outlier detector gains **forget**, deleting a backend's window; the metrics collector gains deletion of a backend's `lb_backend_healthy` and `lb_circuit_state` series (all state labels) and explicitly not deletion of the active-connections series (T4); tests Red-first, one per surface; `make test`, `make test-race`, vet, fmt clean.
  - Test approach: checker add/remove and initial-probe admission through the direct prober/`probeOnce` seam, including the blocked-mid-flight remove `-race` test; outlier forget through its direct API; series deletion read back from the collector's private registry (spec seam 3). Prior art: checker tests, transition-log tests, chaos eviction tests.
  - Completion: `Checker.Add(ctx, b)` registers a reload-added prober (marks `b` unhealthy, admits on one successful probe with reason `initial_probe`); the shared unexported `add(ctx, b, added)` behind it also backs `Start(ctx)`, so startup probers keep their old schedule and admission rule. Each prober's `cancel` + `done` live in a mutex-guarded `map[*backend.Backend]*proberHandle`; `Remove(b)` cancels then waits on `done`, so no probe outcome can land after `Remove` returns. `probeOnce` returns early when its context is cancelled, before any health mark, log, or gauge write. `prober.added` selects the one-success admission threshold (`probeInitialSuccessesBeforeHealthy = 1`) and is consumed on admission so later recovery uses the normal two-success rule; added probers probe once immediately in `run`. `OutlierDetector.Forget(b)` deletes the window. `Collector.DeleteBackendHealthy`/`DeleteCircuitState` remove the healthy series and all three circuit-state series, leaving active connections. Logger gains `ReasonInitialProbe`, `EventHealthReinstated` docs widened; `CONTEXT.md`'s Probe entry notes the one-probe exception. Tests Red-first (`reload_test.go` + additions to `outlier_test.go`, `metrics_test.go`, `vocab_test.go`); `make test`, `make test-race`, `go vet`, `make fmt` clean.
  - Hand-off (recorded per the issue): nothing calls `Add`/`Remove`/`Forget`/the delete methods in production yet — T3's reload does. The added-backend `MarkUnhealthy` lives in `Add`, so T3 does not need to mark added instances unhealthy itself; T3 still seeds the added backend's gauge series (active connections 0, healthy 0, circuit closed) before calling `Add`. The removed backend's active-connections series is deliberately not deleted here (T4).

- [DONE] S4.T3 — SIGHUP reload orchestration (issue 06) (opencode, started 2026-09-26T06:56:42Z, completed 2026-09-26T07:02:16Z)
  - Goal: the first demoable reload — an operator edits the backend list and sends SIGHUP: unchanged backends keep serving with their state, added backends take traffic after one successful probe, removed backends stop being selected (their in-flight requests finish), and a broken or disallowed edit is rejected with a log line while the old config keeps serving. Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 1–3, 6, 8, 14–18, 22–23, 25–26, 37–38, 56–57, 59, decisions D13–D14, D16, design per ADR-0015.
  - Depends on: S4.T3.0
  - Acceptance: the application gains a **reload** operation taking a context and an already-parsed, validated config — it rejects the reload if the non-backend comparison (T1) is non-empty, else diffs against the loaded-config record, applies to the registry (T2), replaces the record, and hooks up checker add (as "added")/remove for added and removed backends, outlier forget for removed, seeded series for added (active connections 0, healthy 0, circuit closed), and deleted healthy and circuit-state series for removed, leaving the removed backends' active-connections series in place; `main` registers SIGHUP on a channel of buffer 1 and runs one reload-loop goroutine taking a context, the config path, the signal channel, the application, and a logger, parsing strictly → validating → reloading, with reloads never overlapping and bursts collapsing; the log vocabulary gains **config reloaded** (added/removed/unchanged counts, WARN when unchanged is zero else INFO, honouring the frozen "config reloaded" line) and **config reload failed** (reason: parse error, validation error, or the list of changed non-backend fields); `/readyz` reflects the live selectable set across a reload and `/startupz` stays latched; reload-loop tests (fake signal channel, temp config file, captured logger) cover success, parse failure, validation failure, a non-backend change rejected naming the field, a signal burst collapsing to one reload, and the previous config keeping serving after every failure; an application-level test (chaos harness via build) covers requests in flight on unchanged backends across a reload that adds a backend all succeeding, the added backend selectable only after its first successful probe, a removed idle backend leaving `All`/`Selectable` and its healthy/circuit series, and a successive reload diffing against the previous reload's config; a manual `make run` / edit / `kill -HUP` smoke is recorded in the session log; the hand-off is recorded (a removed backend's in-flight requests finish with no bound and its active-connections series is not deleted — both closed by T4); `make test`, `make test-race`, vet, fmt clean.
  - Test approach: application-level reload through the `internal/app` build seam; `main`'s reload loop driven by a fake signal channel, a temp config file, and a captured logger; captured structured logs for the new events (spec seams 1–2). Prior art: checker tests, transition-log tests, chaos eviction tests.
  - Completion: `App` gains `Reload(ctx, cfg)` (and stores the `health.OutlierDetector` it was already registering). `Reload` rejects a non-backend change whole (logging `config_reload_failed`/`non_backend_change` with the changed fields, returning an error), else diffs against `loadedCfg`, `reg.Apply`s, replaces the record after the apply succeeds, then hooks up removals **before** additions (checker `Remove`, outlier `Forget`, `DeleteBackendHealthy`/`DeleteCircuitState`; then seed the added backend's active 0 / healthy 0 / circuit closed, then checker `Add`), and logs one `config_reloaded` line carrying added/removed/unchanged counts at WARN when unchanged is zero else INFO. `Registry.Apply` now marks each freshly constructed added backend unhealthy **before** publishing the new snapshot, so an added backend is never selectable in the window between the swap and its first probe (closing T2's documented hand-off; startup backends still start healthy). `main` registers SIGHUP on a buffer-1 channel and runs `reloadLoop(ctx, path, sigCh, application, log)`: per signal it `drainSignals`s pending signals (bursts collapse to one reload), strictly loads, validates, and calls `Reload`; parse/validation failures log `config_reload_failed` with `parse_error`/`validation_error` and the loop keeps serving the previous config. `internal/logger/vocab.go` gains `EventConfigReloaded`/`EventConfigReloadFailed` and `ReasonParseError`/`ReasonValidationError`/`ReasonNonBackendChange` (+ defensive `ReasonApplyError`); `internal/logger/doc.go` gains the reload-scoped canonical fields; the frozen `config reloaded`/`config reload failed` lines in `docs/design/sprint-1-contracts.md` are extended with the event and counts/fields. Tests (Red-first): `internal/app/reload_test.go` (non-backend rejection naming the field, adds/removes with counts, blue/green WARN, exact added/removed series seeding/deletion, successive-diff composition, `/readyz` live + `/startupz` latched across a blue/green reload); `cmd/l7LoadBalancer/reload_test.go` (success, parse failure, validation failure, non-backend change, burst collapse, previous config keeps serving); `test/chaos/reload_test.go` (in-flight requests held open across a reload all 200 with unchanged instances, added backend admitted only after one successful probe with reason `initial_probe`, removed idle backend leaves All/Selectable and its healthy/circuit series with active left in place, successive diff re-adds a fresh instance); `internal/backend/registry_test.go` pins added-instances-unhealthy. `make test`, `make test-race`, `go vet`, `make fmt` clean; manual smoke recorded in the session log.
  - Hand-off (recorded per the issue): a removed backend's in-flight requests finish with **no bound** — there is no drain window and no cancellation, because that is S4.T4's; its `lb_active_connections` series is deliberately **not** deleted here (its in-flight requests still decrement it by name, and deleting it now would recreate it negative) — S4.T4 deletes it at drain completion. Added backends start unhealthy and are admitted by one successful probe (startup backends still start healthy); the probe-round-complete latch is never cleared by reload, so `/startupz` stays latched. `Reload` is single-writer (the reload loop); `proxy.New(reg, sel)` was not touched.

- [DONE] S4.T4.0 — Drain window config, retired context, and drain-cancel join + ADR-0016 (issue 07) (opencode, started 2026-09-26T07:20:21Z, completed 2026-09-26T07:29:42Z)
  - Goal: the mechanism that lets a removed backend's in-flight requests be cut off on demand — a `reload.drain_window` config field, a per-backend **retired** context, and the proxy joining each outbound request to it so that retiring a backend cancels its in-flight requests with a clean 502 logged as `window_expired` and counted as no backend's failure — with **ADR-0016 (drain lifecycle)** written first. Nothing retires a backend in production yet (T4 does). Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 10, 12–13, 44–45, 61–62, 64, decisions D18–D19, D21.
  - Depends on: S4.T3
  - Acceptance: ADR-0016 is written and committed before code, recording the retired context + after-func join (and why not a per-request goroutine or a cancel-func registry), the two-phase polling drain, cancel-at-window with 502, the truncated body after headers, abandonment on shutdown, and independence from later reloads, and amending ADR-0002 decision 5 — `AGENTS.md` gains its row; config gains a top-level `reload` block with `drain_window` (pointer duration, default 30s when omitted, zero/negative rejected naming the field, nested typos rejected by strict decoding), included in T1's non-backend comparison and documented in the example config; `Backend` gains an unexported retired context, created at construction, with method-only access to retire it and to observe it; the proxy derives each outbound request context with cancel-with-cause and registers an after-func on the chosen backend's retired context, stopped when the request completes, with no goroutine per request; a drain-cancelled request before headers gets a 502 with its slot released, a WARN line with reason `window_expired`, and observers not called (T2's removed flag), while a cancel after headers truncates the body and the success already recorded when headers arrived stands; the log vocabulary gains the `window_expired` reason; Red-first tests cover config load/validate cases, retiring a backend with gated in-flight requests yielding 502s with the reason and active connections returning to 0, requests on other backends unaffected, a post-headers retirement truncating without a recorded failure, and `-race` cleanliness; a benchmark of the proxy request path with the join is recorded in the session log; `make test`, `make test-race`, vet, fmt clean.
  - Test approach: config table tests over `Load`→`Validate`; proxy drain-cancel through the existing `httptest` proxy seam with gated backends and captured logs; a Go benchmark of the request path with the join (spec seam 3). Prior art: config tests, the proxy 100-concurrent active-connection leak test.
  - Completion: ADR-0016 (drain lifecycle) written and committed before code (`245eee7`); `AGENTS.md` gained its row and key-decision 23. `config.Config` gained a top-level `reload:` block (`ReloadConfig{DrainWindow *time.Duration}`, `DefaultDrainWindow = 30s`), defaulted/rejected by `Validate` naming `reload drain_window`, compared by `NonBackendChanges` (names `reload`), documented in `configs/example.yaml`. `Backend` gained an unexported `retiredCtx`/`retireCancel` created in `newBackend`, `ErrDrainWindowExpired`, and nil-tolerant `Retire()`/`RetiredContext()` (amending ADR-0002 decision 5 again). `proxy.ServeHTTP` derives the outbound context with `context.WithCancelCause` and registers one `context.AfterFunc` on `Retire()`'s context, stopped then cancelled by two deferred calls; `errorHandler` adds `reason=window_expired` when `context.Cause(r.Context())` is `ErrDrainWindowExpired`. `logger.ReasonWindowExpired` added and pinned (13 reasons); contracts doc gained the retired-ctx row and a drain-cancel example line. Tests Red-first (config/diff, backend retired context, vocab, three proxy drain tests); the shared `captureLogger` buffer became mutex-guarded after `-race` flagged the new server-goroutine logging, and the two chaos config builders gained the `Reload` field. Benchmark `BenchmarkProxyServeHTTP` (Apple M3, `-count=3`): ~35.1–35.5 µs/op, 41,448 B/op, 102 allocs/op, and `BenchmarkRetiredContextJoin` isolates the join at ~176–179 ns/op, 256 B/op, 5 allocs/op (no goroutine). `make test`, `make test-race`, `go vet ./...`, `make fmt` clean. Hand-off: nothing retires a backend in production yet (T4 does); the removed backend's active-connections series is still not deleted (T4); `reload.drain_window` is read by nothing yet (T4); the `backend drained` event and `idle` reason land with T4.

- [DONE] S4.T4 — Drain lifecycle and the zero-drop exit criterion (issue 08) (opencode, started 2026-09-26T07:40:39Z, completed 2026-09-26T07:46:04Z)
  - Goal: removed backends now **drain** — each finishes its in-flight requests until it is idle or the drain window expires, at which point the rest are cancelled; either way it is then forgotten, its last metric series is removed, and one log line says how the drain ended. This closes Sprint 4's first exit criterion — SIGHUP with 1000 in-flight requests drops zero — with a test that demonstrates it. Spec `.scratch/s4-t0-t4-reload/spec.md`, stories 9, 11, 19, 24, 32–34, 60, 63, 65–67, decisions D20, D22, design per ADR-0016.
  - Files: `internal/app/drain.go` (new), `internal/app/drain_test.go` (new), `internal/app/app.go`, `internal/app/reload_test.go`, `internal/logger/vocab.go`, `internal/logger/vocab_test.go`, `internal/logger/doc.go`, `internal/metrics/metrics.go`, `internal/metrics/metrics_test.go`, `test/chaos/reload_test.go`, `test/chaos/assertlog_test.go`, `docs/design/sprint-1-contracts.md`, `PROGRESS.md`, `.scratch/s4-t0-t4-reload/issues/08-t4-drain-lifecycle-exit-criterion.md`, `docs/sessions/2026-09-26-opencode.md`
  - Depends on: S4.T4.0
  - Acceptance: reload starts one drain goroutine per removed backend, waiting for whichever comes first — active connections at zero (polled ~100ms) or the drain window timer — and on expiry retires the backend (T4.0) and waits for active connections to reach zero; on completion it deletes the backend's active-connections series only if no current backend shares the name, and logs one **backend drained** event with reason `idle` or `window_expired` and the number of requests cancelled, the log vocabulary gaining the event and the `idle` reason; drain goroutines select on the process shutdown context and exit immediately without cleanup, with shutdown timing unchanged; a drain is independent of later reloads — re-adding the same identity creates a fresh backend (T2) and neither revives nor ends the draining one; **exit test case 1** (chaos harness, `-race`) holds 1000 concurrent requests on gated backends — some unchanged, some on a backend being removed — across a reload that adds one and removes one with a drain window longer than the hold, releases, all 1000 return 200, and afterwards the removed backend is absent from `All`, active connections are zero everywhere, one drained line with reason `idle` exists, no series is left for it, and unchanged backends are the same instances with circuit and EWMA state intact; **exit test case 2** uses a drain window shorter than the hold with the removed name re-added under a new URL, and sees the removed backend's in-flight requests get 502 with `window_expired`, one drained line with reason `window_expired` and the cancelled count, and the fresh same-name backend's circuit, outlier, and gauge state reflecting only its own traffic; a shutdown-during-drain test proves the drain goroutine exits without blocking; the PROGRESS entry notes the Sprint 4 exit criterion met, citing the test; `make test`, `make test-race`, vet, fmt clean.
  - Test approach: the chaos harness over the `internal/app` build seam with gated backends, `-race`, captured structured logs, and gauge series read from the collector's private registry (spec seams 1, 3). Prior art: chaos circuit tests, the proxy 100-concurrent active-connection leak test, config tests.
  - Completion: `internal/app/drain.go` gains `drainBackend(ctx, b, window)` — one goroutine per removed backend, started by `Reload` right after a removed backend's checker/outlier/series teardown — with a two-phase `waitActiveZero` (100ms poll, window-bounded phase one; unbounded phase two after `Retire`) and `completeDrain`, which deletes `lb_active_connections` only when `backendNameInUse` is false and logs one `backend_drained` line (INFO `idle`, WARN `window_expired`) carrying `cancelled`. Both waits select on the process shutdown context and return without cleanup (ADR-0016 decision 8). `logger` gains `EventBackendDrained`/`ReasonIdle` (vocabulary 8/14, pinned in `vocab_test.go`; `doc.go` drain fields updated); `metrics.Collector` gains `DeleteActiveConnections`. Tests: the Sprint 4 exit criterion `TestChaosReloadDrainExitCriterion1000` (1000 gated in-flight requests across an add+remove reload with a long window — all 200, removed absent, active zero everywhere, one `idle` drain, no gauge series left, unchanged instance pointer-identical with EWMA intact), `TestChaosReloadDrainWindowExpired` (short window, same name re-added under a new URL — removed request 502 `window_expired`, one drained line with `cancelled:1`, fresh backend admitted healthy with its active series surviving), `TestChaosReloadRemovedIdleBackendDrains`, and the app-level `TestDrainBackendShutdownExitsWithoutCleanup`. `TestReloadSeedsAddedAndDeletesRemovedSeries` updated to the closed T3→T4 hand-off (active series now deleted). Two-axis review found and fixed one real defect: a same-name remove+re-add in one reload seeded the fresh backend's `lb_active_connections` to 0 while the removed instance still decremented the shared series, driving it negative (spec stories 32/35); the active seed is now skipped when the name is also being removed, and case 2 asserts the gauge is exactly 0 at drain completion. `make test`, `make test-race`, `go vet ./...`, `make fmt` clean. **Sprint 4 exit criterion "SIGHUP with 1000 in-flight requests drops zero" is met — demonstrated by `TestChaosReloadDrainExitCriterion1000`.**

## Sprint 5

See `MILESTONES.md`. Tasks added as scoped.

